# 0508 손익 분석 탭 가구매 금액 DB 영속화 작업계획서

## 0. 이 문서의 위치

이 파일 하나만 보고 다음 세션에서 바로 작업에 들어갈 수 있도록 작성됨. 사용자의 방향성, 데이터 모델 변경, 영향받는 실제 파일/라인, 변경 전/후 로직, 회귀 위험 포인트, 작업 순서, 결정된 정책을 모두 담음. 0430 광고 업로드 단순화 / 0504 비용 시점 스냅샷화 계획서와 같은 형식.

작성 기준일: 2026-05-08

---

## 1. 사용자 방향성 (의사결정 근거)

### 1.1 현재 페인포인트 (사용자 진술 기반)

- 손익 분석 탭의 "가구매 금액" 입력값이 **클라이언트 state 한 개로만 관리**되고 있어 날짜를 옮기거나 페이지를 새로고침하면 입력값이 사라짐.
- 과거 날짜에 대해 그 시점에 적용했던 가구매 금액을 다시 확인할 수 없음 (이력 보존 X).
- 입력창에 값을 쓰고 새로고침 버튼을 누르면 사실상 묵시적으로 "저장"처럼 보이지만 실제로는 화면 보정에 그치고 다음 세션에서 흔적이 사라짐.

### 1.2 변경 후 방향

- **가구매 금액을 (storeId, date) 단위로 DB 에 영구 저장.** 키-밸류 단순 엔티티.
- **날짜 선택 시 그 날짜의 저장 값을 자동 로드.** 저장된 값이 있으면 표시, 없으면 0.
- **저장 버튼을 눌러야만 DB 반영.** 입력창 값 변경은 "드래프트" 상태이며 저장 누르기 전에는 DB / 화면 손익에 영향 X.
- **손익 계산 본체는 변경하지 않음.** 0504 작업의 옵션 B 정신 반복 — 가구매는 손익 페이지의 "예상 순이익" StatCard 한 곳에서만 표시 보정. 대시보드 / 일별 행 / 판매단위 상세는 무영향.

### 1.3 핵심 결정 사항 (사용자와 합의 완료)

1. **A안 채택 (클라이언트 차감 유지).** 백엔드 손익 계산 본체 (`calculateDailyProfitRows`, `calculateDashboardSummary`) 는 절대 건드리지 않음. 가구매는 `profits-view.tsx` 가 `summary.estimatedNetProfit - savedAmount` 로 표시 시점에 차감.
2. **차감 위치는 "예상 순이익" StatCard 한 곳만.** `roughProfit`, 대시보드, 일별 손익, 판매단위 상세, 다른 페이지에는 절대 반영하지 않음.
3. **DELETE 엔드포인트 만들지 않음.** "값을 0 으로 비우기" 는 PUT (amount=0) 한 번으로 충족. row 가 없는 상태와 amount=0 으로 저장된 상태는 의미상 동일 (= 가구매 없음).
4. **미저장 입력 상태에서 날짜 변경 시 그냥 폐기.** confirm 모달 / 경고 / 안내 메시지 없음. 사용자가 다른 날짜로 이동하면 새 날짜의 savedAmount 로 draftAmount 가 덮어씌워짐. (변경 최소화 / 가벼운 UX 정신)
5. **audit log 기록.** 가구매는 회계 보정 값이므로 변경 이력 추적. `domain="FAKE_PURCHASE"`, `action="UPSERT"`, `targetId="{storeId}-{date}"`, `beforeJson` (이전 amount 또는 null), `afterJson` (새 amount).
6. **단일 사용자 환경.** localhost admin 가정. 동시성 충돌 (두 클라이언트의 race) 은 마지막 쓰기 우선 — 별도 락 / 버전 필드 만들지 않음.
7. **표시 보정 기준은 `savedAmount`.** 입력 중인 `draftAmount` 는 화면 손익 카드에 영향 주지 않음. "저장된 값이 보이는 값" 단순 멘탈 모델.
8. **빈 문자열 입력은 0 으로 저장.** 사용자가 입력창을 비우고 저장하면 amount=0 PUT.

### 1.4 명시적으로 작업 스코프에서 제외

- 백엔드 손익 계산 본체 (`profit.service.ts` 의 calculateDashboardSummary / getDailySalesUnitDetail / calculateDailyProfitRows / calculateStoreDeliverySummary).
- 대시보드 페이지 / 판매단위 상세 / 일별 손익 행 / 보고서·엑셀 export 의 가구매 반영.
- 가구매를 판매단위별로 분배하는 로직 (현재처럼 "선택한 날짜의 전체 합계에서 일괄 차감" 만 유지).
- 가구매에 대한 권한 / 다중 사용자 동시 편집 대응.
- 미저장 변경 사항 표시 UI (isDirty 인디케이터, "저장 안 됨" 라벨 등) — 저장 버튼 disabled 상태로만 간접 표현.
- 가구매 회계 분류 변경 (세금 / 매출 차감 / 비용 처리 중 무엇으로 보는지) — 현 동작 (estimatedNetProfit 에서 단순 차감) 보존.

이 항목들은 향후 별도 프로젝트로 검토. 이번 작업이 만약 손익 계산 본체에 영향을 준다면 그건 회귀이며 즉시 수정 대상.

---

## 2. 영향받는 파일 목록

### 백엔드 (NestJS)

| 파일 | 역할 | 수정 범위 |
|---|---|---|
| `packages/shared/src/types.ts` | 공유 타입 | **신규 엔티티 1개** (`DailyFakePurchase`) + `DatabaseShape` 컬렉션 1개 (`dailyFakePurchases`) 추가 |
| `apps/backend/src/fake-purchase.service.ts` | **신규 파일** | `get(storeId, date)`, `upsert({ storeId, date, amount })` 두 메서드 |
| `apps/backend/src/app.module.ts` | NestJS 모듈 | `FakePurchaseService` provider 등록 |
| `apps/backend/src/app.controller.ts` | API 엔드포인트 | 신규 2개 엔드포인트 (`GET /daily-fake-purchases`, `PUT /daily-fake-purchases`) + 의존성 주입 추가 |
| `apps/backend/src/helpers.ts` | 손익 계산 + 유틸 | `createEmptyDatabase()` 에 신규 컬렉션 빈 배열 1줄 추가 (라인 30~45 부근) |
| `apps/backend/src/database.service.ts` | DB 영속화 | `STORAGE_TABLES` 매핑에 `daily_fake_purchases` 1줄 추가 (라인 27 부근) |
| `apps/backend/src/audit-log.service.ts` | 감사 로그 | **변경 없음** — 호출만 추가 |
| `apps/backend/src/run-tests.ts` | 테스트 하네스 | 신규 upsert / get / 빈 상태 / amount=0 케이스 테스트 추가 |
| `apps/backend/src/profit.service.ts` | 손익 응답 | **변경 없음** — A안 채택으로 백엔드 미반영 |
| `apps/backend/src/helpers.ts` `calculateDailyProfitRows`, `calculateDashboardSummary` | 손익 계산 본체 | **변경 없음** — A안 |

### 프론트엔드 (Next.js)

| 파일 | 역할 | 수정 범위 |
|---|---|---|
| `apps/frontend/components/profits/profits-view.tsx` | 손익 분석 화면 | state 분리 (`savedAmount` / `draftAmount` / `isSaving`), 저장 버튼 추가, 날짜 변경 시 fetch + 폐기 useEffect, 표시 차감 기준을 `savedAmount` 로 변경 |
| `apps/frontend/app/(main)/profits/page.tsx` | 페이지 entry | `getProfitsPageData()` 가 가구매도 함께 가져오도록 호출 흐름 그대로 (services.ts 안에서 묶음). 페이지 컴포넌트 자체는 거의 그대로 |
| `apps/frontend/lib/api/services.ts` | 페이지 데이터 페칭 | `getDailyFakePurchase(storeId, date)` 신규 함수, `getProfitsPageData()` 가 이를 호출해 `data.fakePurchase` 필드로 합쳐 반환 |
| `apps/frontend/lib/api/types.ts` | 타입 정의 | `DailyFakePurchaseResponse` 인터페이스 신설, `ProfitsPageData` 에 `fakePurchase: DailyFakePurchaseResponse \| null` 필드 추가 |
| `apps/frontend/lib/api/browser.ts` | CSR fetch | **변경 없음** — 기존 패턴 재사용 |
| `apps/frontend/app/api/daily-fake-purchases/route.ts` | **신규 프록시 라우트** | GET + PUT 한 파일에서 처리 (방식은 기존 라우트들과 일치) |

### 변경 없음 (영향 모니터링만)

- `packages/shared/src/normalization.ts`
- `apps/backend/src/sales-unit-auto-mapper.ts`
- `apps/backend/src/ad-mapping-engine.ts`
- `apps/backend/src/cost.service.ts`
- `apps/backend/src/order-sync.service.ts`
- `apps/backend/src/mapping-seed.service.ts`
- `apps/backend/src/operation.service.ts`
- 대시보드 페이지 / 판매단위 페이지 / 광고 페이지 / 매핑 페이지 / 비용 페이지 (모두 손익 / 매핑과 무관)

---

## 3. 데이터 모델 변경 상세

### 3.1 신규 엔티티 (`packages/shared/src/types.ts`)

```typescript
/**
 * 손익 분석 탭의 가구매 금액 보정 값. (storeId, date) 가 사실상의 복합 키.
 * row 가 없으면 = 가구매 없음 = amount 0 으로 표시.
 * amount=0 으로 저장된 row 와 row 가 없는 상태는 의미상 동일 (둘 다 "가구매 없음").
 */
export interface DailyFakePurchase {
  id: string;
  storeId: string;
  date: string;          // YYYY-MM-DD (KST 기준)
  amount: number;        // 0 이상의 정수 (원)
  createdAt: string;
  updatedAt: string;
}
```

`DatabaseShape` 에 컬렉션 1개 추가:

```typescript
export interface DatabaseShape {
  // ... 기존 16개 컬렉션 ...
  dailyFakePurchases: DailyFakePurchase[];   // 신규 (2026-05-08)
  operations: OperationRecord[];
  auditLogs: AuditLog[];
}
```

### 3.2 PostgreSQL 매핑 (`apps/backend/src/database.service.ts`)

`STORAGE_TABLES` 매핑에 1행 추가:

```typescript
{ key: "dailyFakePurchases", tableName: "daily_fake_purchases" },
```

JSONB 패턴 그대로 적용. 별도 마이그레이션 스크립트 불필요 (과거 날짜의 기본값은 자연스럽게 0 = row 없음).

### 3.3 `createEmptyDatabase()` (helpers.ts 라인 30~45)

신규 컬렉션 1개 추가:

```typescript
dailyFakePurchases: [],
```

### 3.4 마이그레이션 정책

**없음.** 기존 데이터에 가구매 row 가 존재하지 않았으므로 마이그레이션 대상이 0 건. 첫 배포 후 사용자가 저장하기 시작하면 자연스럽게 row 가 누적됨.

---

## 4. 백엔드 비즈니스 로직 (`apps/backend/src/fake-purchase.service.ts` 신규)

### 4.1 클래스 구조

```typescript
import { BadRequestException, Injectable } from "@nestjs/common";
import type { DailyFakePurchase } from "@patima/shared";
import { AuditLogService } from "./audit-log.service";
import { DatabaseService } from "./database.service";
import { StoreService } from "./store.service";
import { createId, formatApiSuccess, nowIso } from "./helpers";

@Injectable()
export class FakePurchaseService {
  constructor(
    private readonly databaseService: DatabaseService,
    private readonly storeService: StoreService,
    private readonly auditLogService: AuditLogService,
  ) {}

  get(storeId: string, date: string): { amount: number; exists: boolean; updatedAt: string | null }
  upsert(payload: { storeId: string; date: string; amount: number }): DailyFakePurchase
}
```

### 4.2 `get(storeId, date)`

**처리 순서**:
1. 입력 검증: `storeId` 비어있으면 `BadRequestException({ field: "storeId", reason: "STORE_ID_REQUIRED" })`. `date` 가 `YYYY-MM-DD` 형식 아니면 `BadRequestException({ field: "date", reason: "INVALID_DATE_FORMAT" })`.
2. `databaseService.getSnapshot()` 으로 스냅샷 획득.
3. `find((row) => row.storeId === storeId && row.date === date)` 로 행 검색.
4. 없으면 `{ amount: 0, exists: false, updatedAt: null }` 반환.
5. 있으면 `{ amount: row.amount, exists: true, updatedAt: row.updatedAt }` 반환.
6. `formatApiSuccess` 봉투 적용 후 반환.

> 권한: 읽기는 `ensureWritable` 호출 안 함 (조회 전용). 단 storeId 가 실존하는지 정도는 검증 가능 (`ensureStoreExists` 헬퍼가 있다면 사용). 변경 최소화 정신상 단순 find 만으로 충분.

### 4.3 `upsert(payload)`

**시그니처**:
```typescript
upsert(payload: {
  storeId: string;
  date: string;
  amount: number;
}): DailyFakePurchase
```

**처리 순서**:
1. `storeService.ensureWritable(payload.storeId)`.
2. 입력 검증:
   - `date` 가 `YYYY-MM-DD` 형식인지 (정규식 `^\d{4}-\d{2}-\d{2}$`). 아니면 `BadRequestException({ field: "date", reason: "INVALID_DATE_FORMAT" })`.
   - `amount` 가 finite number 인지, `>= 0` 인지, `Number.isInteger` 인지. 아니면 `BadRequestException({ field: "amount", reason: "INVALID_AMOUNT" })`.
   - 빈 문자열은 컨트롤러에서 0 으로 변환되어 들어옴 (4.4 참조).
3. 단일 `databaseService.write(draft => ...)` 트랜잭션:
   - `existing = draft.dailyFakePurchases.find((row) => row.storeId === storeId && row.date === date)`
   - `existing` 있으면 → `existing.amount = amount; existing.updatedAt = nowIso();` (in-place update)
   - 없으면 → 신규 row push:
     ```typescript
     {
       id: createId(),
       storeId,
       date,
       amount,
       createdAt: nowIso(),
       updatedAt: nowIso(),
     }
     ```
4. 트랜잭션 종료 후 `auditLogService.record({ domain: "FAKE_PURCHASE", action: "UPSERT", targetId: `${storeId}-${date}`, beforeJson: existing ? { amount: previousAmount } : null, afterJson: { amount } })`.
5. 갱신/생성된 row 를 반환.

> 0504 작업의 정신과 일치: 단일 트랜잭션 안에서 모든 변경, audit 는 트랜잭션 밖에서 fire-and-forget.

### 4.4 빈 문자열 처리

컨트롤러에서 body 의 amount 가 빈 문자열 / null / undefined 이면 `0` 으로 정규화. 이 변환은 컨트롤러에서 처리하고 서비스는 항상 number 만 받도록 시그니처 강제.

```typescript
@Put("daily-fake-purchases")
upsertFakePurchase(@Body() body: { storeId: string; date: string; amount: number | string | null }) {
  const amount = (() => {
    if (body.amount === null || body.amount === undefined || body.amount === "") return 0;
    const n = Number(body.amount);
    if (Number.isNaN(n)) {
      throw new BadRequestException({
        success: false,
        message: "가구매 금액은 0 이상의 정수여야 합니다.",
        errors: [{ field: "amount", reason: "INVALID_AMOUNT" }],
      });
    }
    return Math.floor(n);
  })();
  return formatApiSuccess(this.fakePurchaseService.upsert({ ...body, amount }));
}
```

---

## 5. 컨트롤러 (`apps/backend/src/app.controller.ts`)

### 5.1 의존성 주입 추가

constructor 에 `private readonly fakePurchaseService: FakePurchaseService` 추가. `app.module.ts` 의 `providers` 에도 등록.

### 5.2 신규 엔드포인트

```typescript
@Get("daily-fake-purchases")
getDailyFakePurchase(
  @Query("storeId") storeId: string,
  @Query("date") date: string,
) {
  return formatApiSuccess(this.fakePurchaseService.get(storeId, date));
}

@Put("daily-fake-purchases")
upsertDailyFakePurchase(
  @Body() body: { storeId: string; date: string; amount: number | string | null },
) {
  const amount = normalizeAmount(body.amount);
  return formatApiSuccess(
    this.fakePurchaseService.upsert({ storeId: body.storeId, date: body.date, amount }),
  );
}
```

`normalizeAmount` 는 4.4 의 변환 헬퍼. `app.controller.ts` 안에 private function 으로 두거나 `helpers.ts` 에 추가.

> NestJS 의 `@Put` 데코레이터를 import 추가 (현재 import 되어 있지 않을 수 있음).

### 5.3 기존 엔드포인트

**전부 그대로 유지.** 비용·광고·주문 어느 도메인도 가구매와 무관.

---

## 6. 프론트엔드 상세

### 6.1 데이터 페칭 (`apps/frontend/lib/api/services.ts`)

`getDailyFakePurchase` 신규 함수 + `getProfitsPageData` 안에서 호출.

```typescript
async function getDailyFakePurchase(storeId: string, date: string) {
  return fetchApi<DailyFakePurchaseResponse>({
    label: "fakePurchase",
    path: `/daily-fake-purchases?storeId=${storeId}&date=${encodeURIComponent(date)}`,
    fallback: { amount: 0, exists: false, updatedAt: null },
  });
}

export async function getProfitsPageData(query: { date?: string }): Promise<ProfitsPageData> {
  // ... 기존 흐름 ...
  const fakePurchaseResponse = primaryStore && targetDate
    ? await getDailyFakePurchase(primaryStore.id, targetDate)
    : null;

  return {
    // ... 기존 필드 ...
    fakePurchase: fakePurchaseResponse?.data ?? null,
    sources: collectSources(/* 기존 + fakePurchaseResponse */),
  };
}
```

> `fetchApi` / `collectSources` / `pickPrimaryStore` 등은 기존 패턴 그대로. 라인 번호는 손익 페이지의 기존 `getProfitsPageData` 본문을 직접 보고 매끄럽게 추가.

### 6.2 타입 (`apps/frontend/lib/api/types.ts`)

```typescript
export interface DailyFakePurchaseResponse {
  amount: number;
  exists: boolean;
  updatedAt: string | null;
}

export interface ProfitsPageData {
  // ... 기존 필드 ...
  fakePurchase: DailyFakePurchaseResponse | null;
}
```

mock-data.ts 에도 `fakePurchase: { amount: 0, exists: false, updatedAt: null }` 같은 기본값 추가 (있으면).

### 6.3 프론트 API 라우트 (`apps/frontend/app/api/daily-fake-purchases/route.ts`)

신규 단일 파일에서 GET + PUT 처리.

```typescript
import { NextRequest, NextResponse } from "next/server";
import { buildUpstreamEndpoint } from "@/lib/api/upstream";

export async function GET(request: NextRequest) {
  const url = new URL(request.url);
  const storeId = url.searchParams.get("storeId") ?? "";
  const date = url.searchParams.get("date") ?? "";
  const upstream = `${buildUpstreamEndpoint("/daily-fake-purchases")}?storeId=${storeId}&date=${encodeURIComponent(date)}`;
  const response = await fetch(upstream, { method: "GET", cache: "no-store" });
  const payload: unknown = await response.json().catch(() => ({}));
  return NextResponse.json(payload, { status: response.status });
}

export async function PUT(request: NextRequest) {
  const body = await request.json();
  const response = await fetch(buildUpstreamEndpoint("/daily-fake-purchases"), {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
    cache: "no-store",
  });
  const payload: unknown = await response.json().catch(() => ({}));
  return NextResponse.json(payload, { status: response.status });
}
```

> 0504 작업의 라우트 패턴과 동일. payload type guard 는 응답에서 message 만 추출하지 않으므로 단순 통과로 충분.

### 6.4 컴포넌트 (`apps/frontend/components/profits/profits-view.tsx`)

#### (a) state 추가

기존 `manualFakePurchaseAmount` (라인 39) 한 개를 다음으로 분리:

```typescript
const initialAmount = data.fakePurchase?.amount ?? 0;
const [savedAmount, setSavedAmount] = useState<number>(initialAmount);
const [draftAmount, setDraftAmount] = useState<string>(initialAmount === 0 ? "" : String(initialAmount));
const [isSaving, setIsSaving] = useState(false);
const [saveError, setSaveError] = useState<string | null>(null);
const [saveNotice, setSaveNotice] = useState<string | null>(null);
```

`isDirty` 는 derived value 로 계산 (state 아님):

```typescript
const draftAmountNumber = draftAmount.trim() === "" ? 0 : Number(draftAmount);
const isDirty = !Number.isNaN(draftAmountNumber) && draftAmountNumber !== savedAmount;
```

#### (b) 날짜 변경 시 폐기 + 새 값 로드

`useEffect([data.fakePurchase])` 로 SSR 결과가 갱신될 때 (즉 사용자가 새로고침 버튼을 눌러 `router.replace` 가 일어나 새 SSR 이 끝나면) `savedAmount`/`draftAmount` 를 새 값으로 덮어씀:

```typescript
useEffect(() => {
  const next = data.fakePurchase?.amount ?? 0;
  setSavedAmount(next);
  setDraftAmount(next === 0 ? "" : String(next));
  setSaveError(null);
  setSaveNotice(null);
}, [data.fakePurchase]);
```

> 사용자 결정 4번에 따라 미저장 변경분은 그냥 폐기. confirm 모달 없음.

#### (c) 입력창 + 저장 버튼

기존 form 의 가구매 입력 라벨 (라인 143~152) 을 다음으로 교체:

```tsx
<label className="block w-40">
  <span className="mb-2 block text-sm font-medium text-ink">가구매 금액</span>
  <input
    className="input-shell"
    type="number"
    placeholder="0"
    min={0}
    step={1}
    value={draftAmount}
    onChange={(event) => setDraftAmount(event.target.value)}
  />
</label>
<button
  className="button-shell button-secondary"
  type="button"
  disabled={!isDirty || isSaving}
  onClick={handleSaveFakePurchase}
>
  {isSaving ? "저장 중..." : "가구매 저장"}
</button>
```

> 기존 "새로고침" 버튼은 그대로 유지. `type="submit"` 인 새로고침 버튼이 form onSubmit 으로 트리거되므로 가구매 저장 버튼은 `type="button"` 으로 form submit 방지.

#### (d) `handleSaveFakePurchase` 핸들러

```typescript
const handleSaveFakePurchase = async () => {
  if (!data.primaryStore) return;
  if (Number.isNaN(draftAmountNumber) || draftAmountNumber < 0) {
    setSaveError("가구매 금액은 0 이상의 정수여야 합니다.");
    return;
  }
  setIsSaving(true);
  setSaveError(null);
  setSaveNotice(null);
  try {
    const response = await fetch("/api/daily-fake-purchases", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        storeId: data.primaryStore.id,
        date: selectedDate,
        amount: Math.floor(draftAmountNumber),
      }),
    });
    const payload = await readApiResponse<{ amount: number }>(response);
    if (!response.ok || !payload.success) {
      setSaveError(payload.message ?? "가구매 저장에 실패했습니다.");
      return;
    }
    const persisted = Math.floor(draftAmountNumber);
    setSavedAmount(persisted);
    setDraftAmount(persisted === 0 ? "" : String(persisted));
    setSaveNotice(`${selectedDate} 가구매 ${formatCurrency(persisted)} 저장됨`);
  } catch (error) {
    setSaveError(error instanceof Error ? error.message : "가구매 저장에 실패했습니다.");
  } finally {
    setIsSaving(false);
  }
};
```

> `readApiResponse` 는 기존 `lib/api/browser.ts` 의 헬퍼. response envelope 검증.

#### (e) 표시 차감 로직 변경 (현재 라인 209~228)

기존:
```typescript
const fakePurchaseStr = manualFakePurchaseAmount.trim();
const hasFakePurchase = fakePurchaseStr !== "" && !isNaN(Number(fakePurchaseStr)) && Number(fakePurchaseStr) !== 0;
const displayProfit = hasFakePurchase
  ? data.summary.estimatedNetProfit - Number(fakePurchaseStr)
  : data.summary.estimatedNetProfit;
```

변경 후 (savedAmount 기반):
```typescript
const hasFakePurchase = savedAmount > 0;
const displayProfit = hasFakePurchase
  ? data.summary.estimatedNetProfit - savedAmount
  : data.summary.estimatedNetProfit;
const hintText = hasFakePurchase
  ? "가구매 데이터 제외 완료"
  : data.summary.profitStatus === "INCOMPLETE_COST"
    ? "일부 원가 정보가 누락되었습니다."
    : "스토어 부담 배송비 차감 완료";
```

> `draftAmount` 는 화면 차감에 절대 영향 주지 않음. 결정사항 1.3-7번 보존.

#### (f) 저장 결과 표시

저장 성공/실패 메시지를 form 아래에 inline 으로 1~2초 노출:

```tsx
{saveError ? (
  <p className="text-sm text-rose-700">{saveError}</p>
) : saveNotice ? (
  <p className="text-sm text-emerald-700">{saveNotice}</p>
) : null}
```

토스트 형태도 가능하나, 0504 의 Toast 컴포넌트와 다른 톤 (이쪽은 명시적 사용자 행동에 대한 응답이라 inline 이 자연스러움).

### 6.5 기존 동작 보존

- 새로고침 버튼: 그대로 동작 (`router.replace` → SSR 재요청 → `data.fakePurchase` 재계산 → useEffect 가 state 동기화).
- 날짜 외 다른 필터 (스토어 선택 등) 변경 시에도 `data.fakePurchase` 가 갱신되면 자동 동기화.
- 다른 StatCard / DataTable / 판매단위 상세 페인은 가구매와 무관하게 그대로 동작.

---

## 7. 회귀 위험 포인트 (반드시 확인)

### 7.1 가구매 빈 컬렉션 상태

`dailyFakePurchases` 컬렉션이 비어있는 상태:
- `getDailyFakePurchase()` 가 `{ amount: 0, exists: false, updatedAt: null }` 반환.
- 프론트가 `savedAmount=0`, `draftAmount=""` 으로 렌더.
- 손익 카드에 차감 없음 (= 기존 "가구매 미입력" 상태와 동일 표시).
- 시스템은 죽지 않음. 이게 첫 배포 직후의 표준 상태.

### 7.2 동시 편집 (단일 사용자라 사실상 무관)

두 탭에서 동시에 PUT 보내면 마지막 쓰기가 이김. 본 시스템은 localhost admin 단일 사용자라 중요 X. 그래도 단일 `databaseService.write` 안에서 find + update/push 처리해 부분 실패 방지.

### 7.3 날짜 형식 불일치

`selectedDate` 가 빈 문자열이거나 잘못된 형식이면 PUT 거부. 프론트는 `<input type="date" required>` 로 빈 값 진입을 차단하지만, 백엔드도 정규식 검증 필수 (방어적).

### 7.4 음수 / 소수 / NaN 입력

`<input type="number" min={0} step={1}>` 로 1차 차단, 컨트롤러의 `normalizeAmount` 로 2차 검증 (`Math.floor`, `Number.isFinite`, `>= 0`). 음수면 `BadRequestException`.

### 7.5 amount=0 으로 저장 후 row 잔존

amount=0 으로 PUT 하면 row 가 만들어짐 (또는 기존 row 가 amount=0 으로 update). 이 상태와 row 가 없는 상태는 의미상 동일하지만, 데이터베이스에 row 가 누적되는 것. 운영상 문제 없음 (한 (storeId, date) 당 한 row 만 있음). 정리 원할 시 별도 PR 로 cleanup 스크립트.

### 7.6 미저장 입력 폐기 시 사용자 혼란

날짜 변경 직후 useEffect 가 draftAmount 를 새 값으로 덮어쓰면서 사용자가 입력하던 값이 사라짐. 사용자 결정 4번에 따라 confirm 없음. 이건 의도된 동작이며, 사용자가 저장을 잊은 책임으로 본다.

### 7.7 SSR 동기화 시점

`data.fakePurchase` 는 SSR 시점에 결정됨. 새로고침 버튼 → `router.replace` → SSR 재실행 → 새 props → useEffect 동기화. 만약 PUT 직후 SSR 재요청을 안 하면 데이터 새로고침 버튼을 누르기 전까지 다른 탭/장치에서 변경한 값이 보이지 않음. 본 작업 범위에선 PUT 직후 클라이언트에서 `setSavedAmount` 만 갱신하고 라우터 새로고침은 트리거하지 않음 (불필요한 SSR 비용 회피).

### 7.8 PostgreSQL 모드 직렬화

`DailyFakePurchase` 는 단순 객체라 JSONB 직렬화 그대로 동작. 단 `database.service.ts` 의 `STORAGE_TABLES` 에 등록 안 하면 PostgreSQL 모드에서 저장이 안 됨. JSON 파일 모드는 자동 동작. 0504 작업의 7.8 회귀와 동일 위험 포인트.

### 7.9 mock 데이터 호환성

`apps/frontend/lib/api/mock-data.ts` 의 `mockProfitsPageData` (있다면) 에 `fakePurchase: null` 또는 `{ amount: 0, exists: false, updatedAt: null }` 추가. 안 해도 SSR fallback 으로 동작은 하지만 타입 에러 가능성.

### 7.10 손익 페이지 외 영향

가구매가 손익 분석 탭의 "예상 순이익" StatCard 한 곳만 차감. 대시보드 페이지 / 판매단위 상세 / 일별 행 / 보고서 / 엑셀 export 에는 절대 반영 안 됨 (결정사항 1.3-2번). 만약 사용자가 향후 "대시보드에도 가구매 차감 반영해라" 고 요청하면 그건 별도 결정 / 별도 PR.

### 7.11 audit log 폭증

같은 (storeId, date) 의 amount 를 자주 수정하면 audit log 가 누적됨. 본 시스템은 단일 사용자 환경이고 일별 가구매가 자주 변경되지 않을 것이므로 운영상 문제 없을 것. 우려된다면 별도 PR 로 audit retention 정책.

### 7.12 type 호환성

`DailyFakePurchase` 신규 타입을 import 하는 모든 곳을 grep 으로 점검. 처음에는 service / controller 안에서만 쓰일 것.

---

## 8. 작업 순서 (권장)

이 순서대로 진행하면 중간에 빌드가 깨지지 않음.

### Step 1 — 스키마/타입 추가
1. `packages/shared/src/types.ts` 에 `DailyFakePurchase` 인터페이스 추가.
2. `DatabaseShape` 에 `dailyFakePurchases` 컬렉션 추가.
3. `apps/backend/src/helpers.ts` `createEmptyDatabase()` 에 빈 배열 추가.
4. `apps/backend/src/database.service.ts` `STORAGE_TABLES` 에 1행 추가.
5. **빌드 통과 확인.**

### Step 2 — 백엔드 서비스
1. `apps/backend/src/fake-purchase.service.ts` 신규 작성 (`get`, `upsert`).
2. `apps/backend/src/app.module.ts` providers 에 등록.
3. **빌드 통과 확인.**

### Step 3 — 백엔드 컨트롤러
1. `apps/backend/src/app.controller.ts` 에 `FakePurchaseService` 의존성 주입.
2. `@Get`, `@Put` 엔드포인트 2개 추가.
3. `normalizeAmount` 헬퍼 작성 (controller 내부 또는 helpers.ts).
4. `@Put` 데코레이터 import 확인.
5. **빌드 통과 확인 + curl/Postman 으로 GET / PUT 동작 검증** (선택).

### Step 4 — 프론트 API 라우트
1. `apps/frontend/app/api/daily-fake-purchases/route.ts` 신규 (GET + PUT).
2. **빌드 통과 확인.**

### Step 5 — 프론트 데이터 페칭 + 타입
1. `apps/frontend/lib/api/types.ts` 에 `DailyFakePurchaseResponse` + `ProfitsPageData` 갱신.
2. `apps/frontend/lib/api/services.ts` 에 `getDailyFakePurchase` + `getProfitsPageData` 갱신.
3. mock-data.ts (있으면) 에 기본값 추가.

### Step 6 — 프론트 컴포넌트
1. `apps/frontend/components/profits/profits-view.tsx` state 분리.
2. 저장 버튼 + 핸들러 추가.
3. `useEffect([data.fakePurchase])` 로 동기화 + 폐기.
4. 표시 차감 로직을 `savedAmount` 로 변경.
5. 저장 결과 inline 메시지 추가.

### Step 7 — 빌드 및 수동 테스트
1. `npx tsc --noEmit` 백엔드/프론트 모두 통과 확인.
2. 수동 시나리오:
   - 빈 상태에서 손익 페이지 진입 → 가구매 0, 입력창 빈칸, 저장 버튼 disabled.
   - 입력창에 1000 → 저장 버튼 활성, 손익 카드는 변화 없음 (savedAmount=0 유지).
   - 저장 버튼 클릭 → 손익 카드 차감 반영, 저장 알림.
   - 날짜 변경 → 새 날짜의 가구매 자동 로드 (없으면 0).
   - 입력 중 다른 날짜로 이동 → 미저장 변경 폐기 (조용히).
   - amount=0 으로 저장 → 화면 차감 사라짐, DB 에는 row 잔존.
   - 음수 / 빈 문자열 / 소수 입력 → 빈 문자열은 0 으로, 음수는 거부, 소수는 floor.

### Step 8 — 정리 (선택, 본 PR 외)

본 작업에서 옛 모델을 폐기할 게 없으므로 정리 작업 없음. 향후 가구매 정책이 바뀌면 별도 PR.

---

## 9. 체크리스트

- [ ] `packages/shared/src/types.ts` `DailyFakePurchase` 인터페이스 + `DatabaseShape` 컬렉션 추가
- [ ] `apps/backend/src/helpers.ts` `createEmptyDatabase()` 에 컬렉션 1개 추가
- [ ] `apps/backend/src/database.service.ts` `STORAGE_TABLES` 에 1건 추가
- [ ] `apps/backend/src/fake-purchase.service.ts` 신규 작성 (`get`, `upsert`)
- [ ] `apps/backend/src/app.module.ts` providers 에 등록
- [ ] `apps/backend/src/app.controller.ts` 신규 2개 엔드포인트 + 의존성 주입
- [ ] `apps/backend/src/app.controller.ts` `normalizeAmount` 또는 등가 검증 로직
- [ ] `apps/frontend/app/api/daily-fake-purchases/route.ts` 신규 (GET + PUT)
- [ ] `apps/frontend/lib/api/types.ts` `DailyFakePurchaseResponse` + `ProfitsPageData.fakePurchase`
- [ ] `apps/frontend/lib/api/services.ts` `getDailyFakePurchase` + `getProfitsPageData` 갱신
- [ ] `apps/frontend/components/profits/profits-view.tsx` state 분리 + 저장 버튼 + useEffect 동기화 + 표시 차감 변경
- [ ] mock-data.ts 기본값 (있으면)
- [ ] 백엔드 `tsc --noEmit` 통과
- [ ] 프론트 `tsc --noEmit` 통과 (sales-unit-cost-snapshots 와 무관한 0508 작업 관련 에러 0건)
- [ ] 수동 테스트 시나리오 7개 (Step 7.2)
- [ ] 회귀 검증 (대시보드 / 판매단위 상세 / 비용 / 광고 / 매핑 — 모두 무영향)

---

## 10. 주의사항 (작업 시 명심)

1. **손익 계산 본체 손대지 말 것.** `calculateDailyProfitRows()`, `calculateDashboardSummary()`, `getDailySalesUnitDetail()`, `calculateStoreDeliverySummary()` 어디든. 가구매 차감은 `profits-view.tsx` 컴포넌트의 표시 단계에서만.
2. **가구매 차감 위치는 "예상 순이익" StatCard 한 곳만.** `roughProfit`, 다른 페이지, 보고서 어디에도 반영 X.
3. **DELETE 엔드포인트 만들지 말 것.** PUT amount=0 으로 갈음.
4. **미저장 변경 폐기 시 confirm 모달 없음.** 사용자 결정 4번. UX 보호 장치 X.
5. **표시 차감 기준은 `savedAmount`.** `draftAmount` 는 화면 손익 카드에 영향 주면 안 됨. 입력 즉시 손익 미리보기 같은 동작 절대 만들지 마.
6. **단일 `databaseService.write` 트랜잭션** 안에서 upsert 처리. 부분 실패 방지.
7. **모든 BadRequestException 응답에 errors 배열 포함** (cost.service / ads.service 패턴 일치). 파일 업로드 아니므로 `fileName` 필드는 불필요.
8. **TypeScript strict 빌드 호환.** `Put` 데코레이터 import 누락 / 타입 미스매치 주의.
9. **마이그레이션 없음.** 빈 컬렉션 상태에서 시스템 안 죽음 보장. 첫 PUT 으로 row 생성.
10. **0504 작업과의 일관성.** 같은 정신: 단일 트랜잭션, 변경 최소화, audit log 가볍게, 옛 코드 hard delete 안 함, 마이그레이션 별도 PR. 본 작업은 0504 보다도 작은 분량 (1/3 ~ 1/2).
11. **입력창의 빈 문자열은 0 으로 저장.** 사용자가 입력창을 비우고 저장 버튼 누르면 amount=0 PUT. 거부 / 모달 없음.
12. **isDirty 는 derived value.** state 로 두지 말고 매 렌더 계산. 0504 의 Toast useEffect 와 같은 패턴 회귀 방지.

---

## 11. 작업 후 메모리 갱신

작업 완료 후 다음 항목을 메모리에 추가:
- `project_fake_purchase_db_0508.md` — 손익 분석 탭 가구매 금액을 (storeId, date) 단위로 DB 저장. 손익 계산 본체 무영향. 표시 차감은 `savedAmount` 기준. 미저장 변경은 날짜 변경 시 그냥 폐기. PUT amount=0 = 가구매 없음.

기존 메모리 인덱스에서 영향받는 항목:
- `project_cost_snapshot_excel_0504.md` — 비용 시점 스냅샷화와 무관. 그대로.
- `project_ad_upload_simplification_0430.md` — 광고 업로드와 무관. 그대로.
- 기존코드관찰.md 의 14장 손익 계산 / 14.4 VAT/배송 / 16장 자주 수정 파일 / 17장 최근 변경 이력 — 작업 후 갱신 필요. 14장에 가구매 표시 차감 절 추가, 16장에 fake-purchase.service.ts 추가, 17장에 "가구매 DB 영속화 (2026-05-08, 작업 완료)" 항목 추가.

---

(끝)
