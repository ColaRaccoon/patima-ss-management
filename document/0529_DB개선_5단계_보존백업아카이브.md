# 0529 DB 개선 5단계 - 보존, 백업, 아카이브 지침서

## 이 문서의 목적

이 문서는 DB 개선 5단계 작업자가 운영 DB 크기 증가를 장기적으로 통제하기 위해 보존 기간, archive, backup, restore 리허설을 구현할 수 있도록 만든 지침서다.

이번 단계의 핵심은 이미 도입된 `ORDER_RAW_PAYLOAD_RETENTION_DAYS`를 운영 절차로 완성하고, `auditLogs`, `operations`, 백업 파일까지 장기 누적 정책을 만드는 것이다. 보안/암호화 강화는 별도 보안 단계로 분리하고, 여기서는 DB 크기와 복구 가능성에 집중한다.

## 선행 조건

권장 선행 단계:

- 1단계 저장 안정화
- 2단계 row-level persistence
- 4단계 일별 결과 테이블

이미 완료된 작업:

- 주문/주문상품 rawPayload retention 정책
- cutoff 이전 rawPayload를 `null`로 정리하는 동기화 내 prune

이번 단계는 이를 수동 운영/백업 정책으로 확장한다.

## 현재 문제

남은 문제:

- 이미 쌓인 PostgreSQL JSONB payload를 운영자가 한 번 정리하는 수동 스크립트가 없다.
- rawPayload를 archive로 분리하는 절차가 없다.
- auditLogs가 무제한 증가한다.
- operations와 resultJson이 무제한 증가한다.
- 백업이 운영 DB 전체 snapshot에 가깝고 restore 리허설 절차가 없다.
- 백업 크기와 row 수를 점검하는 report가 없다.

## 이번 단계의 목표

1. rawPayload 수동 prune 스크립트를 추가한다.
2. rawPayload archive 정책을 문서화하고 가능하면 스크립트로 지원한다.
3. auditLogs 보존 기간과 prune/archive 스크립트를 추가한다.
4. operations 보존 기간과 prune/archive 스크립트를 추가한다.
5. 백업 크기/row 수/large payload 현황 report 스크립트를 추가한다.
6. restore 리허설 절차를 README 또는 별도 운영 문서에 남긴다.

## 이번 단계에서 하지 말아야 할 것

- 암호화 키 관리, 백업 암호화, 접근 제어는 보안 단계로 넘긴다.
- 클라우드 object storage 연동은 필수로 하지 않는다.
- 운영 DB row 자체를 임의 삭제하지 않는다.
- 주문/주문상품 핵심 필드를 삭제하지 않는다.
- daily summary를 삭제하지 않는다.
- 보안/인증 작업은 하지 않는다.

## 집/회사 DB 동기화 보존 조건

이 단계는 집/회사 PC 동기화 수단을 공식 운영 도구로 정비하는 단계다.

공통 원칙은 `0529_DB개선_공통_집회사동기화_필수조건.md`를 따른다.

필수 조치:

- `scripts/db-export.mjs`를 현재 schema 전체를 담는 schema-aware full export로 개선한다.
- `scripts/db-import.mjs`를 현재 schema 전체를 복원하는 schema-aware full restore로 개선한다.
- `scripts/README.md`에 집/회사 이동 절차를 최신화한다.
- `report-db-size`에 export 대상 table 누락 여부를 표시한다.
- restore 리허설 절차에 "회사 -> 집", "집 -> 회사" 이동 시나리오를 포함한다.

## 관련 파일

주요 추가/수정 파일:

- `scripts/prune-order-raw-payloads.mjs`
- `scripts/prune-audit-logs.mjs`
- `scripts/prune-operations.mjs`
- `scripts/report-db-size.mjs`
- `scripts/db-export.mjs`
- `scripts/db-import.mjs`
- `scripts/README.md`
- `README.md`
- `.env.example`

필요 시 수정:

- `apps/backend/src/raw-payload-retention.ts`
- `apps/backend/src/database.service.ts`
- `apps/backend/src/run-tests.ts`

참고:

- `0529수정계획.md`
- `0529_DB개선_4단계_일별결과테이블.md`

## 환경변수 설계

이미 존재:

```env
ORDER_RAW_PAYLOAD_RETENTION_DAYS=90
```

추가 후보:

```env
AUDIT_LOG_RETENTION_DAYS=180
OPERATION_RETENTION_DAYS=90
DB_ARCHIVE_DIR=./backups/archive
```

주의:

- rawPayload archive를 암호화할지는 보안 단계에서 결정한다.
- 이번 단계에서는 archive 파일 생성 위치와 dry-run 중심으로 둔다.

## 스크립트 공통 규칙

모든 prune/archive 스크립트는 다음 규칙을 따른다.

1. 기본은 dry-run이다.
2. 실제 변경은 `--yes`가 있을 때만 한다.
3. 실행 전 summary를 출력한다.
4. 대상 row count, 예상 변경 count, cutoff date를 출력한다.
5. `DATABASE_URL`이 없으면 file mode 대상인지 명확히 표시한다.
6. 백엔드가 켜진 상태에서 실행하면 snapshot 덮어쓰기 충돌 가능성을 경고한다.
7. 가능하면 PostgreSQL advisory lock을 잡는다.
8. 실패 시 non-zero exit code로 종료한다.

공통 옵션:

```powershell
node scripts/prune-order-raw-payloads.mjs --days 90
node scripts/prune-order-raw-payloads.mjs --days 90 --yes
node scripts/prune-audit-logs.mjs --days 180
node scripts/prune-operations.mjs --days 90 --keep-failed
node scripts/report-db-size.mjs
```

## rawPayload prune 스크립트

파일:

- `scripts/prune-order-raw-payloads.mjs`

대상:

- PostgreSQL table `orders`
- PostgreSQL table `order_items`
- payload 내부 `rawPayload`

동작:

1. `ORDER_RAW_PAYLOAD_RETENTION_DAYS` 또는 `--days`를 읽는다.
2. KST 기준 cutoff date를 계산한다.
3. `orders`의 기준일은 `paymentDatetime -> orderDatetime -> syncedAt`.
4. `order_items`의 기준일은 `paymentDate -> orderDate -> createdAt`.
5. cutoff 이전이고 `rawPayload`가 null이 아닌 row count를 출력한다.
6. `--yes`가 있으면 `jsonb_set` 또는 payload 재작성으로 `rawPayload`만 null 처리한다.
7. 주문/주문상품 row 자체는 삭제하지 않는다.

SQL 예시:

```sql
UPDATE orders
SET payload = jsonb_set(payload, '{rawPayload}', 'null'::jsonb, true),
    updated_at = NOW()
WHERE payload->'rawPayload' IS NOT NULL
  AND COALESCE(LEFT(payload->>'paymentDatetime', 10), LEFT(payload->>'orderDatetime', 10), LEFT(payload->>'syncedAt', 10)) < $1
```

주의:

- JSONB `null`과 key missing 차이를 테스트한다.
- 기존 앱 타입은 `rawPayload: null`을 기대하므로 key는 유지하고 값만 null로 둔다.

## auditLogs prune/archive

파일:

- `scripts/prune-audit-logs.mjs`

정책:

- 기본 보존 기간 180일
- 오래된 audit log는 archive JSONL로 내보낸 뒤 운영 DB에서 삭제 가능
- archive 없이 삭제하는 옵션은 처음에는 제공하지 않아도 된다.

archive 파일 예:

`backups/archive/audit_logs_2026-01-01_2026-03-31.jsonl`

주의:

- beforeJson/afterJson이 큰 경우 report에서 large log를 표시한다.
- 개인정보/비밀값 masking은 보안 단계에서 다룬다.

## operations prune/archive

파일:

- `scripts/prune-operations.mjs`

정책:

- 기본 보존 기간 90일
- `RUNNING`, `QUEUED`는 삭제하지 않는다.
- 실패 operation은 기본적으로 더 오래 보존하거나 `--keep-failed` 옵션을 둔다.
- retry chain이 있으면 최근 retry와 연결된 원본은 함께 보존한다.

삭제 가능 대상:

- `SUCCEEDED`이고 cutoff 이전
- `FAILED`이고 cutoff 이전이며 `--prune-failed` 명시 시

## DB size report

파일:

- `scripts/report-db-size.mjs`

출력 항목:

- table별 row count
- table별 total payload byte 추정
- orders/order_items rawPayload non-null count
- rawPayload null count
- auditLogs count와 평균 payload 크기
- operations count와 resultJson non-null count
- 가장 큰 payload top 20
- 백업 파일 크기 목록

PostgreSQL 예:

```sql
SELECT 'order_items' AS table_name,
       COUNT(*) AS row_count,
       SUM(pg_column_size(payload)) AS payload_bytes
FROM order_items;
```

## restore 리허설 문서

README 또는 별도 문서에 다음 절차를 추가한다.

1. 현재 DB 백업 생성
2. 임시 PostgreSQL DB 생성
3. 백업 restore
4. backend를 임시 DB로 실행
5. 주요 화면/API smoke test
6. row count와 summary 비교
7. 실패 시 원인 기록

권장 파일:

- `DB_RESTORE_RUNBOOK.md`

## 구현 순서

1. 공통 env parsing helper를 scripts에서 사용할 수 있게 만든다.
2. `report-db-size.mjs`를 먼저 구현한다.
3. `prune-order-raw-payloads.mjs` dry-run을 구현한다.
4. rawPayload prune `--yes`를 구현한다.
5. audit log archive/prune dry-run을 구현한다.
6. operation prune dry-run을 구현한다.
7. README에 운영 절차와 주의사항을 추가한다.
8. 가능하면 script unit test 또는 smoke test를 추가한다.

## 테스트 지침

필수 테스트:

1. rawPayload prune dry-run은 DB를 변경하지 않는다.
2. `--yes`가 있을 때만 rawPayload를 null 처리한다.
3. rawPayload prune은 주문/주문상품 row를 삭제하지 않는다.
4. cutoff 이후 rawPayload는 유지된다.
5. audit log prune은 cutoff 이전 row만 대상으로 잡는다.
6. operation prune은 RUNNING/QUEUED를 삭제하지 않는다.
7. report script가 table별 row count를 출력한다.

검증 명령:

```powershell
npm.cmd run typecheck --workspace @patima/backend
npm.cmd run test --workspace @patima/backend
npm.cmd run lint
node scripts/report-db-size.mjs
node scripts/prune-order-raw-payloads.mjs --days 90
```

운영 DB에 `--yes`를 붙이는 검증은 사용자 승인 후 별도 수행한다.

## 완료 기준

- rawPayload 수동 prune 스크립트가 dry-run과 `--yes`를 지원한다.
- auditLogs/operations 보존 정책이 문서화된다.
- 최소 하나 이상의 prune/archive 스크립트가 동작한다.
- DB size report로 row count와 payload 크기를 확인할 수 있다.
- restore 리허설 절차가 문서화된다.
- backend test와 lint가 통과한다.

## 다음 단계로 넘길 내용

5단계 완료 후 6단계에서 할 일:

- in-memory operation queue를 DB 기반 queue로 전환
- multi-backend/central DB 환경에서 작업 중복 실행 방지
- heartbeat, retry/backoff, lease 구조 도입

## 다음 세션 실행 프롬프트

```text
너는 Codex다. 작업 경로는 C:\Users\seong\Desktop\workspace\patima-naver-ss 이다.

반드시 먼저 0529_DB개선_5단계_보존백업아카이브.md 파일을 끝까지 읽고, 보안/인증 작업은 제외한 채 DB 보존/백업/아카이브 운영 도구를 구현해라.

이번 목표:
- rawPayload 수동 prune 스크립트를 dry-run 기본, --yes 실행 방식으로 추가한다.
- auditLogs와 operations의 보존/아카이브 정책을 스크립트 또는 문서로 구현한다.
- DB size report 스크립트를 추가한다.
- restore 리허설 절차를 문서화한다.
- 주문/주문상품 핵심 row와 daily summary는 삭제하지 않는다.

작업 후 다음을 실행해라:
- npm.cmd run typecheck --workspace @patima/backend
- npm.cmd run test --workspace @patima/backend
- npm.cmd run lint
- node scripts/report-db-size.mjs
- node scripts/prune-order-raw-payloads.mjs --days 90

최종 응답에는 변경 파일, 추가 스크립트, dry-run 결과, 실제 삭제/수정 여부, 테스트 결과를 포함해라.
```
