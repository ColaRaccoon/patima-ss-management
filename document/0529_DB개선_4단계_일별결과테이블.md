# 0529 DB 개선 4단계 - 일별 결과 테이블 도입 지침서

## 이 문서의 목적

이 문서는 DB 개선 4단계 작업자가 마진 계산을 결과 중심 저장 구조로 바꾸기 위한 지침서다.

이번 단계의 핵심은 마진 화면과 export가 요청마다 원천 주문/광고/비용 데이터를 전부 다시 훑지 않도록 `daily_sales_unit_profit`, `daily_store_summary` 성격의 결과 테이블을 도입하는 것이다. 이 단계는 계산식을 바꾸는 작업이 아니라, 현재 계산 결과를 저장하고 재사용하는 구조를 만드는 작업이다.

## 선행 조건

권장 선행 단계:

- 1단계 저장 안정화
- 2단계 row-level persistence
- 3단계 DB 조회 페이지네이션

3단계가 끝나지 않아도 구현은 가능하지만, 장기 성능 효과를 위해서는 2단계 row-level persistence가 먼저 끝나는 것이 좋다.

## 현재 문제

현재 마진 관련 주요 함수:

- `helpers.ts`의 `calculateDailyProfitRows()`
- `helpers.ts`의 `calculateDashboardSummary()`
- `profit.service.ts`의 summary/detail/export 관련 메서드

현재 구조:

- 요청이 들어올 때마다 주문상품, 광고비, 원가 스냅샷, 매핑 정보를 다시 집계한다.
- 같은 날짜를 여러 번 조회해도 매번 계산한다.
- 기간이 길어지면 export와 화면 조회가 느려진다.
- 마감된 과거 날짜도 계속 원천 데이터를 기준으로 재계산된다.

## 이번 단계의 목표

1. 일별 판매단위 마진 결과를 저장하는 타입과 DB collection을 추가한다.
2. 일별 스토어 요약 결과를 저장하는 타입과 DB collection을 추가한다.
3. 기존 계산 함수의 결과를 저장 가능한 row로 변환한다.
4. 주문/광고/비용/매핑 변경 시 영향 날짜만 재계산하는 entry point를 만든다.
5. ProfitService 조회는 저장된 결과를 우선 사용하고, 없으면 기존 계산으로 fallback한다.
6. 계산 결과에는 계산식 버전과 기준 메타를 남긴다.

## 이번 단계에서 하지 말아야 할 것

- 계산식을 임의로 바꾸지 않는다.
- 기존 `calculateDailyProfitRows()`를 삭제하지 않는다.
- 모든 과거 데이터를 한 번에 강제 backfill하지 않는다.
- UI를 대규모로 바꾸지 않는다.
- rawPayload archive 정책은 이번 단계의 직접 범위가 아니다.
- 보안/인증 작업은 하지 않는다.

## 집/회사 DB 동기화 보존 조건

이 단계에서 새 daily summary table/collection을 추가하면, 집/회사 PC 동기화 snapshot에도 반드시 포함해야 한다.

공통 원칙은 `0529_DB개선_공통_집회사동기화_필수조건.md`를 따른다.

필수 조치:

- `dailySalesUnitProfits`, `dailyStoreSummaries`를 `DatabaseShape`, `createEmptyDatabase()`, `DatabaseService.STORAGE_TABLES`에 추가한다.
- `scripts/db-export.mjs`가 새 table을 export하는지 확인한다.
- `scripts/db-import.mjs`가 새 table을 import하는지 확인한다.
- "재계산 가능하므로 export에서 제외"하지 않는다. 완전 동기화의 기본은 결과 테이블까지 포함하는 것이다.

## 관련 파일

주요 수정 파일:

- `packages/shared/src/types.ts`
- `apps/backend/src/helpers.ts`
- `apps/backend/src/profit.service.ts`
- `apps/backend/src/database.service.ts`
- `apps/backend/src/order-sync.service.ts`
- `apps/backend/src/ads.service.ts`
- `apps/backend/src/cost.service.ts`
- `apps/backend/src/order-mapping.service.ts`
- `apps/backend/src/campaign-mapping.service.ts`
- `apps/backend/src/run-tests.ts`

필요 시 추가 파일:

- `apps/backend/src/profit-summary.service.ts`
- `apps/backend/src/profit-summary-calculator.ts`
- `migrations/20260529-daily-profit-summaries.sql`

참고 파일:

- `0529수정계획.md`
- `보완필요사항.html`

## 데이터 모델 설계

### 1. DailySalesUnitProfit

`packages/shared/src/types.ts`에 추가할 후보:

```ts
export interface DailySalesUnitProfit {
  id: string;
  storeId: string;
  date: string;
  canonicalSalesUnitId: string | null;
  salesUnitDisplayName: string;
  totalQuantity: number;
  totalProductRevenue: number;
  totalDeliveryFeeAmount: number;
  totalVatAmount: number;
  totalVatAdjustedRevenue: number;
  totalApiCommission: number;
  totalAdCost: number;
  totalProductCost: number;
  totalDeliveryCost: number;
  totalFakePurchaseCost: number;
  totalOtherCost: number;
  netProfit: number;
  marginRate: number | null;
  roas: number | null;
  orderItemCount: number;
  adRowCount: number;
  costSnapshotId: string | null;
  mappingBasisHash: string | null;
  calculationVersion: string;
  calculatedAt: string;
  createdAt: string;
  updatedAt: string;
}
```

주의:

- 실제 필드명은 현재 `calculateDailyProfitRows()` 반환 shape와 맞춘다.
- 기존 API response와 호환되도록 불필요한 rename은 피한다.

### 2. DailyStoreSummary

후보:

```ts
export interface DailyStoreSummary {
  id: string;
  storeId: string;
  date: string;
  totalProductRevenue: number;
  totalDeliveryFeeAmount: number;
  totalVatAmount: number;
  totalVatAdjustedRevenue: number;
  totalApiCommission: number;
  totalAdCost: number;
  totalProductCost: number;
  totalDeliveryCost: number;
  totalFakePurchaseCost: number;
  totalOtherCost: number;
  netProfit: number;
  marginRate: number | null;
  orderItemCount: number;
  salesUnitCount: number;
  calculationVersion: string;
  calculatedAt: string;
  createdAt: string;
  updatedAt: string;
}
```

### 3. DatabaseShape 확장

`DatabaseShape`에 추가:

```ts
dailySalesUnitProfits: DailySalesUnitProfit[];
dailyStoreSummaries: DailyStoreSummary[];
```

`createEmptyDatabase()`에도 빈 배열 추가.

`DatabaseService.STORAGE_TABLES`에도 추가:

- `daily_sales_unit_profits`
- `daily_store_summaries`

2단계 row-level persistence가 끝났다면 index도 추가:

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_daily_sales_unit_profit_unique
ON daily_sales_unit_profits ((payload->>'storeId'), (payload->>'date'), COALESCE(payload->>'canonicalSalesUnitId', ''));

CREATE UNIQUE INDEX IF NOT EXISTS idx_daily_store_summary_unique
ON daily_store_summaries ((payload->>'storeId'), (payload->>'date'));
```

## 계산 버전과 기준 메타

상수 추가:

```ts
export const PROFIT_CALCULATION_VERSION = "profit-v1";
```

메타:

- `calculationVersion`
- `calculatedAt`
- `costSnapshotId`
- `mappingBasisHash`
- 필요 시 `sourceOrderUpdatedUntil`, `sourceAdUpdatedUntil`

`mappingBasisHash`는 처음에는 간단히 다음 값을 stable hash한다.

- storeId
- date
- 해당 날짜 주문상품의 `orderSourceSignatureId -> canonicalSalesUnitId`
- 해당 날짜 광고 signature의 `canonicalSalesUnitId`

너무 크면 초기 버전에서는 `null` 허용하고, 후속 단계로 남긴다.

## 서비스 설계

### 1. ProfitSummaryService 추가

권장 메서드:

```ts
async recalculateStoreDates(params: {
  storeId: string;
  dateFrom: string;
  dateTo: string;
  reason: "ORDER_SYNC" | "AD_UPLOAD" | "COST_CHANGE" | "MAPPING_CHANGE" | "MANUAL";
}): Promise<{
  recalculatedDateCount: number;
  salesUnitRowCount: number;
  storeSummaryRowCount: number;
}>
```

동작:

1. 날짜 range를 순회한다.
2. 기존 `calculateDailyProfitRows()`와 `calculateDashboardSummary()`를 사용해 계산한다.
3. 해당 날짜의 기존 summary rows를 제거하거나 upsert한다.
4. 결과 rows를 저장한다.
5. operation/audit에 재계산 결과를 남긴다.

### 2. ProfitService 조회 우선순위

조회 시:

1. 요청 날짜/기간에 저장된 summary가 모두 있으면 summary table 사용
2. 일부 날짜가 없으면
   - 초기 구현은 missing 날짜를 기존 계산 fallback으로 계산
   - 응답에 `summarySource: "STORED" | "LIVE_FALLBACK" | "MIXED"` 같은 내부 메타를 추가할지 검토
3. 기존 API shape는 유지

주의:

- 프론트가 모르는 새 필드를 추가해도 되지만 기존 필드는 제거하지 않는다.
- export도 같은 정책을 사용한다.

### 3. 변경 이벤트 연결

재계산 trigger:

- 주문 동기화 성공: 동기화 date range
- 광고 업로드 confirm/delete: 업로드 reportDate 범위
- 비용 설정 생성/수정/삭제: effectiveFrom 이후 영향 범위
- 주문 매핑 재계산/수동 매핑: 영향받은 주문상품 날짜
- 광고 매핑 수동 변경: 영향받은 광고 row 날짜

이번 단계에서는 모든 trigger를 완벽히 자동 연결하지 않아도 된다. 우선 다음을 필수로 한다.

필수:

- 수동 재계산 endpoint 또는 service method
- 주문 동기화 후 date range 재계산
- 광고 confirm/delete 후 reportDate 재계산

후속 가능:

- 비용/매핑 변경 영향 범위 정교화

## 구현 순서

1. shared types에 daily summary 타입을 추가한다.
2. `createEmptyDatabase()`와 `DatabaseShape`를 확장한다.
3. `DatabaseService.STORAGE_TABLES`에 새 table을 추가한다.
4. 기존 계산 결과를 `DailySalesUnitProfit`, `DailyStoreSummary`로 변환하는 helper를 만든다.
5. `ProfitSummaryService`를 추가한다.
6. 수동 재계산 메서드와 controller endpoint를 추가한다.
7. 주문 동기화 성공 후 summary 재계산을 연결한다.
8. 광고 업로드 confirm/delete 후 summary 재계산을 연결한다.
9. `ProfitService` 조회가 저장된 summary를 우선 사용하게 한다.
10. 테스트를 추가한다.

## 테스트 지침

필수 테스트:

1. `createEmptyDatabase()`에 daily summary 배열이 포함된다.
2. 기존 계산 결과가 daily summary row로 변환된다.
3. 같은 store/date/salesUnit 재계산 시 중복 row가 생기지 않는다.
4. 주문 동기화 후 해당 날짜 summary가 저장된다.
5. 광고 confirm 후 해당 날짜 summary가 갱신된다.
6. ProfitService 조회는 저장된 summary가 있으면 원천 계산 대신 저장 결과를 사용한다.
7. summary가 없는 날짜는 기존 live 계산 fallback으로 동작한다.
8. 계산 버전과 calculatedAt이 저장된다.
9. 다른 storeId summary와 섞이지 않는다.

검증 명령:

```powershell
npm.cmd run typecheck --workspace @patima/shared
npm.cmd run typecheck --workspace @patima/backend
npm.cmd run test --workspace @patima/backend
npm.cmd run lint
```

## 완료 기준

- daily sales unit profit table/collection이 생긴다.
- daily store summary table/collection이 생긴다.
- 수동 또는 자동으로 특정 날짜 범위를 재계산해 저장할 수 있다.
- ProfitService가 저장된 summary를 우선 사용할 수 있다.
- 기존 마진 계산 API shape가 깨지지 않는다.
- backend/shared typecheck와 backend test가 통과한다.

## 다음 단계로 넘길 내용

4단계 완료 후 5단계에서 할 일:

- rawPayload 수동 prune/archive 스크립트 추가
- auditLogs/operations 보존 기간과 archive 정책 추가
- 백업/복구 리허설 스크립트 정리

## 다음 세션 실행 프롬프트

```text
너는 Codex다. 작업 경로는 C:\Users\seong\Desktop\workspace\patima-naver-ss 이다.

반드시 먼저 0529_DB개선_4단계_일별결과테이블.md 파일을 끝까지 읽고, 보안/인증 작업은 제외한 채 일별 마진 결과 저장 구조를 구현해라.

이번 목표:
- daily sales unit profit과 daily store summary 타입/저장소를 추가한다.
- 기존 calculateDailyProfitRows/calculateDashboardSummary 결과를 저장 가능한 row로 변환한다.
- ProfitService는 저장된 summary를 우선 사용하고 없으면 기존 계산으로 fallback한다.
- 주문 동기화와 광고 confirm/delete 후 관련 날짜 summary를 갱신한다.
- 계산식 자체는 바꾸지 않는다.

작업 후 다음을 실행해라:
- npm.cmd run typecheck --workspace @patima/shared
- npm.cmd run typecheck --workspace @patima/backend
- npm.cmd run test --workspace @patima/backend
- npm.cmd run lint

최종 응답에는 변경 파일, 새 summary 타입, 재계산 trigger, fallback 방식, 테스트 결과를 포함해라.
```
