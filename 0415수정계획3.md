# 2026-04-15 수정 계획 3 — 옵션정보만 있는 시그니처 식별성 개선

`0415수정계획.md` (관리코드 매핑 + 스토어 전체 광고비), `0415수정계획2.md`(판매단위 그룹화), 그리고 2026-04-15 판매단위 리셋(33→22) 완료를 전제로 작성. 이번에는 **수동 매핑 UX의 근본 문제**를 해결한다.

---

## 배경

### 현재 문제

주문 시그니처 목록/수동 매핑 UI에서 상품명을 식별할 수 없는 세 가지 패턴이 발견됐다.

**패턴 1. 상품명 완전 누락**

```
여성용1+1 || 3513194860
```

- `rawProductNameSnapshot`이 빈 문자열 / null.

**패턴 2. 상품명이 옵션 값으로 덮어써짐 (함께배송 케이스)**

```
l || [함께배송⭐추가할인]러닝깔창: l
```

- `rawProductNameSnapshot = "L"` ← 실제로는 사이즈 옵션 값인데 상품명 자리에 들어가 있음.
- `rawOptionInfoSnapshot = "[함께배송⭐추가할인]러닝깔창: L"` ← **진짜 상품명은 여기에** 있음.
- 2026-04-15 샘플: 최소 수십 건.

**패턴 3. products 테이블 엔트리 자체가 빈 껍데기**

- `externalProductId`로 `products`를 조회해도 `displayName`이 null.
- 즉 상품 카탈로그 동기화가 불완전.

세 패턴 모두 결과는 같다: 사용자가 시그니처를 봐도 어떤 상품인지 모른다.

### 원인 추정

1. **네이버 주문 수집 시점에 상품명이 누락**되는 케이스가 있다.
   - 삭제된 상품, 외부 이관된 상품, 옵션정보만 필수인 특정 상품 형태 등.
   - `OrderItem.rawProductName`은 비어있지 않더라도, 시그니처 집계 시 `rawProductNameSnapshot`이 "" 로 저장된 케이스.
2. 과거 수집 데이터의 스키마 변경 이력(`normalizedProductName` 도입 이전 데이터)으로 snapshot 필드가 누락된 경우도 가능.
3. 시그니처 생성 로직이 `rawProductNameSnapshot`을 order_item 최초 하나에서 복사하는 방식이면, 해당 item이 상품명 없는 상태라 시그니처도 비게 된다.

### 데이터 복구 가능성

**있다.** 같은 `orderSourceSignatureId`를 공유하는 `OrderItem`들을 들여다보면 다음이 살아있는 경우가 많다:

- `externalProductId` (네이버 상품 ID)
- `rawProductName` — order_item 레코드에는 남아있을 수 있음
- `products` 테이블에 `externalProductId` 키로 매칭되는 상품 정보

또한 `commerce_api_credentials`가 있으면 **네이버 커머스 API**로 해당 상품 ID를 조회해 상품명을 받아올 수 있다.

---

## 해결 방향

세 단계로 나눈다. 우선순위: **A → B**. C는 여력 있을 때.

### A. UI 보조 표시 (비파괴적, 빠른 효과)

시그니처 표시 영역에서 "신뢰 가능한 상품명"이 없으면 다음 fallback 체인을 차례로 시도하여 **보조 라벨**을 붙인다.

#### A-0. "의미 있는 상품명"의 정의

단순 non-empty 체크로는 부족하다(패턴 2). 다음 중 하나라도 참이면 *의미 없음*으로 취급하고 fallback으로 내려간다:

- 빈 문자열 / null / 공백만.
- 길이 ≤ 2 (예: "L", "XL" 등 대부분 사이즈 옵션).
- 정규식 `^(xx?s|s|m|l|xx?l|free|one|원사이즈|\d+)$` 매칭 (대소문자 무시).
- 또는 `rawOptionInfoSnapshot`에 **자기 자신을 포함**하는 패턴(`"L"`이 옵션 안에 있으면 옵션 값이 상품명 칸으로 새어들어갔을 가능성).

#### A-1. Fallback 체인

1. **order_items의 rawProductName** 중 A-0 기준으로 "의미 있는" 값이 있으면 사용.
2. **rawOptionInfo 파싱** — `[함께배송⭐추가할인]러닝깔창: L` 같은 패턴에서 상품명 추출. 규칙 후보:
   - `/\][^:]+:/` → `]` 뒤 `:` 앞의 토큰이 상품명.
   - `/\[[^\]]*\]\s*([^:]+?)\s*:/` 로 캡처 그룹.
   - 추출값에 또 A-0 기준 적용 (연쇄 오염 방지).
3. **products 테이블 조회** (`externalProductId` → `displayName`) — 단 `displayName` 자체가 A-0으로 의미 있어야 유효.
4. **네이버 커머스 API 단건 조회** (자격증명 있을 때만, opt-in, 캐시) — 결과를 `products`에 upsert하고 displayName 사용.
5. 모두 실패 시 네이버 상품 페이지 딥링크 버튼만이라도 노출 (`https://smartstore.naver.com/<storeSlug>/products/<externalProductId>`).

표시는 *옵션정보 + 외부ID* 아래 작은 회색 텍스트 한 줄. 출처 라벨 포함 (`↳ 러닝깔창 · 옵션에서 추출`).

### B. 데이터 정규화 (근본 대책)

주문 동기화 파이프라인이 order_item/signature를 저장할 때 `rawProductNameSnapshot`이 비면 A의 1→2 fallback을 **저장 시점에 적용**해 채운다. 이후 수집되는 데이터는 처음부터 깨끗해진다. 과거 데이터는 일회성 backfill 스크립트로 동일 fallback을 적용.

### C. 상품 카탈로그 동기화 강화

`products` 테이블이 비어있는 externalProductId가 있으면 네이버 커머스 API로 상품명만이라도 확보해 `products`에 upsert. B의 fallback 2단계 적중률이 올라감.

---

## 작업 1. 조사 — 데이터 범위 확정

### 1-1. 얼마나 많은 시그니처/주문이 영향받는가

```
node scripts/inspect-sparse-signatures.mjs
```

진단 스크립트가 Postgres에서 다음을 보고하게 한다(이미 `scripts/sales-unit-reset/12-inspect-signature.mjs`로 일부 존재, 복붙하여 독립 위치로 옮기거나 `scripts/diagnostics/`에 정리).

- `order_source_signatures` 중 `rawProductNameSnapshot`이 공백/빈 문자열인 건수와 비율.
- 같은 시그니처에 연결된 order_items 중 `rawProductName`이 non-empty인 건수 — fallback 1로 복구 가능한 범위.
- 남은 건들의 `externalProductId`가 `products` 테이블에 있는 비율 — fallback 2로 복구 가능한 범위.
- 최종 잔여 (커머스 API 호출 필요 대상).

### 1-2. 시그니처 표시 포맷 확인

`apps/frontend/` 내 현재 시그니처 렌더 위치 찾기 (`sourceSignature`, `rawOptionInfoSnapshot`, `externalProductId` 노출 컴포넌트). 주로 수동 매핑 모달 / 시그니처 목록 / 시그니처 상세 3곳.

---

## 작업 2. 백엔드 — Signature enrichment

### 2-1. `helpers.ts` 또는 신규 모듈 `signature-enrichment.ts`

**함수: `enrichSignatureDisplayName(database, signature) → { fallbackProductName: string | null, source: 'snapshot' | 'orderItem' | 'optionInfo' | 'product' | 'commerceApi' | null }`**

- 입력: DatabaseShape, OrderSourceSignature.
- 공통 헬퍼: `isMeaningfulName(s)` — A-0 기준 판정.
- 로직:
  1. `isMeaningfulName(signature.rawProductNameSnapshot)` 이면 그대로 반환 (`source: 'snapshot'`).
  2. 같은 signature id를 가진 order_items에서 `isMeaningfulName(rawProductName)` 매칭 첫 건 반환 (`source: 'orderItem'`).
  3. `rawOptionInfoSnapshot` (+ items의 `rawOptionInfo`)에서 A-1-2 정규식으로 상품명 추출 → `isMeaningfulName` 통과 시 사용 (`source: 'optionInfo'`).
  4. items의 `externalProductId`로 `products` 조회 → `isMeaningfulName(product.displayName)` 만 채택 (`source: 'product'`).
  5. (선택, 2-3단에 구현) 커머스 API 호출 결과 (`source: 'commerceApi'`).
  6. 모두 실패 시 `null`.

모든 중간 결과의 정체(`source`)를 응답에 함께 실어서 UI가 신뢰 수준을 표시할 수 있게 한다.

### 2-2. API 응답에 포함

`GET /order-source-signatures` 응답 row에 `fallbackProductName`, `fallbackProductNameSource` 필드 추가. 기존 `rawProductNameSnapshot`은 건드리지 않고 새 필드로 합류하여 UI가 선택해서 표시하도록 한다.

동일 처리를 수동 매핑 API 응답 (`POST /order-source-signatures/:id/mapping` 조회 경로), 주문 시그니처 상세 조회 경로에 전파.

---

## 작업 3. 프론트엔드 — 보조 라벨 표시

### 3-1. 시그니처 카드/행 컴포넌트 수정

`rawProductNameSnapshot`이 비어있을 때 `fallbackProductName`이 있으면 2차 라인으로 표시:

```
여성용1+1 || 3513194860
↳ 베놈 무릎보호대 (상품DB 매칭)       ← 회색 작은 글씨
```

`source`에 따라 라벨 뒤에 배지(또는 tooltip) 표기:
- `orderItem`: 원본 주문 표기
- `product`: 상품 DB 매칭
- null: "상품 정보 없음" + 네이버 스토어 딥링크 버튼

### 3-2. 외부 링크 버튼

`externalProductId`가 존재하고 스토어의 네이버 store URL (예: `smartstore.naver.com/<slug>/`)을 알면 `products/<externalProductId>` 링크를 새 탭으로 연다. 스토어 slug는 `stores.payload` 또는 `commerce_api_credentials`에서 획득.

---

## 작업 4. 데이터 정규화(B)

### 4-1. order-sync.service.ts 수집 훅

order_item 저장 시 `rawProductName`이 공백이면 동일한 enrichment 로직을 시도해 최선의 값을 채워 저장.

order_source_signature upsert 시 `rawProductNameSnapshot`이 공백으로 들어오면 동일하게 보강.

단, 자동 채움한 값과 원본이 다를 때 **나중에 원본이 복구되면 덮어쓸 수 있도록** 소스 정보를 별도 필드(`productNameSource` 등)에 기록하는 것도 고려.

### 4-2. 일회성 backfill 스크립트

`scripts/backfill-signature-product-name.mjs`

- 전체 `order_source_signatures`를 순회하여 빈 `rawProductNameSnapshot`에 대해 enrichment 적용.
- 트랜잭션 + audit log `SIGNATURE_PRODUCT_NAME_BACKFILL` 기록.
- 영향 레코드 수를 dry-run 모드로 먼저 보고 → 사용자 확인 후 실행.

---

## 작업 5. 상품 카탈로그 보강(C, 선택)

### 5-1. 네이버 커머스 API 조회 유틸

`apps/backend/src/commerce/` 내 기존 커머스 클라이언트 재사용. `externalProductId`로 상품 단건 조회 엔드포인트가 있으면 사용.

### 5-2. 자동 upsert 배치

- 일일 크론 또는 수동 트리거: `products`에 없는 `externalProductId` 목록을 `order_items`에서 추출하여 커머스 API로 조회, 성공한 것만 upsert.
- 실패(404, 삭제된 상품 등)는 별도 `missing_products_log` 또는 감사 로그로 남김.

---

## 제약 / 규칙

1. `rawProductNameSnapshot` 자체는 **원본 데이터 유지**를 위해 가급적 건드리지 않는다. 새 필드(`fallbackProductName`)로 합류.
2. enrichment 로직은 순수 함수로 짜서 단위 테스트 가능하게(`run-tests.ts`에 케이스 추가).
3. 네이버 API 호출은 opt-in. API 자격증명 없는 환경에서도 A/B 단독 동작해야 한다.
4. 프론트 표시 변경은 디자인 리그레션 있을 수 있으므로 기존 시그니처 카드의 높이 변화 체크.
5. 스냅샷-리플레이스 DB 특성상, backfill 스크립트 실행 전 반드시 `scripts/sales-unit-reset/01-backup.mjs`와 동일한 JSON 덤프.

---

## 작업 순서 제안

1. 1-1 조사 스크립트 작성·실행 → 영향 범위 수치 확보.
2. 2-1, 2-2 백엔드 enrichment 구현 + 단위 테스트.
3. 3-1, 3-2 프론트엔드 보조 라벨 표시.
4. (A 배포 후 실제 UX 확인)
5. 4-1, 4-2 수집 훅·backfill로 원본 데이터 정규화.
6. (선택) 5-1, 5-2 상품 카탈로그 자동 보강.

---

## 결과 수용 조건

- 다음 세 케이스 모두 수동 매핑 UI에서 상품명(또는 최소 네이버 상품 딥링크)을 볼 수 있다:
  - `여성용1+1 || 3513194860` (패턴 1, 상품명 완전 누락)
  - `l || [함께배송⭐추가할인]러닝깔창: l` (패턴 2, 사이즈가 상품명 칸에 있음 → 옵션에서 "러닝깔창" 추출되어야 함)
  - `L || Q-LABEL` (패턴 3, products 엔트리 있으나 displayName null → 커머스 API 또는 딥링크)
- 조사 스크립트가 보고한 `fallbackProductName` 복구 가능 비율만큼은 UX에서 100% 복구된다.
- 근본 해결(작업 4) 적용 후에는 새로 수집되는 데이터에서 `rawProductNameSnapshot`에 사이즈 옵션 값이 들어가거나 빈 채 저장되지 않는다.
