# 2026-04-16 수정 계획 3-1 — 손익/매핑 페이지 타임아웃 근본 해결

> 0415수정계획3.md 작업 1-3(시그니처 enrichment) 마무리 후 발견된 별개 이슈.
> 손익 페이지가 `Unmapped summary: The operation was aborted due to timeout` 에러로 SSR이 실패하는 현상.
> 임시 조치(timeout 늘림 / mock fallback) 금지. 진짜 원인을 잡는다.

---

## 0. 증상

```
⨯ Error: Unmapped summary: The operation was aborted due to timeout
    at fetchApi (lib\api\client.ts:74:13)
    at async getProfitsPageData (lib\api\services.ts:943:63)
    at async ProfitsPage (app\(main)\profits\page.tsx:24:16)
```

`/api/v1/profits/unmapped-summary` 호출이 `apps/frontend/lib/api/client.ts:57`의 `AbortSignal.timeout(2500)` 컷에 걸려 throw → SSR 페이지 실패.

---

## 1. 진짜 원인 (두 겹)

### 1-1. `getOrderItemMappingStatus`의 O(N×M) 풀스캔

`apps/backend/src/helpers.ts:457`

```ts
export const getOrderItemMappingStatus = (database, item) => {
  if (item.orderSourceSignatureId) {
    const signature = database.orderSourceSignatures.find(
      (entry) => entry.id === item.orderSourceSignatureId,
    );  // ← 호출마다 풀스캔
    if (signature) return getSignatureMappingStatus(signature);
  }
  return item.canonicalSalesUnitId ? "MAPPED" : "UNMAPPED";
};
```

이 함수가 다음에서 행 단위로 반복 호출됨:

| 위치 | 호출 패턴 |
|---|---|
| `helpers.ts:824, 827` (calculateDashboardSummary) | `eligibleOrders.filter(... === "UNMAPPED")` + `... === "CONFLICT"` |
| `helpers.ts:858, 861` (calculateDashboardSummary) | 위 두 줄과 동일하게 두 번 더 (excluded용) |
| `helpers.ts:920` (mapOrderItemResponse) | `mappingStatus: getOrderItemMappingStatus(database, item)` |
| `profit.service.ts:232, 241` (getUnmappedSummary) | unmappedItems / conflictItems 각각 |
| `order-sync.service.ts:326` (listOrders) | `query.mappingStatus` 필터 |

→ orderItems N개, signatures M개일 때 **`getUnmappedSummary` 한 번에 약 2N×M번** find 수행. M이 수백~수천이면 손쉽게 수 초가 걸림.

### 1-2. `DatabaseService.getSnapshot()`이 매 호출마다 deep-clone

`apps/backend/src/database.service.ts:69`

```ts
getSnapshot(): DatabaseShape {
  return this.cloneSnapshot(this.database);  // JSON.parse(JSON.stringify(...))
}
```

`profit.service.ts`, `helpers.ts` 안의 다수 함수가 같은 요청 처리 도중 `getSnapshot()`을 여러 차례 부른다. 그때마다 모든 테이블(orderItems, orders, signatures, ads, ...)을 JSON 직렬화 후 역직렬화. 데이터가 커지면 이 자체로 수백 ms~수 초 소비.

또한 1-1의 캐시 전략으로 `WeakMap<DatabaseShape, Index>`를 쓰려 해도 매 호출마다 새 객체가 반환돼 캐시가 무효화된다 → 1-1 픽스를 위해서도 1-2를 같이 정리해야 한다.

---

## 2. 해결 방향

### 단계 A. snapshot 정체성 안정화 (1-2 픽스)

스냅샷을 **immutable한 단일 reference**로 다루고, mutation 시에만 새 reference로 교체하는 패턴으로 전환.

- `getSnapshot()`은 `this.database`를 그대로 반환 (clone 제거).
- mutation 진입점은 이미 `write(mutator)` 단일 경로. 그 안에서 새 객체를 만들어 `this.database`에 대입하도록 변경. 호출자가 반환된 draft를 mutate해도 외부 영향이 없도록.

```ts
// database.service.ts (요지)
getSnapshot(): DatabaseShape {
  return this.database;          // ← 클론 제거
}

write<T>(mutator: (draft: DatabaseShape) => T): T {
  const draft = this.cloneSnapshot(this.database);  // mutation은 클론 위에서
  const result = mutator(draft);
  this.database = draft;                            // reference 교체
  if (this.storageMode === "postgres") {
    this.queuePostgresPersistence(draft);
  } else {
    writeFileSync(this.filePath, JSON.stringify(this.database, null, 2), "utf-8");
  }
  return result;
}
```

전제: 읽기 경로에서 snapshot 객체를 mutate하지 않는 것. 코드를 grep하여 `snapshot.<table>.push|splice|sort|reverse|... = ...` 같은 직접 mutation이 있는지 확인. 있으면 readonly 패턴으로 옮기거나 `[...arr]`로 사본 만든 뒤 정렬.

리스크: 스냅샷 외부 mutation이 있던 코드가 있다면 직접 누수. 해결 후 반드시 51개 단위 테스트 + dev 서버 라운드트립으로 회귀 확인.

### 단계 B. signature 인덱스 도입 (1-1 픽스)

`helpers.ts`에 WeakMap 기반 lazy 인덱스 캐시를 두고 `getOrderItemMappingStatus`가 O(1)로 룩업하게 한다.

```ts
// helpers.ts (추가)
const signatureIndexCache = new WeakMap<
  DatabaseShape,
  Map<string, OrderSourceSignature>
>();

export const getSignatureIndex = (database: DatabaseShape) => {
  let idx = signatureIndexCache.get(database);
  if (!idx) {
    idx = new Map(database.orderSourceSignatures.map((s) => [s.id, s]));
    signatureIndexCache.set(database, idx);
  }
  return idx;
};

export const getOrderItemMappingStatus = (database, item) => {
  if (item.orderSourceSignatureId) {
    const signature = getSignatureIndex(database).get(item.orderSourceSignatureId);
    if (signature) return getSignatureMappingStatus(signature);
  }
  return item.canonicalSalesUnitId ? "MAPPED" : "UNMAPPED";
};
```

A 단계 덕분에 같은 요청 안에서 같은 reference가 반복 사용 → 인덱스가 한 번만 만들어진다. mutation 후에는 새 reference라 캐시 자동 무효화.

### 단계 C. `getUnmappedSummary` 단일 패스화 (상수 절감)

`profit.service.ts:222` 의 동일 orderItems를 두 번 필터링·집계하는 패턴을 단일 패스로 변경. adRows도 같은 방식.

```ts
let unmappedCount = 0, unmappedRevenue = 0, conflictCount = 0, conflictRevenue = 0;
for (const item of snapshot.orderItems) {
  if (item.storeId !== storeId) continue;
  if (!item.paymentDate || item.paymentDate < dateFrom || item.paymentDate > dateTo) continue;
  if (item.saleStatus !== "SALE") continue;
  const status = getOrderItemMappingStatus(snapshot, item);
  if (status === "UNMAPPED") { unmappedCount++; unmappedRevenue += item.productPaymentAmount; }
  else if (status === "CONFLICT") { conflictCount++; conflictRevenue += item.productPaymentAmount; }
}
```

`calculateDashboardSummary`(`helpers.ts:790~`)도 같은 식으로 정리. `eligibleOrders.filter(...)` 4회 + `.reduce` 가 한 번의 for 루프로.

### 단계 D. 회귀 방지 검증

1. `npm run test --workspace @patima/backend` — 51개 모두 PASS.
2. dev 서버 띄우고 `/profits` 페이지 SSR 응답 시간 측정 (2.5s 미만 확인).
3. `/orders`, `/mappings`, `/dashboard` 페이지 회귀 없는지 click-through.
4. 백엔드 로그에서 `[DatabaseService] PostgreSQL persistence failed` 신규 발생 없는지.

---

## 3. 구체 변경 파일 목록

| # | 파일 | 변경 요지 |
|---|---|---|
| 1 | `apps/backend/src/database.service.ts` | `getSnapshot` 클론 제거 / `write` 안에서 클론 후 reference 교체 |
| 2 | `apps/backend/src/helpers.ts` | `signatureIndexCache` WeakMap + `getSignatureIndex` 추가, `getOrderItemMappingStatus` 인덱스 사용 |
| 3 | `apps/backend/src/profit.service.ts` | `getUnmappedSummary` 단일 패스 |
| 4 | `apps/backend/src/helpers.ts` (`calculateDashboardSummary`) | excluded* 루프 단일 패스화 |
| 5 | `apps/backend/src/run-tests.ts` | `getSignatureIndex` 단위 테스트 (cache hit, mutation 후 invalidate) 추가 |
| 6 | (선택) `apps/backend/src/order-sync.service.ts` | `enrichSignatureDisplayName`의 `productsByIdMap`도 같은 패턴(WeakMap)으로 일반화하면 코드 중복 감소 |

> 직접 mutation을 하던 곳이 발견되면 그 파일도 함께 수정. (예상 후보: `order-sync.service.ts`, `mapping-seed.service.ts`, `sales-unit.service.ts` — 모두 `write()` 콜백 안에서만 mutate하면 안전.)

---

## 4. 작업 순서 (개발자/평가자 루프)

1. **(읽기 전용 검증)** `apps/backend/src/**` 전체에서 `snapshot`/`getSnapshot()` 결과를 직접 mutate하는 곳 grep.
   - `\.(push|splice|sort|reverse|shift|unshift|pop)\(` 패턴
   - `database\.\w+\s*=\s*` 패턴
   - 발견된 모든 곳을 표로 정리. write() 콜백 내부면 OK, 그 외면 단계 A 적용 전 수정.
2. **단계 A 적용** + 단위 테스트.
3. **단계 B 적용** + 단위 테스트(`getSignatureIndex` 캐시 hit/invalidate 케이스).
4. **단계 C 적용** + dashboard summary 회귀 테스트.
5. dev 서버 띄우고 실제 페이지 응답 시간 확인 (가능한 측정 도구: 백엔드 로그 직접 측정 또는 `console.time`).
6. 평가자가 회귀·스타일·테스트 통과 검증.

---

## 5. 수용 조건

- `/profits` SSR이 timeout 없이 정상 응답 (실제 데이터 양 기준 < 1초 권장, 최소 < 2.5초).
- `npm run test` 전체 PASS.
- 기존 페이지 (orders, mappings, sales-units, ads, costs, operations) 회귀 없음.
- snapshot mutation 누수 없음 (외부에서 snapshot을 mutate해도 다음 `getSnapshot()` 호출이 깨끗하게 분리되거나, 더 이상 그런 코드가 존재하지 않음).
- 새로 추가된 인덱스/캐시는 자동 무효화 (write 후 다음 read에서 stale 데이터 없음).

---

## 6. 비파괴 원칙

- 공개 API 응답 shape 변경 금지.
- 헬퍼 함수 시그니처는 최대한 유지(내부 구현만 교체).
- `getSnapshot()` 시맨틱 변경(클론 → reference)은 **읽기 전용**이라는 묵시 계약을 명시 주석으로 남길 것:
  ```ts
  /**
   * Returns the live snapshot reference. **MUST NOT be mutated by callers.**
   * Use `write(mutator)` for any change.
   */
  ```
- 기존 `signature-enrichment.ts`의 `EnrichmentContext` 패턴과 충돌하지 않게: B 단계의 인덱스가 도입되면 enrichment의 `productsByIdMap`도 동일 헬퍼로 통합 가능(작업 6, 선택).
