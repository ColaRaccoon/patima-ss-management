# 2026-04-15 수정 계획 2 — 판매단위 그룹화 (단품/1+1 통합 손익)

`0415수정계획.md`(관리코드 매핑 + 스토어 전체 광고비)가 완료된 시점을 전제로 작성. 다음 단계로 **단품 ↔ 1+1 같이 운영되는 광고를 손실 없이 손익에 반영하기 위한 판매단위 그룹화** 구조를 도입한다.

---

## 배경

### 현재 구조의 한계

- 주문 데이터는 `베놈 무릎보호대 단품` / `베놈 무릎보호대 1+1` 같이 SKU별로 분리된다.
- 반면 광고 캠페인은 `단품`, `1+1`을 구분하지 않고 하나로 운영된다.
- 결과적으로 하나의 광고비를 단품/1+1 어느 쪽에 귀속시켜도 반대쪽 판매단위의 손익이 왜곡된다.

### 해결 방향

`CanonicalSalesUnit`에 부모-자식 관계를 도입하여 다음을 가능케 한다.

- **그룹(부모) 판매단위**: 광고비 귀속 대상. 매출/원가는 자식들의 합산.
- **자식 판매단위**: 주문 매핑 대상. 매출/원가 보유. 광고비 없음.
- 광고 매핑은 항상 **그룹 단위**로 연결. 자식에는 직접 매핑 불가.
- 손익 대시보드에서는 기본적으로 **그룹 행**만 표시, 펼치기로 자식의 매출·수량·원가를 드릴다운 확인.

---

## 작업 1. 데이터 모델 확장

### 1-1. `packages/shared/src/types.ts`

**CanonicalSalesUnit 타입 수정** (lines 79~94)

```typescript
export interface CanonicalSalesUnit {
  // ... 기존 필드 ...
  parentSalesUnitId: string | null;   // 그룹의 부모 ID. null이면 최상위 또는 단일 판매단위
  isGroup: boolean;                   // 그룹 역할(자식을 합산하는 부모) 여부
  // ... 기존 필드 ...
}
```

- `isGroup: true`인 판매단위는 자식을 합산하는 *컨테이너*.
- 제약: `isGroup: true`이면 `linkedProductIds/linkedOptionCodes/linkedManageCodes`는 항상 빈 배열이어야 한다(주문 직접 매핑 금지).
- 제약: `parentSalesUnitId`가 설정된 자식은 자기 자신이 `isGroup`이 될 수 없다(1단계 계층만 허용, 트리 깊이 제한).
- 제약: `isStoreLevel`과 `isGroup`은 동시 true가 될 수 없다.

**DailySalesUnitProfit 타입 수정** (lines 302~319)

```typescript
export interface DailySalesUnitProfit {
  // ... 기존 필드 ...
  isGroup?: boolean;                  // 그룹 합산 행인지
  parentSalesUnitId?: string | null;  // 자식 행의 부모 ID (드릴다운 매칭용)
  childRows?: DailySalesUnitProfit[]; // 그룹 행에 자식 행을 함께 반환할지 (선택)
}
```

자식 행을 별도 리스트로 포함할지, 클라이언트에서 별도 쿼리로 가져올지는 구현 1-3에서 결정.

---

## 작업 2. 백엔드 — 그룹 관리 API

### 2-1. `apps/backend/src/sales-unit.service.ts`

**신규 메서드: `createSalesUnitGroup(storeId, displayName, childSalesUnitIds)`**

- `isGroup: true, parentSalesUnitId: null` 상위 판매단위 1개 생성.
- 전달받은 `childSalesUnitIds`에 대해 각각 `parentSalesUnitId`를 새 그룹 ID로 업데이트.
- 검증:
  - 모든 자식이 동일 `storeId`인가.
  - 자식 중 이미 `parentSalesUnitId != null`인 것이 없는가(재그룹화 별도 처리).
  - 자식이 `isGroup: true`가 아닌가.
  - 자식이 `isStoreLevel: true`가 아닌가.
- 광고 매핑 승계: 자식에 연결되어 있던 `CampaignSalesUnitMapping` 레코드의 `canonicalSalesUnitId`를 새 그룹 ID로 마이그레이션.
- `auditLogs`에 `SALES_UNIT_GROUP_CREATED` 기록.

**신규 메서드: `attachChildToGroup(storeId, groupId, childId)` / `detachChildFromGroup(storeId, childId)`**

- 기존 그룹에 자식을 추가/분리.
- `detach` 시 해당 자식의 `parentSalesUnitId`를 `null`로 되돌리고, 광고 매핑은 건드리지 않는다(그룹이 그대로 유지되면 광고비는 그룹에 남음).

**신규 메서드: `dissolveGroup(storeId, groupId)`**

- 그룹 해체. 자식들의 `parentSalesUnitId`를 `null`로 복구.
- 광고 매핑 처리: 그룹에 연결되어 있던 `CampaignSalesUnitMapping`은 `INTENTIONALLY_UNMAPPED`로 전환하거나 사용자에게 재매핑을 요구(정책 결정 필요).
- 자식이 0개인 그룹은 삭제 가능.

**기존 `createSalesUnit` 수정**

- 새 필드 초기화: `parentSalesUnitId: null, isGroup: false` (기본값).
- 호출 측에서 선택적으로 override 가능.

### 2-2. `apps/backend/src/helpers.ts`

**`migrateCanonicalSalesUnit` 하이드레이션 수정** (lines 398~411)

```typescript
parentSalesUnitId: raw.parentSalesUnitId ?? null,
isGroup: raw.isGroup ?? false,
```

`isStoreLevel`, `linkedManageCodes`와 동일한 패턴으로 추가.

### 2-3. 검증 헬퍼 추가 (`helpers.ts` 또는 `sales-unit.service.ts`)

- `assertGroupInvariants(unit: CanonicalSalesUnit): void` — 위 제약 위반 시 예외.
- `createSalesUnit`, `createSalesUnitGroup`, `attachChildToGroup`에서 호출.

---

## 작업 3. 광고 매핑 엔진 — 그룹 매핑 강제

### 3-1. `apps/backend/src/ad-mapping-engine.ts`

**`evaluateAdMapping()` 수정**

- 현재: 캠페인 → `canonicalSalesUnitId` 1개로 매핑.
- 변경: 해결된 대상이 자식(`parentSalesUnitId != null`)이면 자동으로 **부모 ID로 승격**하여 매핑.
- `resolvedBy`에 `"groupPromoted"` 또는 기존 reason 유지 + `promotedFromChild` 플래그 추가.

구현 예시(의사코드):

```typescript
function resolveTargetSalesUnitId(candidate: CanonicalSalesUnit): string {
  // 자식이면 부모로 승격, 아니면 그대로
  return candidate.parentSalesUnitId ?? candidate.id;
}
```

`MANUAL_MAPPED`, `INTENTIONALLY_UNMAPPED`, 스토어 전체, 키워드 룰, 텍스트 fallback 각각의 분기에서 최종 `targetSalesUnitId`를 위 헬퍼로 통과시킨다.

**`recalculateAdMappingsForStore()` 수정**

- 기존 매핑된 캠페인의 `canonicalSalesUnitId`가 자식을 가리키고 있다면 재계산 시 부모로 옮긴다(마이그레이션 효과).
- 기존에 수동 매핑(`MANUAL_MAPPED`)으로 자식에 꽂아뒀던 매핑도 자동으로 부모로 끌어올릴지, 사용자 선택을 존중할지 결정 필요. **권장: 자동으로 부모로 승격, 승격 감사 로그 기록.**

### 3-2. `apps/backend/src/campaign-mapping.service.ts`

**`seedDefaultCampaignMappingsForStore` 수정**

- 기본 시드 패턴의 `targetSalesUnit` 해석 시 `isGroup` 그룹이 있으면 그룹 우선. 없으면 단일 유닛. 그것도 없으면 스킵(현재 동작).
- 예: `한줄무릎보호대` 키워드는 `한줄보호대` 그룹에 매핑되도록 seed 조회 우선순위 변경.

### 3-3. 매핑 UI 보호 (백엔드)

- `CampaignSalesUnitMapping.canonicalSalesUnitId`에 자식 ID를 저장하려는 요청은 `400` 또는 서버에서 자동으로 부모로 변환 후 저장.
- 자식이 광고 매핑의 대상으로 선택되면 에러 반환: `"그룹 자식 판매단위에는 광고를 직접 매핑할 수 없습니다"`.

---

## 작업 4. 손익 계산 — 집계 구조 변경

### 4-1. `apps/backend/src/helpers.ts` — `calculateDailyProfitRows` (lines 488~619)

**현재 구조**

- `rowMap` 키: `${paymentDate}:${canonicalSalesUnitId}`
- 주문 루프: 자식(실제 매핑 대상) 매출을 자식 키에 적재.
- 광고 루프: 그룹(또는 단일 유닛)에 광고비 적재.
- 결과: 그룹 행은 매출 없고 광고비만, 자식 행은 매출만 있고 광고비 없음 → 잘못된 손익.

**변경 구조**

두 단계 집계로 분리.

```
1단계: childRowMap — 자식 + 단독 판매단위의 주문 집계 (현재 로직 유지)
2단계: groupRollup — 자식의 매출/수량/원가를 부모(그룹)로 합산
3단계: adRowMap — 광고비는 그룹(또는 단독 유닛)에 그대로 귀속
4단계: 최종 rowMap — 그룹 행 = 자식 합산 + 그룹 광고비 / 자식 행 = 자식 고유 값 + totalAdCost=0
```

구체적으로:

```typescript
// 1. 주문 집계 (기존 로직)
const childRowMap = new Map<string, DailySalesUnitProfit>();
orderItems.forEach((item) => {
  const key = `${date}:${item.canonicalSalesUnitId}`;
  // ... 기존 로직 유지. 자식/단독 유닛에 매출 적재 ...
});

// 2. 자식 → 부모 롤업
const groupRowMap = new Map<string, DailySalesUnitProfit>();  // 그룹 행 전용
childRowMap.forEach((childRow) => {
  const unit = salesUnitsById.get(childRow.canonicalSalesUnitId);
  if (!unit?.parentSalesUnitId) return;
  const parentKey = `${childRow.date}:${unit.parentSalesUnitId}`;
  const parentUnit = salesUnitsById.get(unit.parentSalesUnitId);
  const parentRow = groupRowMap.get(parentKey) ?? initRow(childRow.date, parentUnit);
  parentRow.totalQuantity += childRow.totalQuantity;
  parentRow.totalProductRevenue += childRow.totalProductRevenue;
  parentRow.totalDeliveryFeeAmount += childRow.totalDeliveryFeeAmount;
  parentRow.totalUnitCost += childRow.totalUnitCost;
  parentRow.totalFeeCost += childRow.totalFeeCost;
  parentRow.totalOtherCost += childRow.totalOtherCost;
  parentRow.fallbackIncomplete ||= childRow.fallbackIncomplete;
  groupRowMap.set(parentKey, parentRow);
});

// 3. 광고비 귀속 (기존 로직 — 대상은 이제 그룹 또는 단독 유닛)
adCosts.forEach((cost) => {
  const key = `${date}:${cost.canonicalSalesUnitId}`;
  const row = groupRowMap.get(key) ?? childRowMap.get(key) ?? initRow(...);
  row.totalAdCost += cost.totalCost;
  // row가 그룹이면 groupRowMap에, 단독이면 childRowMap에 저장
});

// 4. 반환: 그룹 행 + 부모 없는 자식 행 + 단독 행. 부모 있는 자식 행은 childRows[]로 그룹 행에 첨부.
```

**반환 정책**

- 기본 응답: 그룹 행은 합산 값, 자식 행은 `childRows[]` 안에 들어가서 UI에서 필요 시 펼쳐 볼 수 있게 한다.
- 옵션 파라미터 `includeGroupChildren?: boolean`로 클라이언트가 선택.
- 자식 행을 최상위 리스트에 함께 반환하지 않음으로써 총합 집계가 이중계산 되지 않도록 한다.

### 4-2. `calculateDashboardSummary` (lines 621~)

- `calculateDailyProfitRows` 결과가 이미 자식을 부모에 합산한 상태이므로 기존 합계 로직 그대로 동작.
- 확인 사항: 매출/광고비/원가 합이 그룹화 이전과 **동일**해야 한다. 회귀 테스트 필수.

### 4-3. `filterSalesUnitId` 인자 처리

- 현재 `filterSalesUnitId`는 단일 유닛 필터.
- 변경: `filterSalesUnitId`가 그룹 ID면 그룹 + 자식 전부 포함. 자식 ID면 해당 자식만.

---

## 작업 5. 프론트엔드

### 5-1. `apps/frontend/components/sales-units/sales-units-view.tsx` (392 lines)

- 그룹 판매단위 생성 UI 추가.
  - "판매단위 그룹 만들기" 버튼 → 기존 유닛 중 자식 선택 모달 → 그룹 displayName 입력.
- 목록 표시 계층화:
  - 스토어 전체(`isStoreLevel`)는 최상단.
  - 그룹(`isGroup: true`)은 그 다음, 펼쳐서 자식 표시. 배지 `"그룹"`.
  - 자식(`parentSalesUnitId != null`)은 그룹 아래 들여쓰기. 배지 `"그룹 자식"`.
  - 일반 단독 유닛은 기본 목록.
- 자식 유닛 편집 시 `linkedProductIds/linkedOptionCodes/linkedManageCodes`는 편집 가능(주문 매핑은 자식 레벨), `matchAliases`도 편집 가능.
- 그룹 유닛 편집 시 `linkedProductIds/linkedOptionCodes/linkedManageCodes`는 편집 불가(서버에서 빈 배열 강제), `matchAliases`는 편집 가능(fallback 텍스트 매칭 시 광고만 잡음).
- "그룹 해체" 및 "자식 추가/제거" 버튼 제공.

### 5-2. `apps/frontend/components/mappings/mappings-view.tsx` (1113 lines)

- 캠페인 광고 매핑 드롭다운:
  - 스토어 전체 옵션은 그대로 최상단.
  - 그 아래 **그룹 옵션만** 노출(자식은 광고 매핑 대상이 아님).
  - 자식 유닛은 "단품 → 베놈 무릎보호대 그룹에 귀속" 식으로 안내.
- 주문-판매단위 매핑 드롭다운:
  - 그룹 유닛은 선택 불가(그룹에는 주문을 직접 매핑하지 않음). 자식 또는 단독 유닛만 선택 가능.

### 5-3. `apps/frontend/components/profits/profits-view.tsx` (392 lines)

- 현재 "스토어 전체" 패널 / "일자별 손익" 패널 구조 유지.
- 일자별 손익 테이블에 **expandable row** 도입.
  - 그룹 행: 자식 합산 표시. 행 좌측 `▸` 버튼으로 펼치기.
  - 펼친 상태: 자식 행들의 매출/수량/원가 표시. **`totalAdCost` 컬럼은 `-`로 표시**(그룹에만 귀속되므로).
  - 자식 행에는 "[단품]", "[1+1]" 같은 접미사 구분 배지.
- 합계 계산은 그룹 행 기준으로만 집계(자식 행은 시각적 참고용, 합계 포함 X).
- 필터/정렬 시 자식 행은 그룹 행의 확장 상태를 따라감.

### 5-4. `apps/frontend/lib/api/types.ts`

- `SalesUnitListItem`에 `parentSalesUnitId: string | null`, `isGroup: boolean` 추가.
- `DailySalesUnitProfit` 타입 반영.

---

## 작업 6. 데이터 마이그레이션

### 6-1. 기존 단품/1+1 유닛 그룹화 스크립트

`migrations/phase3-create-sales-unit-groups.ts` (또는 관리자 API 1회성 실행)

- 스토어별로 displayName에 "단품"/"1+1"이 같이 등장하는 후보 세트 자동 탐지 로그.
- **사용자가 수동으로 그룹명을 지정하여 묶는 절차 권장** (자동 매칭은 오탐 가능).
- 사용자가 페어를 확정하면 `createSalesUnitGroup`을 호출하는 관리 UI 제공.

### 6-2. 광고 매핑 마이그레이션

- `recalculateAdMappingsForStore` 실행 시 자식에 꽂혀있던 매핑이 자동으로 부모로 승격(작업 3-1 참고).
- 승격 내역을 감사 로그로 저장하여 사용자가 검수 가능하게.

---

## 작업 7. 판매단위 이름 정리 (별도 미니작업)

현재 displayName이 "히트체인지 넥워머 겨울 바라클라바 방한 마스크..." 식으로 소비자 노출명 그대로라 내부 식별이 어렵다. 그룹화 작업과 함께 다음 displayName으로 정리한다(사용자 지정):

```
러닝조끼 / 러닝모자 / 러닝양말 / 러닝장갑 / 러닝깔창 / 스카이깔창
베놈 무릎보호대 (그룹)
 ├ 베놈 무릎보호대 단품
 └ 베놈 무릎보호대 1+1
한줄보호대 (그룹)
 ├ 한줄보호대 단품
 └ 한줄보호대 1+1
두줄보호대 (그룹)
 ├ 두줄보호대 단품
 └ 두줄보호대 1+1
자외선마스크 / 쿨토시 / 바라클라바
히트체인지 넥워머 (그룹)
 ├ 히트체인지 넥워머 단품
 └ 히트체인지 넥워머 1+1
카프슬리브
```

- 이름 변경 시 `matchAliases`에 **기존 displayName을 별칭으로 추가**하여 시그니처 기반 fallback이 깨지지 않게 한다.
- 이미 매핑된 `canonicalSalesUnitId` 참조는 ID가 그대로이므로 영향 없음.

---

## 구현 순서 권장

1. **작업 1** (타입 정의, 하이드레이션) — 위험도 낮음. 오늘 작업과 동일 패턴.
2. **작업 2** (그룹 관리 API) — 백엔드 단위 테스트와 함께.
3. **작업 3** (광고 매핑 엔진 부모 승격) — 기존 매핑 회귀 테스트 필수.
4. **작업 4** (손익 계산 집계 변경) — 가장 위험. 별도 PR로 분리 권장.
5. **작업 5-1, 5-2** (판매단위/매핑 UI) — 백엔드 완료 후.
6. **작업 5-3** (손익 대시보드 expandable row) — 작업 4 완료 후.
7. **작업 6** (마이그레이션) — 실제 데이터 마이그레이션 전 **드라이런 필수**.
8. **작업 7** (이름 정리) — 마이그레이션 타이밍에 함께.

---

## 검증 항목

- 그룹 생성 전/후 `calculateDashboardSummary`의 총매출·총광고비·총이익이 **완전 동일**(오차 0원)한지 회귀 테스트.
- 광고비가 그룹에만 귀속되고 자식에는 0인지 쿼리 확인.
- 자식 레벨 매출의 합 === 그룹 레벨 매출.
- 그룹 해체 시 자식으로 매출이 정상 복원되고 광고 매핑은 `INTENTIONALLY_UNMAPPED` 또는 경고로 전환되는지.
- UI 필터에서 그룹 선택 시 자식까지 포함되어 표시되는지.
- 매핑 UI에서 자식에 광고를 직접 연결하려 하면 차단되거나 자동으로 부모로 올라가는지.

---

## 예상 작업 규모

`0415수정계획.md`의 작업 1+3 대비 **약 1.5~2배**. 주요 리스크는 `calculateDailyProfitRows`의 집계 로직 재설계와 `profits-view.tsx`의 expandable row UI 두 곳에 집중됨. 나머지는 오늘 완료한 패턴(필드 추가 + 하이드레이션 + 우선순위 로직)의 반복이라 안전하게 진행 가능.
