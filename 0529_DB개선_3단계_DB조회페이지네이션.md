# 0529 DB 개선 3단계 - DB 조회 페이지네이션 전환 지침서

## 이 문서의 목적

이 문서는 DB 개선 3단계 작업자가 주문/광고/작업 목록 조회를 DB query 기반 페이지네이션으로 전환할 수 있도록 만든 지침서다.

이번 단계의 핵심은 화면에는 50~200개만 보여주는데 서버가 매번 전체 배열을 filter/sort/map하는 병목을 줄이는 것이다. 전체 저장 구조를 한 번에 정규화하지 않고도, 2단계에서 만든 JSONB expression index와 query helper를 사용해 hot path부터 개선한다.

## 선행 조건

권장 선행 단계:

- 1단계 저장 안정화
- 2단계 row-level persistence 및 주요 index 추가

2단계가 완전히 끝나지 않았더라도, 이 단계는 file mode fallback과 postgres query branch를 함께 두면 부분 진행 가능하다. 단, 실제 성능 개선은 PostgreSQL query branch가 있어야 체감된다.

## 현재 문제

현재 주요 목록 조회는 대부분 다음 패턴이다.

```ts
const snapshot = databaseService.getSnapshot();
const items = snapshot.someRows
  .filter(...)
  .sort(...)
  .map(...);
return paginate(items, page, pageSize);
```

문제:

- pageSize가 50이어도 전체 row를 매번 훑는다.
- sort 후 paginate라 row 수가 커질수록 느려진다.
- 주문/광고/operation 목록은 장기 운영에서 가장 자주 조회되는 hot path다.
- `getSnapshot()` live reference 의존이 계속 남는다.

## 이번 단계의 목표

1. 주문상품 목록 조회를 DB query 기반으로 전환한다.
2. 광고 캠페인 시그니처/매핑 목록 조회를 DB query 기반으로 전환한다.
3. operation 목록 조회를 DB query 기반으로 전환한다.
4. file mode에서는 기존 in-memory fallback을 유지한다.
5. API 응답 shape는 유지한다.
6. 검색/필터/정렬/page/pageSize 동작이 기존과 동일해야 한다.

## 이번 단계에서 하지 말아야 할 것

- UI pagination 컴포넌트를 대규모로 바꾸지 않는다.
- 모든 서비스 조회를 한 번에 DB query로 바꾸지 않는다.
- 마진 계산 결과 테이블은 이번 단계에서 만들지 않는다. 그것은 4단계다.
- DB 정규 컬럼 테이블 전체 전환은 하지 않는다.
- 보안/인증 작업은 하지 않는다.

## 집/회사 DB 동기화 보존 조건

이 단계는 조회 방식을 바꾸는 작업이므로 full snapshot export/import가 깨질 이유는 없다. 그래도 index나 query helper를 추가했다면 import 후 해당 index가 보장되어야 한다.

공통 원칙은 `0529_DB개선_공통_집회사동기화_필수조건.md`를 따른다.

필수 확인:

- `scripts/db-export.mjs`로 만든 snapshot을 다른 DB에 import한 뒤 목록 조회가 정상 동작한다.
- import 후 필요한 JSONB expression index가 생성되어 있다.
- file mode fallback과 PostgreSQL query branch 모두 기존 API shape를 유지한다.

## 관련 파일

주요 수정 파일:

- `apps/backend/src/database.service.ts`
- `apps/backend/src/order-sync.service.ts`
- `apps/backend/src/ads.service.ts`
- `apps/backend/src/operation.service.ts`
- `apps/backend/src/helpers.ts`
- `apps/backend/src/run-tests.ts`

필요 시 추가 파일:

- `apps/backend/src/query-builders.ts`
- `apps/backend/src/database-query.service.ts`

참고 파일:

- `apps/backend/src/app.controller.ts`
- `packages/shared/src/types.ts`
- `0529_DB개선_2단계_row_level_persistence.md`

## 우선 전환 대상

### 1. 주문상품 목록

현재 위치:

- `OrderSyncService.listOrderItems()`
- controller endpoint는 `app.controller.ts`의 order items 조회 부분

필터:

- `storeId`
- `dateFrom`
- `dateTo`
- `productName`
- `optionInfo`
- `mappingStatus`
- `orderStatus`
- `saleStatus`
- `paymentDateStatus`
- `page`
- `pageSize`

정렬:

- 기존 동작을 확인하고 동일하게 유지한다.
- 보통 `paymentDate/orderDate/createdAt` 최신순이 적합하다.

### 2. 광고 캠페인 시그니처 목록

현재 위치:

- `AdsService.listAdCampaignSignatures()`

필터:

- `storeId`
- 검색어 `q`
- mapping 상태/이유
- page/pageSize

주의:

- 현재 로직은 active upload id와 ad cost rows를 묶어 signature summary를 만든다.
- 바로 SQL 하나로 완벽히 옮기기 어렵다면 1차로 "필터/페이지 대상 signature id만 DB에서 먼저 가져오고, 해당 id 범위만 summary 계산"하는 하이브리드로 시작한다.

### 3. operation 목록

현재 위치:

- `OperationService.list()`

필터:

- `storeId`
- `status`
- `operationType`
- page/pageSize

정렬:

- `createdAt DESC`

가장 단순하므로 첫 번째 구현 대상으로 적합하다.

## DatabaseService query API 설계

### 1. storage mode 분기

`DatabaseService`에 query 메서드를 추가한다.

```ts
async queryOrderItems(query: OrderItemQuery): Promise<PaginatedResult<OrderItemListRow>>
async queryOperations(query: OperationQuery): Promise<PaginatedResult<OperationRecord>>
async queryAdCampaignSignatures(query: AdSignatureQuery): Promise<PaginatedResult<AdSignatureListRow>>
```

동작:

- PostgreSQL 모드: SQL query 사용
- file 모드: 기존 snapshot filter/sort/paginate 사용

주의:

- service layer는 storage mode를 몰라도 되게 한다.
- API 응답 shape는 기존과 같게 service에서 마지막 mapping을 맞춘다.

### 2. count와 page items를 분리한다

pagination 응답에는 total이 필요하다. PostgreSQL에서는 다음을 사용한다.

```sql
SELECT COUNT(*) FROM ...
SELECT payload FROM ... ORDER BY ... LIMIT ... OFFSET ...
```

또는 window function:

```sql
SELECT payload, COUNT(*) OVER() AS total
FROM ...
WHERE ...
ORDER BY ...
LIMIT $n OFFSET $m
```

단순성과 테스트 편의상 count query와 item query를 분리해도 된다.

### 3. SQL injection 방지

필터 값은 parameter binding을 사용한다.

금지:

```ts
`WHERE payload->>'storeId' = '${storeId}'`
```

허용:

```ts
conditions.push(`payload->>'storeId' = $${params.length + 1}`);
params.push(storeId);
```

정렬 컬럼은 allowlist만 허용한다.

### 4. JSONB field query 예시

주문상품 기본 query:

```sql
SELECT payload
FROM order_items
WHERE payload->>'storeId' = $1
  AND COALESCE(payload->>'paymentDate', payload->>'orderDate', LEFT(payload->>'createdAt', 10)) >= $2
  AND COALESCE(payload->>'paymentDate', payload->>'orderDate', LEFT(payload->>'createdAt', 10)) <= $3
ORDER BY COALESCE(payload->>'paymentDate', payload->>'orderDate', LEFT(payload->>'createdAt', 10)) DESC,
         payload->>'updatedAt' DESC
LIMIT $4 OFFSET $5
```

검색:

```sql
LOWER(payload->>'normalizedProductName') LIKE $n
```

주의:

- 기존 `normalizeText()` 결과와 SQL 검색어 정규화가 일치해야 한다.
- 가능하면 service에서 검색어를 `normalizeText()`로 만든 뒤 SQL에는 normalized field를 검색한다.

## 구현 순서

1. `PaginatedResult` shape를 확인한다.
2. `DatabaseService`에 operation 목록 query를 먼저 추가한다.
3. `OperationService.list()`를 새 query API로 전환한다.
4. operation list 테스트를 DB query/file fallback 양쪽 기준으로 추가한다.
5. 주문상품 query builder를 만든다.
6. `OrderSyncService.listOrderItems()`를 새 query API로 전환한다.
7. 기존 주문상품 목록 테스트를 보존하고 필터별 regression test를 추가한다.
8. 광고 캠페인 시그니처 목록은 하이브리드로 전환한다.
9. SQL query helper의 parameter index 테스트를 추가한다.
10. README 또는 운영 메모에 DB pagination 대상 endpoint를 적는다.

## 테스트 지침

필수 테스트:

1. operation list가 `status`, `operationType`, `storeId` 필터를 지킨다.
2. operation list가 `createdAt DESC` 정렬과 page/pageSize를 지킨다.
3. 주문상품 list가 날짜 필터를 지킨다.
4. 주문상품 list가 productName/optionInfo 검색을 기존과 동일하게 처리한다.
5. 주문상품 list가 mappingStatus/orderStatus/saleStatus/paymentDateStatus를 지킨다.
6. 다른 storeId row가 섞이지 않는다.
7. pageSize보다 많은 row에서 total과 items 길이가 정확하다.
8. SQL builder가 모든 사용자 입력을 parameter로 전달한다.
9. file mode fallback 결과와 postgres query branch 결과가 같은 dataset에서 동일하다.

권장 테스트:

- 10만 row를 직접 만들 필요는 없지만, 1000 row 이상 fixture로 query가 전체 map 후 paginate하지 않는 구조를 검증한다.
- fake pool/fake client로 SQL과 params를 검증한다.

검증 명령:

```powershell
npm.cmd run typecheck --workspace @patima/backend
npm.cmd run test --workspace @patima/backend
npm.cmd run lint
```

## 완료 기준

- operation 목록이 DB query 기반으로 동작한다.
- 주문상품 목록이 DB query 기반으로 동작한다.
- 광고 캠페인 시그니처 목록이 최소 하이브리드 방식으로 전체 row 순회를 줄인다.
- file mode fallback이 유지된다.
- 기존 API 응답 shape가 깨지지 않는다.
- backend test, backend typecheck, 전체 lint가 통과한다.

## 다음 단계로 넘길 내용

3단계 완료 후 4단계에서 할 일:

- 마진 계산을 요청마다 원천 데이터에서 다시 계산하지 않도록 daily summary table을 만든다.
- 주문/광고/비용/매핑 변경 시 해당 날짜만 summary를 재계산한다.

## 다음 세션 실행 프롬프트

```text
너는 Codex다. 작업 경로는 C:\Users\seong\Desktop\workspace\patima-naver-ss 이다.

반드시 먼저 0529_DB개선_3단계_DB조회페이지네이션.md 파일을 끝까지 읽고, 보안/인증 작업은 제외한 채 DB 조회 페이지네이션만 구현해라.

이번 목표:
- operation 목록, 주문상품 목록, 광고 캠페인 시그니처 목록의 전체 배열 filter/sort/map 병목을 줄인다.
- PostgreSQL 모드에서는 DB query + LIMIT/OFFSET 또는 cursor 기반으로 조회한다.
- file mode fallback은 유지한다.
- API 응답 shape는 기존과 동일하게 유지한다.
- 마진 daily summary와 전체 정규화는 이번 단계에서 하지 않는다.

작업 후 다음을 실행해라:
- npm.cmd run typecheck --workspace @patima/backend
- npm.cmd run test --workspace @patima/backend
- npm.cmd run lint

최종 응답에는 변경 파일, DB query로 전환한 endpoint, fallback 방식, 테스트 결과를 포함해라.
```
