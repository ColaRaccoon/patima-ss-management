# 0414 수정 계획

> **작성일**: 2026-04-14  
> **프로젝트**: patima-naver-ss (네이버 스마트스토어 수익 추적 시스템)  
> **스택**: NestJS + Next.js  
> **목적**: 함께배송 매핑 안정화, 디버그 코드 정리, 광고 매핑 고도화

---

## 작업 1: 관리코드(optionManageCode) 기반 함께배송 자동매핑

### 목적

함께배송(추가상품) 아이템의 판매단위 매핑을 불안정한 `optionCode` 대신 관리코드(`optionManageCode`)로 전환하여 매핑 정확도와 안정성을 확보한다.

### 배경

현재 함께배송 아이템은 메인 상품과 동일한 `externalProductId`를 공유하기 때문에 ID 기반 매핑이 불가능하다. 기존에는 `optionCode → linkedOptionCodes` 방식을 사용하는데, `optionCode`는 주문마다 바뀌는 불안정한 값이라 매핑이 깨지는 문제가 반복된다.

네이버 어드민에서 추가상품별로 관리코드(예: `BDL_RUN_VEST`, `BDL_RUN_CAP`)를 입력해두었으며, 이 값이 주문 API의 `optionManageCode` 필드로 내려올 것으로 기대된다. 현재 새 주문 데이터로 검증 중이다.

### 수정 파일 및 구현 상세

#### 1) `packages/shared/src/types.ts`

**SyncedOrderItemInput 타입 수정**

- `optionManageCode?: string` 필드 추가
- 네이버 주문 API에서 내려오는 관리코드를 저장할 필드

**CanonicalSalesUnit 타입 수정**

- `linkedManageCodes?: string[]` 필드 추가
- 하나의 판매단위에 연결된 관리코드 목록 (예: 러닝조끼 판매단위 → `["BDL_RUN_VEST"]`)

#### 2) `apps/backend/src/naver-commerce.service.ts`

**normalizeOrderDetail() 수정**

- 네이버 주문 상세 API 응답에서 `optionManageCode` 값을 추출
- `SyncedOrderItemInput` 객체에 `optionManageCode` 필드를 포함하여 반환
- 값이 빈 문자열이거나 `undefined`인 경우 필드를 생략 (falsy 체크)

#### 3) `apps/backend/src/mapping-seed.service.ts`

**판매단위 시드 생성 로직 수정**

- 함께배송 판매단위 생성 시 `linkedManageCodes` 배열을 포함
- 기존 `linkedOptionCodes`와 병행 유지 (하위 호환)
- 시드 데이터에 관리코드가 있는 경우 `linkedManageCodes`를 우선 설정

#### 4) `apps/backend/src/sales-unit-auto-mapper.ts`

**매핑 우선순위 변경**

기존 매핑 로직에 관리코드 기반 매핑 단계를 추가한다. 최종 매핑 우선순위는 다음과 같다:

1. **confirmedAt 있는 시그니처 매핑** — 사용자가 수동으로 확인한 매핑. 최우선.
2. **optionManageCode → linkedManageCodes** (신규) — 주문 아이템의 `optionManageCode`가 판매단위의 `linkedManageCodes` 배열에 포함되는지 검사. 함께배송 매핑의 핵심 경로.
3. **optionCode → linkedOptionCodes** (기존) — 관리코드가 없는 과거 주문 또는 관리코드 미설정 상품에 대한 폴백.
4. **externalProductId → linkedProductIds** — 일반 상품(함께배송 아닌) 매핑. 함께배송 아이템은 이 단계에서 제외해야 함 (동일 productId 문제).
5. **시그니처 텍스트 매칭 fallback** — 위 단계에서 모두 매핑되지 않은 경우 상품명/옵션명 기반 텍스트 매칭.

**구현 시 주의사항**

- 매핑 함수에서 각 단계를 순차적으로 시도하고, 매핑 성공 시 즉시 반환하는 구조 유지
- `optionManageCode`가 빈 문자열인 경우는 매핑 대상에서 제외
- 매핑 결과에 어떤 방식으로 매핑되었는지 로그 남기기 (디버깅용, 운영에서는 debug 레벨)

### 구현 순서

1. `types.ts`에 `optionManageCode`, `linkedManageCodes` 타입 추가
2. `naver-commerce.service.ts`의 `normalizeOrderDetail()`에서 관리코드 추출 구현
3. `mapping-seed.service.ts`에서 시드 데이터에 `linkedManageCodes` 포함
4. `sales-unit-auto-mapper.ts`에 관리코드 매핑 단계 삽입
5. 새 주문 발생 후 `optionManageCode` 값이 실제로 내려오는지 검증
6. 검증 완료 후 기존 판매단위 시드에 관리코드 반영

### 검증 방법

- 새 주문 동기화 후 DB에서 `optionManageCode` 필드가 정상적으로 저장되는지 확인
- 함께배송 아이템이 관리코드 기반으로 올바른 판매단위에 매핑되는지 확인
- 관리코드가 없는 기존 주문은 기존 `optionCode` 방식으로 매핑되는지 확인 (하위 호환)

---

## 작업 2: 디버그 코드 정리

### 목적

관리코드 검증을 위해 임시로 추가한 디버그 코드를 제거하여 코드베이스를 정리한다.

### 배경

작업 1의 관리코드 매핑 도입을 위해 `optionManageCode` 값이 실제로 주문 API에서 내려오는지 확인하는 디버그 코드를 추가해둔 상태다. 관리코드 매핑이 구현되고 실제 데이터로 검증이 완료된 후에 제거한다.

### 선행 조건

- 작업 1의 관리코드 매핑 구현 완료
- 실제 새 주문으로 `optionManageCode` 값이 정상적으로 수신되는 것을 확인
- 관리코드 기반 매핑이 정상 동작하는 것을 확인

### 제거 대상

#### 1) `apps/backend/src/naver-commerce.service.ts` — console.log 블록

- **위치**: lines 539~549 부근
- **식별자**: `[DEBUG optionManageCode]` 라벨이 포함된 console.log 블록
- **작업**: 해당 블록 전체 삭제. 주변 로직에는 영향 없음.

#### 2) `apps/backend/src/mapping-seed.service.ts` — debugGetOptionManageCodes()

- **대상**: `debugGetOptionManageCodes()` 메서드 전체
- **작업**: 메서드 정의 및 관련 import가 있다면 함께 삭제
- **주의**: 이 메서드를 호출하는 곳이 app.controller.ts의 디버그 엔드포인트뿐인지 확인 후 삭제

#### 3) `apps/backend/src/app.controller.ts` — 디버그 엔드포인트

- **대상**: `GET /api/v1/debug/option-manage-codes/:storeId` 엔드포인트
- **작업**: 라우트 핸들러 메서드 전체 삭제
- **주의**: 관련 import (`MappingSeedService` 등)가 이 엔드포인트에서만 사용되는 경우 import도 함께 정리

### 구현 순서

1. `app.controller.ts`에서 디버그 엔드포인트 제거
2. `mapping-seed.service.ts`에서 `debugGetOptionManageCodes()` 메서드 제거
3. `naver-commerce.service.ts`에서 `[DEBUG optionManageCode]` console.log 블록 제거
4. 사용되지 않는 import 정리
5. 빌드 확인 (`npm run build`)

### 검증 방법

- 빌드 에러 없음 확인
- 기존 기능(주문 동기화, 매핑)이 정상 동작하는지 확인
- `/api/v1/debug/option-manage-codes/:storeId` 호출 시 404 반환 확인

---

## 작업 3: 광고 매핑 개선 — 스토어 전체 광고비 버킷 + 자동매핑 룰

### 목적

캠페인명 기반 키워드 자동매핑 룰을 도입하고, 특정 판매단위에 귀속할 수 없는 광고비(카탈로그, 스토어타겟 등)를 "스토어 전체" 판매단위로 집계하여 광고비 누락 없이 수익을 추적한다.

### 배경

현재 광고 매핑은 캠페인명 텍스트로 판매단위를 찾는 방식인데, 캠페인명은 사람이 임의로 짓는 값이라 불안정하다. 또한 카탈로그 광고, 스토어 타겟 광고처럼 스토어 전체를 대상으로 하는 광고는 특정 판매단위에 귀속할 수 없어 현재 매핑에서 누락된다.

실제 캠페인 데이터 분석 결과, 캠페인명에 포함된 키워드로 안정적인 자동매핑이 가능한 패턴이 도출되었다.

### 캠페인 자동매핑 키워드 룰

| 캠페인명에 포함된 키워드 | 매핑 대상 판매단위 |
|---|---|
| `한줄무릎보호대` | 한줄 무릎보호대 |
| `두줄무릎보호대`, `인피니티가드_두줄` | 두줄 무릎보호대 |
| `런닝양말` | 러닝양말 |
| `다이어트양말`, `인피니티가드_다이어트양말` | 다이어트양말 |
| `유쉴드`, `유쉴드마스크` | 자외선차단 마스크 |
| `카프슬리브` | 쿨토시 |
| `카탈로그`, `키워드타겟`, `무릎보호대`(단독, 한줄/두줄 미포함), `인피니티가드`(단독, 한줄/두줄 미포함) | **스토어 전체 광고비** |

**키워드 매칭 규칙**

- 캠페인명에 해당 키워드가 포함(contains)되는지 검사
- "무릎보호대"와 "인피니티가드"는 `한줄`/`두줄` 등 세부 키워드가 함께 포함되지 않은 경우에만 스토어 전체로 분류
- 매칭 순서: 세부 키워드(한줄무릎보호대 등)를 먼저 검사한 후, 포괄 키워드(무릎보호대 단독 등)를 검사하여 오분류 방지
- 어떤 키워드에도 매칭되지 않는 캠페인은 미매핑 상태로 유지 (기존 동작)

### 수정 파일 및 구현 상세

#### 1) `packages/shared/src/types.ts`

**CanonicalSalesUnit 타입 수정**

- `isStoreLevel?: boolean` 필드 추가
- `true`인 판매단위는 스토어 전체 광고비를 집계하는 특수 판매단위
- 이 판매단위에는 매출(revenue)이 발생하지 않고, 광고비만 귀속됨

#### 2) `apps/backend/src/sales-unit.service.ts`

**스토어 전체 판매단위 자동 생성 로직**

- 스토어별로 `isStoreLevel: true`인 판매단위가 존재하는지 확인
- 없으면 자동 생성 (스토어당 1개만 존재해야 함)
- 판매단위명: `[스토어명] 전체 광고비` 등 구분 가능한 네이밍
- `unitCost`, `otherCost`는 0 또는 null로 설정 (원가 개념이 없음)

#### 3) `apps/backend/src/ad-mapping-engine.ts`

**자동매핑 룰 엔진 수정**

- 기존 텍스트 매칭 로직을 키워드 룰 기반으로 교체 또는 보강
- 키워드 룰을 데이터 구조(배열/맵)로 정의:
  ```
  룰 구조 예시:
  {
    keywords: ["한줄무릎보호대"],
    targetSalesUnitIdentifier: "한줄 무릎보호대 판매단위 ID 또는 식별자"
  }
  ```
- 매칭 로직 순서:
  1. 캠페인명에 세부 키워드가 포함되는지 검사 (구체적인 것 우선)
  2. 세부 키워드에 매칭되지 않으면 포괄 키워드 검사 → 스토어 전체 판매단위로 매핑
  3. 어떤 룰에도 매칭되지 않으면 미매핑 유지
- "무릎보호대", "인피니티가드" 포괄 키워드 처리 시 세부 키워드 제외 조건 구현:
  - 캠페인명에 `무릎보호대`가 포함되지만 `한줄` 또는 `두줄`이 포함되지 않는 경우 → 스토어 전체
  - 캠페인명에 `인피니티가드`가 포함되지만 `두줄`, `다이어트양말` 등 세부 식별자가 없는 경우 → 스토어 전체

#### 4) `apps/frontend/components/mappings/mappings-view.tsx`

**매핑 관리 UI 수정**

- `isStoreLevel: true`인 판매단위를 목록에서 시각적으로 구분 (배지, 색상 등)
- 해당 판매단위의 원가 설정(`unitCost`, `otherCost`) 입력 필드를 숨기거나 비활성화
- 스토어 전체 판매단위는 사용자가 직접 삭제/수정할 수 없도록 처리 (또는 경고 표시)

#### 5) `apps/frontend/components/profits/profits-view.tsx`

**수익 대시보드 UI 수정**

- `isStoreLevel: true`인 판매단위 행을 시각적으로 구분 (배경색, 아이콘 등)
- 해당 행에는 매출(revenue), 원가 관련 컬럼을 `-` 또는 비활성 표시
- 광고비 컬럼만 유의미한 값으로 표시
- 테이블 하단 합계 계산 시 스토어 전체 광고비도 포함되어야 함 (총 수익에서 차감)

### 수정 불필요 파일

- `helpers.ts`의 `calculateDailyProfitRows` — 광고비 루프와 주문 루프가 분리되어 있어 `revenue=0`인 판매단위도 자연스럽게 처리됨. 광고비가 있는 날만 row가 생성되는 구조이므로 phantom row 문제 없음.

### 구현 순서

1. `types.ts`에 `isStoreLevel` 필드 추가
2. `sales-unit.service.ts`에 스토어 전체 판매단위 생성/조회 로직 구현
3. `ad-mapping-engine.ts`에 키워드 룰 데이터 구조 정의 및 매칭 로직 구현
4. 기존 광고 데이터로 자동매핑 룰 검증 (매핑 결과 로그 확인)
5. `mappings-view.tsx`에 스토어 전체 판매단위 UI 구분 적용
6. `profits-view.tsx`에 스토어 전체 행 구분 및 표시 로직 적용
7. 전체 수익 계산 결과가 정확한지 검증

### 검증 방법

- 알려진 캠페인 목록으로 자동매핑 결과가 위 키워드 룰 표와 일치하는지 확인
- 스토어 전체 판매단위에 광고비가 정상 집계되는지 확인
- 대시보드 합계에서 스토어 전체 광고비가 총 수익에 반영(차감)되는지 확인
- 스토어 전체 판매단위가 스토어당 1개만 생성되는지 확인
- 기존 매핑된 캠페인이 새 룰 적용 후에도 올바르게 매핑되는지 확인

---

## 전체 작업 순서 및 의존성

```
작업 1 (관리코드 매핑) ──검증완료──→ 작업 2 (디버그 코드 정리)
작업 3 (광고 매핑 개선) ──────────→ 독립적, 병행 가능
```

- 작업 1과 작업 3은 서로 독립적이므로 병행 진행 가능
- 작업 2는 반드시 작업 1이 완료되고 실데이터 검증이 끝난 후에 진행
- 각 작업 완료 후 `npm run build` 및 기본 동작 확인 필수
