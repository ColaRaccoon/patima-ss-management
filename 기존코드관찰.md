# patima-naver-ss — 기존 코드 관찰 노트

> 새 세션이 이 한 파일만 읽고도 프로젝트 전체를 빠르게 파악할 수 있도록 정리한 문서.
> 마지막 갱신: 2026-04-16.

---

## 1. 한눈에 보는 프로젝트 개요

- **무엇**: 네이버 스마트스토어 셀러용 손익 관리 MVP.
  - 주문 수집(네이버 커머스 API) → 주문 시그니처/판매단위 매핑 → 광고비 매핑 → 일별 손익 계산.
- **모노레포**: npm workspaces.
  - `apps/backend` (NestJS 11, Node ≥ 22)
  - `apps/frontend` (Next.js 15 App Router + React 19 + Tailwind 3)
  - `packages/shared` (타입/normalization 유틸 — 양쪽이 모두 import)
- **스토리지**: 단일 in-memory `DatabaseShape` snapshot.
  - `DATABASE_URL` 있으면 Postgres JSONB로 **테이블 단위 snapshot-replace** (TRUNCATE + bulk INSERT).
  - 없으면 `apps/backend/data/database.json` 단일 파일.
- **도커**: `docker-compose.yml`에 Postgres만 들어있음.

### 실행 명령

| 목적 | 명령 |
|---|---|
| 전체 dev | `npm run dev` (frontend + backend 동시 실행) |
| frontend dev만 | `npm run dev:frontend` |
| backend dev (watch) | `npm run dev:backend:watch` |
| 빌드 | `npm run build` (shared → backend → frontend 순) |
| 타입체크 | `npm run lint` |
| 테스트 | `npm run test` (= backend의 `run-tests.ts` 컴파일 후 실행) |

### 환경 변수

- `DATABASE_URL` — Postgres 연결 문자열. 없으면 file 모드.
- `DATA_DIR` — file 모드 데이터 폴더 (기본 `apps/backend/data`).
- `API_BASE_URL` / `NEXT_PUBLIC_API_BASE_URL` — 프런트가 호출할 백엔드 base. 기본 `http://localhost:4000/api/v1`.

---

## 2. 디렉터리 지도

```
patima-naver-ss/
├─ apps/
│  ├─ backend/src/
│  │  ├─ main.ts                          NestFactory 부트
│  │  ├─ app.module.ts                    모든 서비스 한 모듈에 등록
│  │  ├─ app.controller.ts                모든 HTTP 엔드포인트(50+개) 한 컨트롤러에 모음
│  │  ├─ database.service.ts              snapshot 보유 + Postgres/file 영속화
│  │  ├─ helpers.ts                       비즈니스 로직의 70%가 여기 (955줄)
│  │  ├─ run-tests.ts                     단순 assertion 기반 단위 테스트 러너 (jest 미사용)
│  │  ├─ signature-enrichment.ts          0415수정계획3 작업으로 도입한 enrich 모듈
│  │  ├─ store.service.ts                 스토어 CRUD/primary/active
│  │  ├─ credential.service.ts            네이버 커머스 키 (bcrypt+aes 암호화)
│  │  ├─ crypto.service.ts                AES helper
│  │  ├─ naver-commerce.service.ts        실제 네이버 API 클라이언트 (851줄, 토큰 캐시)
│  │  ├─ naver-commerce-config.service.ts 키 메타
│  │  ├─ order-sync.service.ts            주문 수집 + listOrders/listOrderSourceSignatures
│  │  ├─ order-mapping.service.ts         주문 시그니처 → 판매단위 수동 매핑
│  │  ├─ mapping-seed.service.ts          관리코드/규칙 기반 자동 매핑 시드
│  │  ├─ sales-unit.service.ts            판매단위(canonical) CRUD + 그룹/하위 attach
│  │  ├─ sales-unit-auto-mapper.ts        판매단위 자동 매핑 알고리즘
│  │  ├─ ads.service.ts                   광고비 엑셀 업로드/확정/리스트
│  │  ├─ ad-mapping-engine.ts             광고비 → 판매단위 매핑 엔진
│  │  ├─ campaign-mapping.service.ts      광고 캠페인 패턴 매핑 규칙
│  │  ├─ cost.service.ts                  판매단위별 원가 설정
│  │  ├─ profit.service.ts                대시보드 + 손익 + unmapped summary
│  │  ├─ operation.service.ts             비동기 작업(주문 수집 등) 상태 추적
│  │  ├─ audit-log.service.ts             변경 감사 로그 기록
│  │  └─ environment-bootstrap.service.ts 부트시 .env 점검
│  └─ frontend/
│     ├─ app/
│     │  ├─ layout.tsx                    루트 레이아웃
│     │  ├─ page.tsx                      홈 (대시보드 리다이렉트)
│     │  ├─ (main)/                       메인 라우트 그룹 (사이드바 레이아웃)
│     │  │  ├─ profits/page.tsx           일별 손익
│     │  │  ├─ orders/page.tsx            주문 + 시그니처
│     │  │  ├─ mappings/page.tsx          수동 매핑
│     │  │  ├─ sales-units/page.tsx       판매단위
│     │  │  ├─ ads/page.tsx               광고비
│     │  │  ├─ costs/page.tsx             원가
│     │  │  ├─ operations/page.tsx        작업 큐
│     │  │  └─ settings/page.tsx          스토어/키
│     │  └─ api/                          Next route handlers (백엔드 프록시 일부)
│     │     ├─ _utils/                    프록시 공용 유틸
│     │     └─ <도메인>/route.ts          /api/v1/<도메인> 으로 패스스루
│     ├─ components/
│     │  ├─ layout/                       Sidebar, Topbar
│     │  ├─ shared/                       DataTable, Pagination, EmptyState 등
│     │  ├─ profits/, orders/, mappings/, sales-units/, ads/, costs/, operations/, stores/, dashboard/
│     │  │  └─ <domain>-view.tsx          페이지별 메인 뷰. 대부분 단일 거대 컴포넌트 + 모달들
│     ├─ lib/
│     │  ├─ api/
│     │  │  ├─ client.ts                  fetchApi, withQuery (전역 timeout 2.5s)
│     │  │  ├─ services.ts                getProfitsPageData 같은 페이지 단위 fetch (1012줄)
│     │  │  ├─ types.ts                   프런트 전용 응답 타입
│     │  │  ├─ mock-data.ts               mock fallback (USE_*_MOCK_FALLBACK 토글)
│     │  │  ├─ browser.ts                 브라우저용 fetch 래퍼
│     │  │  ├─ route-proxy.ts             Next route → 백엔드 프록시 헬퍼
│     │  │  └─ upstream.ts                백엔드 base URL 결정
│     │  ├─ format.ts                     숫자/날짜/통화 포맷
│     │  ├─ status-tone.ts                상태 → 색상 매핑
│     │  └─ cn.ts                         tailwind 클래스 머지
│     └─ next.config.js, postcss/tailwind config 등
├─ packages/shared/src/
│  ├─ types.ts                            DatabaseShape 등 양쪽 공통 타입 (341줄)
│  ├─ normalization.ts                    normalizeText, normalizeMatchAlias, createSourceSignature
│  └─ index.ts                            re-export
├─ scripts/
│  ├─ db-export.mjs / db-import.mjs       postgres ↔ json dump
│  ├─ diagnostics/inspect-sparse-signatures.mjs   0415수정계획3 작업 1-1 산출물
│  └─ sales-unit-reset/ (이전 작업 흔적)
├─ migrations/                            Postgres 보조 SQL (백엔드는 직접 실행 안 함, 수동)
├─ backups/                               JSON 덤프 보관소
├─ docker-compose.yml                     Postgres 서비스
├─ 0415수정계획.md / 2.md / 3.md         이전 라운드 기획서들
├─ 수정계획3-1.md                         손익 timeout 픽스 (완료)
└─ 0416수정계획.md                        수동 매핑 시 오토매퍼 자동 트리거 제거
```

---

## 3. 도메인 & 데이터 모델 (핵심만)

### `DatabaseShape` 컬렉션 14개 (`packages/shared/src/types.ts:326`)

| 컬렉션 | 무엇 |
|---|---|
| `stores` | 스토어. `isPrimary`, `isActive` 플래그. |
| `commerceCredentials` | 스토어별 네이버 API 키 (암호화). |
| `products` | 네이버 상품 카탈로그 캐시. `externalProductId` 키. |
| `canonicalSalesUnits` | 판매단위 (사용자가 정의). `parentSalesUnitId`로 그룹/자식 가능. `isGroup`, `isStoreLevel`. |
| `orderSourceSignatures` | 주문 시그니처 (상품명 + 옵션의 정규화 키). 매핑의 단위. |
| `orders` / `orderItems` | 네이버 원본 주문. `OrderItem`이 `orderSourceSignatureId` 와 `canonicalSalesUnitId` 둘 다 가짐. |
| `campaignMappings` | 광고 캠페인 패턴 → 판매단위 매핑 규칙. |
| `adExcelUploads` / `adUploadPreviewRows` / `adCampaignDailyCosts` | 광고비 엑셀 업로드 파이프라인 (preview → confirm). `state`, `weekdayValidationStatus` 등 상태머신. |
| `salesUnitCostSettings` | 판매단위별 원가/시작·종료일. |
| `operations` | 비동기 작업 큐 기록. |
| `auditLogs` | 모든 변경 감사 로그. |

### 매핑 상태 (`MappingStatus = "MAPPED" | "UNMAPPED" | "CONFLICT"`)

- **시그니처 기준**: `getSignatureMappingStatus` (`helpers.ts:453`).
  - `signature.mappingStatus` 가 있으면 그것, 없으면 `canonicalSalesUnitId` 유무.
- **주문 아이템 기준**: `getOrderItemMappingStatus` (`helpers.ts`).
  - 우선 시그니처를 찾아 그 status 사용, 없으면 자체 `canonicalSalesUnitId` 유무.
  - `getSignatureIndex(database)` WeakMap 캐시로 O(1) 룩업 (수정계획3-1에서 개선 완료).
- **광고 캠페인 기준**: `getAdMappingStatus` (`helpers.ts:471`).
  - `mappingReason === "MULTIPLE_RULES"` → CONFLICT, 나머지는 `canonicalSalesUnitId` 유무.

---

## 4. 백엔드 아키텍처 핵심

### 4-1. snapshot 기반 in-memory DB

`DatabaseService` (`apps/backend/src/database.service.ts`)

```ts
private database: DatabaseShape = createEmptyDatabase();

/**
 * Returns the live snapshot reference. **MUST NOT be mutated by callers.**
 * Use `write(mutator)` for any change.
 */
getSnapshot(): DatabaseShape {
  return this.database;          // ← reference 그대로 반환 (클론 없음)
}

write<T>(mutator: (draft: DatabaseShape) => T): T {
  const draft = this.cloneSnapshot(this.database);  // mutation은 클론 위에서
  const result = mutator(draft);
  this.database = draft;                            // reference 교체
  // postgres 모드: 비동기 큐로 영속화
  // file 모드: 동기 writeFileSync
  return result;
}
```

- **`getSnapshot()`은 live reference를 반환** — 호출자는 절대 mutate하면 안 됨. 같은 요청 안에서 여러 번 호출해도 동일 reference → WeakMap 캐시가 유효.
- **모든 mutation은 `write(draft => { ... })` 콜백 안에서**. 콜백이 받는 `draft`는 안전한 사본(cloneSnapshot). write 완료 시 새 reference로 교체돼 기존 캐시 자동 무효화.
- Postgres 영속화는 `STORAGE_TABLES` 정의를 따라 **테이블 단위 TRUNCATE + bulk INSERT** (snapshot-replace). 대용량에 비효율이지만 일관성 단순.
- `normalizeSnapshot`이 로딩 시 마이그레이션 보정 (예: signature.mappingStatus 채움).

### 4-2. 모듈 구조

- 모든 서비스가 `app.module.ts`에 한 번에 등록.
- HTTP 진입점은 `app.controller.ts` 단일 컨트롤러 (50+개 라우트). 각 메서드는 적절한 service에 위임.
- 베이스 prefix `api/v1`. CORS, 글로벌 파이프 등은 `main.ts`에서 설정.

### 4-3. helpers.ts — 비즈니스 로직 허브 (955줄)

특히 자주 쓰이는 export:

| 함수 | 역할 |
|---|---|
| `createEmptyDatabase` | 빈 DB shape |
| `repairMojibakeText` | 한글 mojibake 복구 |
| `getActiveConfirmedUploadIds` | 광고 업로드 활성화된 ID Set |
| `getSignatureIndex` | WeakMap 캐시 기반 signature id→obj 인덱스 (O(1) 룩업) |
| `getSignatureMappingStatus`, `getOrderItemMappingStatus`, `getAdMappingStatus` | 매핑 상태 계산 |
| `getCostSettingForDate` | 날짜 기준 활성 원가 설정 |
| `calculateFee` | 수수료 계산 |
| `calculateDailyProfitRows` | 일별 손익 row 계산 |
| `calculateDashboardSummary` | 대시보드 집계 |
| `mapOrderItemResponse` | 주문 아이템 → API 응답 shape |
| `formatApiSuccess` | 표준 성공 envelope `{success, data, message}` |
| `sortByUpdatedAtDesc` | 정렬 |
| `ensureNormalizedLength`, `ensureStoreExists`, `ensureNoCrossStoreReference` | NestJS 예외 던지는 가드 |

### 4-4. signature-enrichment.ts (0415수정계획3 작업 산출물)

- `isMeaningfulName(s, contextOptionInfo?)` — 사이즈 옵션값 등 무의미한 이름 판별.
- `extractNameFromOptionInfo(optionInfo)` — `[태그]상품명: 옵션` 또는 `상품명: 옵션` 두 패턴 모두 캡처.
- `enrichSignatureDisplayName(db, signature, ctx?)` — 4단계 fallback (snapshot → orderItem → optionInfo → product). `EnrichmentContext`로 N+1 회피용 미리 계산된 Map 주입 가능.
- 결과는 `{ fallbackProductName, fallbackProductNameSource }`.
- `order-sync.service.ts:listOrderSourceSignatures` 가 호출. API 응답에 `fallbackProductName`, `fallbackProductNameSource`, `storeSlug(null)` 추가.

### 4-5. 테스트 (`run-tests.ts`)

- jest/vitest 안 씀. 자체 assertion 함수로 직렬 실행. 53개 케이스 (기존 51 + getSignatureIndex 캐시 2개).
- 추가 시 `runTest("name", () => { ... })` 패턴 그대로 따르기.

---

## 5. 프런트엔드 아키텍처 핵심

### 5-1. 라우팅

- App Router. `(main)` 그룹 안의 페이지들이 사이드바 레이아웃을 공유.
- 각 페이지는 SSR로 `services.ts`의 `get<도메인>PageData()`를 호출 → 단일 view 컴포넌트에 props로 넘김.

### 5-2. fetchApi의 timeout 정책 (`apps/frontend/lib/api/client.ts`)

```ts
signal: AbortSignal.timeout(2500),
```

- **모든 API 호출이 2.5초 컷.**
- `fallback`을 넘기면 timeout/실패 시 mock으로 대체 + `source: "mock"`.
- `fallback`이 없으면 `throw new Error("${label}: ${message}")` → SSR 페이지 에러.
- 타입이 큰 페이지(profits, dashboard)는 `USE_*_MOCK_FALLBACK` 토글로 fallback 사용 여부 제어.

### 5-3. services.ts 패턴

- 페이지별 단일 함수 (`getProfitsPageData`, `getMappingsPageData`, ...).
- 내부에서 `Promise.all`로 여러 엔드포인트를 병렬 호출하고 결과를 합쳐 페이지 데이터로 정형.
- 각 호출은 `{label, path, fallback?}` 형식의 `fetchApi` 인자.

### 5-4. 컴포넌트 컨벤션

- `<domain>-view.tsx` 가 페이지 메인. 종종 700~1500줄. 모달/필터/테이블 모두 한 파일.
- 공통 빌딩 블록: `components/shared/` (DataTable, Pagination, EmptyState, Toast 등).
- 스타일: Tailwind 유틸 클래스. 디자인 토큰은 직접 클래스로 (예 `text-ink/50`, `bg-paper`).
- 아이콘: `lucide-react`.

---

## 6. API 표면 (백엔드 라우트 요약)

기본 prefix `/api/v1`. 모두 `formatApiSuccess` 응답 envelope.

| 도메인 | 주요 엔드포인트 |
|---|---|
| stores | `GET/POST/PATCH stores`, `set-primary`, `deactivate`, `activate`, `commerce-credentials [GET/POST]`, `commerce-credentials/test` |
| order sync | `POST stores/:id/order-sync`, `GET order-items`, `GET order-source-signatures` |
| 매핑 | `POST order-source-signatures/:id/mapping`, `POST .../batch-mapping`, `POST mapping-seed/:storeId` |
| 판매단위 | `GET/POST/PATCH canonical-sales-units`, `deactivate`, `activate`, `group/create`, `group/:id/attach-child`, `group/:id/detach-child`, `group/:id/dissolve` |
| 광고비 | `POST ad-uploads/preview`, `GET ad-uploads/:id/preview-rows`, `GET ad-uploads`, `POST ad-uploads/:id/confirm`, `DELETE ad-uploads/:id` |
| 광고 매핑 | `GET ad-campaign-costs`, `POST ...:id/intentional-unmapped`, `POST .../batch-intentional-unmapped`, `POST ...:id/mapping`, `POST .../batch-mapping`, `POST ...:id/recalculate-mapping`, `POST .../batch-recalculate-mapping` |
| 캠페인 규칙 | `GET/POST/PATCH campaign-mappings`, `deactivate`, `activate` |
| 원가 | `GET/POST/PATCH sales-unit-cost-settings`, `close`, `deactivate` |
| 손익 | `GET dashboard/summary`, `GET profits/latest-date`, `GET profits/daily-sales-units`, `GET profits/daily-sales-units/:salesUnitId`, `GET profits/unmapped-summary` |
| 운영 | `GET operations`, `GET operations/:id`, `POST operations/:id/retry` |

---

## 7. 데이터 흐름 시나리오 (자주 보는 경로)

### 7-1. 주문 수집 → 매핑 → 손익

1. 사용자가 `POST stores/:id/order-sync` → `OrderSyncService.syncOrders`가 네이버 API 호출 → `orders`/`orderItems`/`orderSourceSignatures` upsert.
2. `mapping-seed`가 관리코드/규칙으로 자동 매핑 시도 → `signature.canonicalSalesUnitId` 채움.
3. 남은 시그니처는 사용자가 `mappings` 페이지에서 수동 매핑 (`POST .../mapping` 또는 `batch-mapping`).
4. `profits` 페이지 SSR이 `Promise.all`로:
   - `profits/latest-date`
   - `dashboard/summary`
   - `profits/daily-sales-units`
   - `profits/unmapped-summary`
   - `profits/daily-sales-units/:salesUnitId` (선택된 첫 행)
   를 호출하고 한 화면에 렌더.

### 7-2. 광고비 업로드

1. `POST ad-uploads/preview` (multipart) → `adUploadPreviewRows` 생성, `weekdayValidationStatus` 검증.
2. `POST ad-uploads/:id/confirm` → 새 `adCampaignDailyCosts` 행 생성, 캠페인 매핑 자동 적용.
3. 손익 페이지가 `getActiveConfirmedUploadIds`로 활성 업로드 행만 합산.

---

## 8. 코드 컨벤션

- **TypeScript strict**. 타입 강제, 타입 캐스팅은 최소화.
- Nest 데코레이터/DI 패턴.
- 모든 ID는 `uuid v4` (`createId()`).
- 응답 envelope: `{success: true, data, message: null}` (`formatApiSuccess`).
- 에러 envelope: NestJS Exception(`BadRequestException` 등) + `{success: false, message, errors[]}`.
- 시간: KST(`Asia/Seoul`) 기준. `nowInSeoul`, `KST_TIMEZONE` 사용.
- 정규화 텍스트는 항상 `normalizeText` / `normalizeMatchAlias`(packages/shared) 통과.
- 네이밍: camelCase, Nest 서비스/컨트롤러는 PascalCase.
- 새 endpoint 추가 시 → service 메서드 추가 → controller에 라우트 추가 → frontend `services.ts`에 호출 추가 → page.tsx에서 사용.

---

## 9. 알려진 함정 (시간 절약용)

1. ~~`DatabaseService.getSnapshot()`은 매 호출 deep-clone~~ → **해결됨** (수정계획3-1). 이제 live reference 반환. 호출자가 mutate하면 안 됨.
2. ~~`getOrderItemMappingStatus`는 풀스캔~~ → **해결됨** (수정계획3-1). `getSignatureIndex` WeakMap으로 O(1) 룩업. `getUnmappedSummary`/`calculateDashboardSummary`도 단일 패스화 완료.
3. **fetchApi는 2.5s 컷**. 백엔드가 느리면 SSR이 통째로 fail. fallback 없는 호출은 주의.
4. **mock fallback 토글**(`USE_*_MOCK_FALLBACK`)이 false면 진짜로 던짐. dev 중 백엔드 안 떠있으면 페이지 안 열림.
5. **Postgres 영속화는 비동기 큐**. `write()` 콜백은 동기로 메모리 반영 후 큐잉. 큐 실패 시 stderr 로그만 — 다음 부팅에서 in-memory가 DB와 다를 수 있음. 운영 시 모니터링 필요.
6. **Postgres 모드는 TRUNCATE + bulk INSERT**. 컬렉션이 커지면 영속화 비용 증가. 부분 update API 없음.
7. **시그니처 enrichment의 `EnrichmentContext`** — `listOrderSourceSignatures` 한정으로만 도입. 다른 시그니처 조회 API에는 안 흐름.
8. **store slug 미확보** — 0415수정계획3.md 3-2 딥링크는 storeSlug 항상 null. 딥링크 버튼이 절대 안 보임. 추후 작업.
9. **테스트 러너는 자체 구현**. 외부 mock/snapshot 도구 없음. assert 함수 패턴만.
10. **mapping-seed.service.ts**는 ID 기반 매핑으로 전환 작업 중(메모리 기록). 검증 미완 상태.
11. **수동 매핑 시 불필요한 오토매퍼 자동 트리거** — `order-mapping.service.ts:saveMappingsInternal()`이 매핑 저장 후 `operationService.enqueue("RECALCULATE_ORDER_MAPPING")`을 호출해 전체 오토매퍼를 비동기로 재실행. 수동 매핑은 사용자가 이미 판단한 결과이므로 불필요한 재연산. (0416수정계획.md에서 개선 예정)

---

## 10. 최근 작업 이력 메모

- **0415수정계획.md**: 관리코드 매핑 + 스토어 전체 광고비 (완료).
- **0415수정계획2.md**: 판매단위 그룹화 (완료).
- **2026-04-15 판매단위 리셋**: 33개 → 22개로 재편 (완료).
- **0415수정계획3.md**: 옵션정보만 있는 시그니처 식별성 개선.
  - 작업 1-3 (조사 스크립트, signature-enrichment, 프런트 보조 라벨, 딥링크 자리) 완료.
  - 작업 4 (수집 훅·backfill로 데이터 정규화), 5 (커머스 API 단건 조회) 미진행.
- **수정계획3-1.md**: 손익/매핑 timeout 근본 해결 (**완료** — snapshot reference 안정화 + `getSignatureIndex` WeakMap O(1) 인덱스 + `getUnmappedSummary`/`calculateDashboardSummary` 단일 패스화. 테스트 51→53개).

---

## 11. 어디부터 읽기 시작할지 (시나리오별)

- **신규 기능 추가**: `app.controller.ts` 라우트 모양 → 해당 service.ts → `helpers.ts`의 공용 함수 → `packages/shared/types.ts`. 그 다음 frontend `services.ts` + 해당 `<domain>-view.tsx`.
- **버그 디버깅**: 에러 메시지의 파일/라인을 우선. 데이터 흐름이면 `database.service.ts` → 해당 service → helpers.
- **성능 이슈**: §9 함정 1·2번 먼저 의심. `getSnapshot` 호출 횟수와 helpers 안의 `find/filter`를 grep.
- **타입 헷갈릴 때**: `packages/shared/src/types.ts` 가 단일 진실 소스.
- **스키마 변경**: shared/types.ts → `database.service.ts:STORAGE_TABLES` → `helpers.ts:createEmptyDatabase` → `normalizeSnapshot` (마이그레이션 보정) → 영향 받는 service들.
