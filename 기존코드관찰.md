# 기존코드관찰.md

> 새로운 AI 세션이 이 파일 하나만 읽고 전체 프로젝트 구조와 비즈니스 로직을 파악할 수 있도록 작성된 고밀도 정리본.  
> 작성일: 2026-04-17 (최종 업데이트: 2026-04-30)

---

## 1. 프로젝트 개요

**Patima Naver SS** — 네이버 스마트스토어 손익 관리 MVP  
주문·광고비·원가를 통합 추적하여 일별 판매단위 손익을 계산하는 내부 관리 도구.

- 모노레포 (npm workspaces)
- Backend: NestJS 스타일 (단일 AppModule), Port **4000** (기본값, `PORT` 환경변수로 변경 가능)
- Frontend: Next.js 15 App Router, Port 3000
- Shared: `@patima/shared` 패키지 (타입 + 정규화 유틸)
- Storage: PostgreSQL (`DATABASE_URL` 있으면) 또는 JSON 파일 (`apps/backend/data/database.json`)

```
patima-naver-ss/
├── apps/
│   ├── backend/src/          ← NestJS 서비스들 (*.service.ts, *.ts)
│   └── frontend/
│       ├── app/(main)/       ← Next.js 페이지
│       ├── app/api/          ← Next.js API Routes (백엔드 프록시)
│       ├── components/       ← 뷰 컴포넌트
│       └── lib/api/          ← 프론트엔드 API 클라이언트
└── packages/shared/src/      ← 공유 타입, 정규화 함수
```

---

## 2. 기술 스택

| 영역 | 기술 |
|------|------|
| Backend | NestJS (단일 AppModule), TypeScript, pg (PostgreSQL), XLSX, bcryptjs |
| Frontend | Next.js 15 App Router, TypeScript, Tailwind CSS |
| Shared | `@patima/shared` (types.ts, normalization.ts) |
| Storage | PostgreSQL (PRIMARY) / JSON 파일 (FALLBACK) |
| Crypto | Node crypto (AES-256-GCM, SHA-256), bcryptjs (Naver client secret 서명) |
| Auth | 없음 (localhost admin only). 자격증명은 AES-256-GCM 암호화 저장 |

---

## 3. 핵심 데이터 모델 (`packages/shared/src/types.ts`)

### 3.1 기본 타입 열거

```typescript
OperationStatus: "QUEUED" | "RUNNING" | "SUCCEEDED" | "FAILED"
OperationType: "ORDER_SYNC" | "AD_UPLOAD_CONFIRM" | "RECALCULATE_ORDER_MAPPING" | "RECALCULATE_AD_MAPPING"
SaleStatus: "SALE" | "CANCELED" | "CANCEL_REQUESTED" | "RETURNED" | "EXCHANGED" | "UNKNOWN"
MappingStatus: "MAPPED" | "UNMAPPED" | "CONFLICT"
ProfitStatus: "COMPLETE" | "INCOMPLETE_COST"
UploadState: "PREVIEW_PARSED" | "CONFIRMED" | "REPLACED" | "EXPIRED" | "DELETED"
WeekdayValidationStatus: "PENDING" | "PASSED" | "MISSING" | "MULTIPLE" | "MISMATCH"
CampaignMappingReason: "RULE_MATCHED" | "MANUAL_MAPPED" | "NO_RULE" | "MULTIPLE_RULES" | "INTENTIONALLY_UNMAPPED"
```

### 3.2 주요 엔티티

**Store** — 네이버 스마트스토어 1개
- `id`, `name`, `sellerAccountId`, `channelNo`, `isPrimary`, `isActive`
- `lastOrderSyncAt`, `lastOrderSyncStatus: "SUCCEEDED"|"FAILED"|"NEVER"`
- `credentialConnectionStatus: "SUCCEEDED"|"FAILED"|"NOT_TESTED"`

**CanonicalSalesUnit** — 표준 판매단위 (상품/옵션의 canonical 묶음)
- `id`, `storeId`, `displayName`
- `matchAliases: string[]` — 텍스트 매핑용 별칭
- `normalizedMatchAliases: string[]` — 정규화된 별칭 (자동 계산)
- `linkedProductIds: string[]` — ID 기반 매핑 (externalProductId)
- `linkedOptionCodes: string[]` — ID 기반 매핑 (optionCode)
- `linkedManageCodes: string[]` — ID 기반 매핑 (optionManageCode, 함께배송)
- `isStoreLevel: boolean` — "스토어 전체 광고비" 특수 유닛
- `isGroup: boolean` — 그룹 부모 여부 (자식들의 손익 합산)
- `parentSalesUnitId: string | null` — 그룹 계층 (최대 1단계)

**OrderSourceSignature** — 주문 상품명+옵션 조합의 유일 식별자
- `sourceSignature: string` — `normalizedProductName || normalizedOptionInfo`
- `canonicalSalesUnitId: string | null` — 매핑된 판매단위
- `mappingStatus: MappingStatus`
- `confirmedAt: string | null` — 수동 확인 완료 시각 (null이면 auto-recalc 대상)
- `rawProductNameSnapshot`, `rawOptionInfoSnapshot` — 원문 스냅샷
- **표시명은 엔티티에 저장하지 않음**: `displayName` 필드 없음. 런타임에 `enrichSignatureDisplayName()` (13장) 으로 폴백 체인 따라 매번 계산.

**OrderItem** — 개별 주문 아이템
- `externalProductOrderId` — 네이버 고유 ID
- `orderSourceSignatureId` — 시그니처 참조
- `canonicalSalesUnitId` — 매핑된 판매단위 (auto-mapper가 채움)
- `productPaymentAmount` — 상품 결제금액
- `paymentCommission`, `knowledgeShoppingSellingInterlockCommission` — 네이버 수수료
- `saleStatus: SaleStatus` — 손익 계산 시 "SALE"만 포함

**AdExcelUpload** — 네이버 DA 엑셀 광고비 업로드
- `state: UploadState` — PREVIEW_PARSED → CONFIRMED 흐름
- `weekdayValidationStatus` — PASSED인 것만 유효
- `isActive: boolean` — CONFIRMED + PASSED + isActive인 것만 손익 계산에 반영

**AdCampaignDailyCost** — 확정된 캠페인 일별 광고비
- `canonicalSalesUnitId` — 매핑된 판매단위
- `mappingReason: CampaignMappingReason`
- `sourceUploadId` — 원본 업로드 참조

**SalesUnitCostSetting** — 판매단위별 원가 설정 (기간별)
- `unitCost: number` — 수량당 원가
- `feeRate: number | null` — 수수료율 (null이면 API 실제값 사용)
- `otherCost: number` — 수량당 기타비용
- `effectiveFrom`, `effectiveTo` — 유효 기간

**DatabaseShape** — 전체 DB 형태 (14개 컬렉션)
```typescript
{ stores, commerceCredentials, products, canonicalSalesUnits,
  orderSourceSignatures, orders, orderItems, campaignMappings,
  adExcelUploads, adUploadPreviewRows, adCampaignDailyCosts,
  salesUnitCostSettings, operations, auditLogs }
```

---

## 4. 백엔드 구조 (`apps/backend/src/`)

### 4.1 서비스 목록 및 역할

| 파일 | 역할 |
|------|------|
| `database.service.ts` | 단일 in-memory DB + PostgreSQL/JSON 파일 이중 저장소 |
| `app.controller.ts` | 모든 REST API 엔드포인트 (`@Controller("api/v1")`) |
| `store.service.ts` | 스토어 CRUD, 활성화/비활성화 |
| `credential.service.ts` | API 자격증명 upsert/test |
| `order-sync.service.ts` | 주문 동기화 (네이버 API 또는 mock), 시그니처 생성 |
| `order-mapping.service.ts` | 시그니처 수동 매핑 저장 |
| `sales-unit.service.ts` | 판매단위 CRUD, 그룹 생성/해체 |
| `mapping-seed.service.ts` | 초기 판매단위 자동 생성 (주문 데이터 기반) |
| `sales-unit-auto-mapper.ts` | 주문→판매단위 자동 매핑 재계산 |
| `ad-mapping-engine.ts` | 광고→판매단위 자동 매핑 재계산 |
| `ads.service.ts` | 광고비 엑셀 업로드, 파싱, 확정 |
| `campaign-mapping.service.ts` | 캠페인 매핑 규칙 CRUD |
| `cost.service.ts` | 원가 설정 CRUD |
| `profit.service.ts` | 손익 계산 (대시보드, 일별 판매단위) |
| `operation.service.ts` | 비동기 작업 큐 (스토어별 순차 실행) |
| `audit-log.service.ts` | 변경 이력 기록 |
| `naver-commerce.service.ts` | 네이버 커머스 API 통신 |
| `naver-commerce-config.service.ts` | env 기반 자격증명 해석 (`getEnvCredentialForStore`, `getBootstrapStorePayload`, `maskClientId`) |
| `crypto.service.ts` | AES-256-GCM 기반 자격증명 암·복호화 (`MASTER_KEY`를 SHA-256 해시하여 키 유도) |
| `environment-bootstrap.service.ts` | `NAVER_BOOTSTRAP_STORE=true`일 때만 env → 스토어/자격증명 자동 생성 |
| `signature-enrichment.ts` | 시그니처 표시명 보강 (snapshot→orderItem→optionInfo→product→commerceApi 폴백 체인) |
| `helpers.ts` | 손익 계산 핵심 함수 + 유틸 (WeakMap 시그니처 인덱스 캐시 포함) |
| `run-tests.ts` | 백엔드 단위 테스트 하네스 (정규화·매핑·손익·광고·시그니처 enrichment 등 전체 커버) |

### 4.2 DatabaseService 핵심 패턴

```typescript
// 읽기: 스냅샷 참조 반환 (불변 취급)
const snapshot = databaseService.getSnapshot();

// 쓰기: draft 클론 → 뮤테이션 → 교체 (원자적)
databaseService.write((draft) => {
  draft.orders.push(newOrder);
  recalculateOrderMappingsForStore(draft, storeId);
});
```

- PostgreSQL 모드: `write()` 호출 시 비동기 큐(`persistenceQueue`)에 적재 → 트랜잭션으로 각 테이블 TRUNCATE + INSERT
- 각 테이블은 `id TEXT PRIMARY KEY, payload JSONB` 구조 (완전 JSON 직렬화)
- `normalizeSnapshot()`: 로드 시 `canonicalSalesUnits` 마이그레이션, `orderSourceSignatures` 매핑상태 재계산
- **WeakMap 캐시**: `getSignatureIndex(database)` — 시그니처 ID→객체 인덱스, DB 참조 교체 시 자동 무효화

### 4.3 API 엔드포인트 요약

```
GET    /api/v1/health
GET    /api/v1/stores
POST   /api/v1/stores
PATCH  /api/v1/stores/:storeId
POST   /api/v1/stores/:storeId/set-primary
POST   /api/v1/stores/:storeId/deactivate / activate
POST   /api/v1/stores/:storeId/commerce-credentials
GET    /api/v1/stores/:storeId/commerce-credentials
POST   /api/v1/stores/:storeId/commerce-credentials/test
POST   /api/v1/stores/:storeId/order-sync          → 주문 동기화 큐잉
GET    /api/v1/order-items                          → 주문 아이템 목록 (필터/페이징)
GET    /api/v1/order-source-signatures              → 시그니처 목록
POST   /api/v1/order-source-signatures/:id/mapping → 단건 수동 매핑
POST   /api/v1/order-source-signatures/batch-mapping
POST   /api/v1/mapping-seed/:storeId               → 초기 판매단위 자동 생성
GET    /api/v1/canonical-sales-units                → 판매단위 목록
POST   /api/v1/canonical-sales-units               → 판매단위 생성
PATCH  /api/v1/canonical-sales-units/:id
POST   /api/v1/canonical-sales-units/:id/deactivate / activate
POST   /api/v1/canonical-sales-units/group/create
POST   /api/v1/canonical-sales-units/group/:groupId/attach-child
POST   /api/v1/canonical-sales-units/group/:childId/detach-child
POST   /api/v1/canonical-sales-units/group/:groupId/dissolve
POST   /api/v1/ad-uploads/preview                  → 엑셀 파싱 프리뷰
GET    /api/v1/ad-uploads/:uploadId/preview-rows
GET    /api/v1/ad-uploads
POST   /api/v1/ad-uploads/:uploadId/confirm        → 확정 큐잉
DELETE /api/v1/ad-uploads/:uploadId
GET    /api/v1/ad-campaign-costs                   → 확정된 광고비 목록
POST   /api/v1/ad-campaign-costs/:id/intentional-unmapped
POST   /api/v1/ad-campaign-costs/batch-intentional-unmapped
POST   /api/v1/ad-campaign-costs/:id/mapping       → 수동 매핑
POST   /api/v1/ad-campaign-costs/batch-mapping
POST   /api/v1/ad-campaign-costs/:id/recalculate-mapping
POST   /api/v1/ad-campaign-costs/batch-recalculate-mapping
GET    /api/v1/campaign-mappings
POST   /api/v1/campaign-mappings
PATCH  /api/v1/campaign-mappings/:id
POST   /api/v1/campaign-mappings/:id/activate / deactivate
GET    /api/v1/sales-unit-cost-settings
POST   /api/v1/sales-unit-cost-settings
PATCH  /api/v1/sales-unit-cost-settings/:id
POST   /api/v1/sales-unit-cost-settings/:id/close / deactivate
GET    /api/v1/dashboard/summary                   → 일별 요약 손익
GET    /api/v1/profits/latest-date
GET    /api/v1/profits/daily-sales-units           → 판매단위별 일별 손익
GET    /api/v1/profits/daily-sales-units/:salesUnitId → 상세
GET    /api/v1/profits/unmapped-summary
GET    /api/v1/operations
GET    /api/v1/operations/:id
POST   /api/v1/operations/:id/retry
```

---

## 5. 핵심 비즈니스 로직

### 5.1 주문 매핑 시스템 (`sales-unit-auto-mapper.ts`)

주문 동기화 또는 판매단위 변경 시 `recalculateOrderMappingsForStore(draft, storeId)` 호출.

**Phase 1 — 시그니처 텍스트 매핑** (confirmedAt 없는 것만):
- `normalizedMatchAliases` 포함 여부로 매칭
- 1개 매칭 → MAPPED, 2개 이상 → CONFLICT

**Phase 2 — 주문 아이템 ID 기반 매핑** (우선순위 순):
1. `signature.confirmedAt` 있으면 → 시그니처 매핑 그대로 사용
2. 함께배송 아이템이면 `optionManageCode → linkedManageCodes` 검색
3. `optionCode → linkedOptionCodes` 검색
4. `externalProductId → linkedProductIds` 검색 (함께배송 제외)
5. 텍스트 fallback (시그니처 매핑 상속)
- 비활성화된 판매단위: `deactivatedAt > paymentDate`이면 배정 가능, 아니면 null

> 성능 최적화: `getActiveSalesUnitsForIdMapping()` 호출 시 `manageCodeMap`, `optionCodeMap`, `productIdMap` 3개의 Map을 사전 구축하여 Phase 2의 각 검색을 O(1)로 수행. 주문 수가 많을 때 전체 성능에 직결됨.

**Phase 3 — 역추론**: 같은 시그니처 내 아이템이 모두 동일 판매단위 → 시그니처도 MAPPED로 승격, 서로 다른 판매단위가 섞이면 CONFLICT로 표시.

### 5.2 광고 매핑 시스템 (`ad-mapping-engine.ts`)

`recalculateAdMappingsForStore(database, storeId)` 호출로 모든 광고비 재계산.

**evaluateAdMapping** 우선순위:
1. MANUAL_MAPPED 오버라이드 → 지정 판매단위 (자식이면 부모로 승격)
2. INTENTIONALLY_UNMAPPED 오버라이드 → null
3. 명시적 캠페인 매핑 규칙 (`CampaignSalesUnitMapping`) — 패턴 포함 여부 검사
   - 1개 매칭 → RULE_MATCHED, 2개 이상 → MULTIPLE_RULES (CONFLICT)
4. 하드코딩 키워드 규칙 (`KEYWORD_MAPPING_RULES`) — 세부 키워드 우선, 포괄 키워드 후순
   - 스토어 전체(`isStoreLevel: true`) 또는 특정 판매단위로 매핑
5. 텍스트 기반 자동매핑 fallback (판매단위 `normalizedMatchAliases` 기준)

**자식→부모 승격**: 광고비가 자식 판매단위에 매핑되면 부모 그룹으로 자동 승격.

**스토어 레벨 판매단위**: `isStoreLevel: true` 특수 유닛("스토어 전체 광고비"). 키워드 규칙에 걸리면 자동 생성.

### 5.3 손익 계산 로직 (`helpers.ts` - `calculateDailyProfitRows`)

**Step 1 — 자식/단독 판매단위 주문 집계** (날짜:판매단위별 Map):
- `saleStatus === "SALE"` + `paymentDate` 있는 주문아이템만
- `calculateFee(item, costSetting)`: `paymentCommission + knowledgeShoppingSellingInterlockCommission` (null이면 feeRate로 폴백)
- 비용 항목: `totalProductRevenue`, `totalDeliveryFeeAmount`, `totalFeeCost`, `totalUnitCost`, `totalOtherCost`

**Step 2 — 자식→부모 롤업** (childRowMap → groupRowMap):
- `parentSalesUnitId` 있는 유닛의 행을 부모 ID로 합산

**Step 3 — 광고비 귀속**:
- 광고비가 그룹이면 `groupRowMap`에, 단독이면 `childRowMap`에 적재
- 자식 광고비는 부모로 승격 후 groupRowMap에 적재

**Step 4 — 최종 결과**:
- `totalRevenue = totalProductRevenue` (배송비 제외)
- `roughProfit = totalRevenue - totalAdCost`
- `estimatedNetProfit = totalRevenue - totalAdCost - totalUnitCost - totalFeeCost - totalOtherCost`
- `profitStatus`: costSetting 없거나 fee incomplete이면 `INCOMPLETE_COST`
- `includeGroupChildren=true`이면 `childRows` 첨부

### 5.4 대시보드 요약 (`calculateDashboardSummary`)

`calculateDailyProfitRows`로 그룹+단독 rows 획득 → 단일 패스 합산.

**제외 항목**:
- 주문: UNMAPPED(`excludedUnmappedOrderRevenue`), CONFLICT(`excludedConflictOrderRevenue`), 비SALE(`excludedNonSaleOrderRevenue`)
- 광고: UNMAPPED, CONFLICT, INTENTIONALLY_UNMAPPED

### 5.5 초기 판매단위 자동생성 (`mapping-seed.service.ts`)

`POST /api/v1/mapping-seed/:storeId` 호출 시:
1. **사전 정리** (Step 0):
   - 해당 스토어의 `canonicalSalesUnits` 전체 삭제
   - 모든 주문 아이템의 `canonicalSalesUnitId`를 null로 초기화
   - **confirmedAt이 없는 시그니처**의 매핑만 초기화 (수동 확인된 시그니처 매핑은 보존)
2. 주문 아이템 분류:
   - **함께배송**: `rawOptionInfo.includes("[함께배송")` → `normalizeBundledCategory()`로 카테고리 정규화 후 그룹핑 → `linkedOptionCodes`/`linkedManageCodes` 사용
     - `normalizeBundledCategory`: 공백 제거, `FREE` 접미사 제거, 사이즈 접미사(`XS/S/M/L/XL/XXL/2XL/3XL`) 제거
     - 파싱 실패(bundledParseFailures) 시 일반 상품 그룹으로 폴백
   - **일반 상품**: `externalProductId` 단위로 그룹핑 → `linkedProductIds` 사용
3. **find-or-merge 로직**: `findExistingSalesUnit()`으로 같은 displayName 또는 linkedManageCodes 중복이 있으면 병합(MERGED), 없으면 신규 생성(CREATED)
4. matchAliases를 **빈 배열**로 생성 (ID 기반 매핑 전용, 텍스트 충돌 방지)
5. `recalculateOrderMappingsForStore + recalculateAdMappingsForStore` 호출

> 주의: 과거 문서에는 "단순 전체 초기화"로 표기되어 있었으나, 실제 구현은 confirmedAt 시그니처 보존 + 병합 로직을 포함한다.

### 5.6 오퍼레이션 큐 (`operation.service.ts`)

- 스토어별 Promise 체인으로 순차 실행 (`storeQueues: Map<storeId, Promise<void>>`)
- `enqueue()` → QUEUED → RUNNING → SUCCEEDED|FAILED
- 서버 재시작 시 QUEUED/RUNNING 오퍼레이션 → 자동 FAILED 처리
- Stale 감지: RUNNING 15분, QUEUED 5분 초과 시 FAILED

### 5.7 광고비 엑셀 업로드 흐름

1. `POST /ad-uploads/preview` — 엑셀 파싱, 요일 검증, 매핑 프리뷰 저장
2. 확인 후 `POST /ad-uploads/:id/confirm` — Operation 큐잉
3. `performConfirm`: `adUploadPreviewRows` → `adCampaignDailyCosts`로 확정. 같은 `uploadId`의 기존 `adCampaignDailyCosts` 행만 제거 후 신규 행 push. 같은 `reportDate`의 다른 업로드는 영향받지 않음 (현재 코드는 `replacedUploadId`를 설정하지 않으며, 같은 날짜에 여러 CONFIRMED + isActive=true 업로드가 동시에 존재 가능).
4. 유효 업로드 조건: `state=CONFIRMED + weekdayValidationStatus=PASSED + isActive=true`

> **참고**: `UploadState` 에 `REPLACED` 값은 정의되어 있으나 현재 코드에서 자동 부여되는 경로는 없음. `deleteUpload()` 시에는 `DELETED`로만 전환됨. REPLACED는 향후 확장 또는 수동 마이그레이션 용도로 예약된 상태값.

---

## 6. 텍스트 정규화 (`packages/shared/src/normalization.ts`)

```typescript
normalizeText(value)         // NFC, 소문자, 공백 정리
normalizeMatchAlias(value)   // normalizeText + 비문자/숫자 제거 (정규식 /[^\p{L}\p{N}]+/gu)
createSourceSignature(name, option)  // `normalizedName || normalizedOption`
```

**소스 시그니처**: 상품명+옵션으로 계산된 유일 키 (주문 매핑의 기본 단위).

---

## 7. 프론트엔드 구조 (`apps/frontend/`)

### 7.1 페이지 구조

```
app/(main)/              ← AppShell 레이아웃 (사이드바 + 헤더)
├── page.tsx             ← 대시보드 (오늘 날짜 기준 손익 요약)
├── orders/page.tsx      ← 주문 목록 + 시그니처 매핑
├── sales-units/page.tsx ← 판매단위 관리
├── mappings/page.tsx    ← 매핑 규칙 관리
├── ads/uploads/page.tsx ← 광고비 엑셀 업로드
├── costs/page.tsx       ← 원가 설정
├── profits/page.tsx     ← 손익 조회 (날짜 범위)
├── operations/page.tsx  ← 비동기 작업 조회
└── settings/stores/page.tsx ← 스토어/자격증명 설정

app/naver/callback/page.tsx  ← 네이버 커머스 JWT 콜백 진단 페이지 (AppShell 외부)
                                JWT payload 디코드(검증 전), accountUid / defaultChannelNo /
                                channelName / solutionId 표시. 테스트 URL:
                                http://localhost:3000/naver/callback
```

### 7.2 API 클라이언트 패턴 (`lib/api/`)

**`client.ts`**: `fetchApi({label, path, fallback?})` — 백엔드 실패 시 mock 데이터 반환 (DataSource: "live"|"mock"). 기본 베이스 URL은 `http://localhost:4000/api/v1`, 2500ms AbortSignal.timeout, `{ success, data }` 봉투 검증.

**`services.ts`**: 페이지별 데이터 fetch 함수 (`getDashboardPageData`, `getProfitsPageData` 등).

**`route-proxy.ts`**: Next.js API Routes가 백엔드로 프록시. `app/api/_utils/proxy.ts` 사용.

**`browser.ts`**: 브라우저(CSR) 컨텍스트에서 사용하는 경량 fetch 래퍼. 쿠키/CSRF 등 런타임 관리 영역.

**`upstream.ts`**: 서버 사이드(Route Handler/SSR)에서 백엔드 URL 결정 + 헤더 전파 담당. 내부 호출 공통 기반.

**`types.ts`**: 프론트엔드 내부 확장 타입 (페이지 props, DTO 가공 결과 등).

**Mock 데이터**: `lib/api/mock-data.ts` — 백엔드 미연결 시 fallback 사용.

`USE_AD_UPLOADS_MOCK_FALLBACK`, `USE_PROFITS_MOCK_FALLBACK` 상수로 특정 페이지 mock 비활성화 가능.

### 7.3 주요 fetch 헬퍼

```typescript
fetchAllRecordPages({label, path, query, fallback?})  // 페이지네이션 자동 처리 (pageSize=200)
```

---

## 8. 판매단위 그룹 시스템

- **단독 유닛**: `isGroup=false, parentSalesUnitId=null`
- **그룹 부모**: `isGroup=true, parentSalesUnitId=null` — linkedProductIds/linkedOptionCodes 불가
- **그룹 자식**: `isGroup=false, parentSalesUnitId=<groupId>` — 최대 1단계 계층만 허용
- **스토어 레벨**: `isStoreLevel=true, isGroup=false` — 스토어 전체 광고비 귀속용

**그룹 생성 시 광고 매핑 승계**: 자식 판매단위에 연결된 `CampaignSalesUnitMapping`이 그룹으로 이전됨.

**그룹 해체 시**: 자식들의 `parentSalesUnitId=null` 복구, 그룹 연결 광고비 INTENTIONALLY_UNMAPPED로 변경.

---

## 9. 중요 제약 및 규칙

### 매핑 재계산 트리거 (자동)
- 판매단위 생성/수정/활성화/비활성화 → `recalculateOrderMappingsForStore + recalculateAdMappingsForStore`
- 주문 동기화 후 → `recalculateOrderMappingsForStore`
- 캠페인 매핑 규칙 변경 → `recalculateAdMappingsForStore`

### confirmedAt 규칙
- 시그니처에 `confirmedAt` 있으면 자동 재계산 대상 **제외** (수동 매핑 보존)
- mapping-seed 실행 시에도 `confirmedAt` 있는 시그니처는 매핑이 보존됨. 초기화는 `confirmedAt`이 null인 시그니처에만 적용 (5.5 Step 0 참조). — 과거 문서에 "완전 초기화"로 표기되어 있었으나 실제 코드는 보존 동작.

### 광고비 유효성 조건
```
activeUploadIds = adExcelUploads
  .filter(state=CONFIRMED && weekdayValidationStatus=PASSED && isActive=true)
  .map(id)
```
손익 계산 시 `adCampaignDailyCosts.sourceUploadId`가 이 집합에 속해야만 반영.

### 비용 계산 공식
```
fee = paymentCommission + knowledgeShoppingSellingInterlockCommission  (null이면 feeRate 폴백)
estimatedNetProfit = revenue - adCost - (unitCost × qty) - fee - (otherCost × qty)
profitStatus = INCOMPLETE_COST  ← costSetting 없거나 fee incomplete
```

### 크로스 스토어 참조 금지
모든 리소스는 `storeId`로 격리. `ensureNoCrossStoreReference()` 검증.

### 함께배송 처리
- `rawOptionInfo.includes("[함께배송")` 감지
- 정규식 `\[함께배송[^\]]*\]\s*(.+?)[:：]\s*(.+)` 으로 카테고리+옵션값 파싱
- ID 기반 매핑: `linkedManageCodes(1순위) → linkedOptionCodes(2순위)`
- 함께배송 아이템은 `externalProductId` fallback 사용 금지 (메인 상품과 ID 공유하기 때문)

---

## 10. PostgreSQL 스키마 패턴

```sql
-- 각 테이블 공통 구조
CREATE TABLE {table_name} (
  id TEXT PRIMARY KEY,
  payload JSONB NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 메타데이터 테이블
CREATE TABLE storage_metadata (key TEXT PRIMARY KEY, value JSONB NOT NULL, updated_at TIMESTAMPTZ);
```

테이블 목록: `stores`, `commerce_api_credentials`, `products`, `canonical_sales_units`, `order_source_signatures`, `orders`, `order_items`, `campaign_sales_unit_mappings`, `ad_excel_uploads`, `ad_upload_preview_rows`, `ad_campaign_daily_costs`, `sales_unit_cost_settings`, `operations`, `audit_logs`

저장 방식: write 시마다 해당 테이블 **TRUNCATE → 전체 재삽입** (JSONB 직렬화). 트랜잭션으로 원자성 보장.

---

## 11. 환경변수

```env
# 저장소
DATABASE_URL=              # 있으면 PostgreSQL, 없으면 JSON 파일
DATA_DIR=                  # JSON 파일 경로 (기본: apps/backend/data/)

# 서버
PORT=4000                  # 백엔드 포트 (기본 4000)

# 보안 — CredentialService의 clientSecret 암·복호화 키
MASTER_KEY=                # 미설정 시 "development-master-key" 사용 (운영에서는 반드시 설정)

# 네이버 커머스
NAVER_SOLUTION_ID=
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
NAVER_ACCOUNT_UID=
NAVER_CHANNEL_NO=
NAVER_CALLBACK_URL=

# 부트스트랩 제어
NAVER_BOOTSTRAP_STORE=     # "true"일 때만 EnvironmentBootstrapService가 동작
NAVER_STORE_NAME=          # 부트스트랩으로 생성될 스토어 표시명
```

`NAVER_BOOTSTRAP_STORE=true` + NAVER_* 자격증명이 모두 있을 때만 `EnvironmentBootstrapService`가 스토어/자격증명을 자동 생성·갱신한다. `NaverCommerceConfigService.getBootstrapStorePayload()`가 이 플래그를 검사한다. 또한 `getEnvCredentialForStore(store)`는 env의 `sellerAccountId`+`channelNo`가 해당 스토어와 일치할 때만 env 자격증명을 반환한다.

---

## 12. 네이버 커머스 API 연동 (`naver-commerce.service.ts`)

실제 연동 시:
- `POST /v1/oauth2/token` — 토큰 발급 (bcrypt로 clientSecret 서명)
- `GET /v1/pay-order/seller/product-orders/last-changed-statuses` — 변경된 주문 조회
- `POST /v1/pay-order/seller/product-orders/query` — 주문 상세 조회
- `GET /v1/seller/account`, `GET /v1/seller/channels` — 자격증명 테스트용

자격증명 없으면 → mock 데이터 자동 생성 (4개 상품 템플릿 × 날짜 × 상태 조합).

### 12.1 자격증명 암호화 (`crypto.service.ts`)
- 알고리즘: **AES-256-GCM**
- 키 유도: `createHash("sha256").update(MASTER_KEY || "development-master-key").digest()`
- 저장 포맷: `iv(base64):authTag(base64):ciphertext(base64)`
- `CredentialService.upsert()`에서 clientSecret을 암호화하여 저장, `get()` / 토큰 발급 시 복호화.
- DB 엔티티(`CommerceCredential`)의 실제 필드명은 `clientSecretEncrypted` (저장된 암호화 문자열). 해석/응답 단계에서 `credentialSource: "DATABASE" | "ENV" | "NONE"` 가 런타임 값으로 합쳐짐 (엔티티에 저장되지 않음).

### 12.2 자격증명 해석 우선순위 (`naver-commerce-config.service.ts`)
1. DB에 암호화된 자격증명이 있으면 DATABASE 사용
2. env의 `sellerAccountId`/`channelNo`가 대상 스토어와 일치하면 ENV 사용 (`getEnvCredentialForStore`)
3. 둘 다 없으면 NONE → mock 경로

로그/응답에서는 `maskClientId()`로 clientId를 앞 8자 + `****` 형태로 마스킹.

---

## 13. 시그니처 표시명 보강 (`signature-enrichment.ts`)

주문 시그니처의 UI 표시명이 불완전할 때(빈 문자열, 2자 이하, 사이즈 토큰, optionInfo 누출 등) 사용자에게 보여줄 이름을 다단계로 보강한다.

### 13.1 `isMeaningfulName(value, contextOptionInfo?)`
다음 중 하나에 해당하면 "의미 없는 이름"으로 간주하여 다음 단계로 폴백한다:
- 빈 값 또는 공백뿐
- 길이 2자 이하
- 사이즈 패턴: `xs / s / m / l / xl / xxl / free / one / 원사이즈` 또는 숫자만으로 구성
- `contextOptionInfo`가 주어진 경우, value가 optionInfo 안의 옵션값으로만 누출된 문자열

### 13.2 `extractNameFromOptionInfo(optionInfo)`
rawOptionInfo 문자열에서 상품명을 정규식으로 추출한다. 두 패턴을 순차 시도:
1. `]<name>:` — 함께배송 등 대괄호 prefix 뒤의 이름
2. `^<name>:` — 맨 앞에 바로 이름이 오는 형태

### 13.3 `enrichSignatureDisplayName` 폴백 체인
다음 순서로 첫 번째 "의미 있는" 이름을 채택한다:
1. **snapshot**: `orderSourceSignature.displayName` 현재값
2. **orderItem**: 해당 시그니처에 속한 orderItem의 `productName`
3. **optionInfo**: `extractNameFromOptionInfo(rawOptionInfo)`
4. **product**: `products[externalProductId].name` (외부 상품 정보)
5. **commerceApi**: (stub) 네이버 커머스 API로 상품 정보 조회하여 fetch
6. 모두 실패 → `null`

`FallbackProductNameSource` 타입으로 어느 단계에서 해결되었는지 추적.

---

## 14. 손익 계산 / 대시보드 응답 세부 (`profit.service.ts`)

### 14.1 `getLatestActivityDate`
우선순위: `latestOverlapDate > latestOrderDate > latestAdDate`.
- overlap이란 주문과 광고가 모두 존재하는 최신 날짜. 대시보드 기본 조회일을 결정.

### 14.2 판매단위 상세 응답 (`GET /api/v1/profits/daily-sales-units/:salesUnitId`)
추가 필드:
- `deliveryFeeSummary`: `{ totalDeliveryFeeAmount, includedInProductRevenue: false, includedInEstimatedNetProfit: false }` — 배송비가 매출/순이익 계산에 제외됨을 명시
- `costBreakdown.aggregatedFeeCandidates` — paymentCommission/knowledgeShopping 후보 집계
- `costBreakdown.fallbackFeeCostPortion` — 수수료 null 폴백 사용분
- `excludedSummary.excludedOrderStatusCounts`: `{ CANCELED, CANCEL_REQUESTED, RETURNED, EXCHANGED }` — SALE 이외의 상태별 건수

### 14.3 대시보드 요약 제외 항목 (이전 5.4의 보강)
단일 패스 합산 시 다음을 별도 집계:
- 주문: `excludedUnmappedOrderRevenue`, `excludedConflictOrderRevenue`, `excludedNonSaleOrderRevenue`
- 광고: UNMAPPED / CONFLICT / INTENTIONALLY_UNMAPPED 금액별 집계

### 14.4 부가세(VAT) 및 배송비 신규 필드 (2026-04-21~2026-04-25 추가)

`packages/shared/src/types.ts` 의 `DashboardSummary` 와 `DailySalesUnitProfit` 에 다음 필드들이 추가됨:

**DashboardSummary 신규 필드**:
```typescript
totalVatAmount: number;                  // 부가세 합산
totalVatAdjustedRevenue: number;         // VAT 조정 후 매출
uniquePackageCount: number;              // 배송 건수 (중복 제외)
deliveryUnitCost: number;                // 배송 1건당 원가
estimatedDeliveryBaseCost: number;       // 추정 배송 원가 합
customerPaidDeliveryFee: number;         // 고객 부담 배송비
storeBorneDeliveryCost: number;          // 스토어 부담 배송 원가
```

**DailySalesUnitProfit 신규 필드**:
```typescript
vatAmount: number;
vatAdjustedRevenue: number;
isStoreLevel?: boolean;
isGroup?: boolean;
parentSalesUnitId?: string | null;
childRows?: DailySalesUnitProfit[];      // includeGroupChildren=true 일 때만
```

### 14.5 손익 상세 응답 신규 인터페이스

`GET /api/v1/profits/daily-sales-units/:salesUnitId` 응답에 사용되는 추가 인터페이스:

- `DailySalesUnitDetailRevenueBreakdown` — 매출 구성 분해 (상품/배송/VAT 분리)
- `DailySalesUnitDetailDeliveryContext` — 배송비 컨텍스트 (uniquePackageCount, customerPaid, storeBorne 분해)

> 주의: 배송비는 여전히 `estimatedNetProfit` 직접 계산에는 들어가지 않으나, **스토어가 부담한 배송 원가(`storeBorneDeliveryCost`)는 별도 표시 필드로 분리됨**. 배송비 회계 처리가 운영상 더 정밀해진 결과.

---

## 15. 현재 특이 사항 / 주의점

1. **mojibake 처리**: `repairMojibakeText()` — 네이버 DA 엑셀의 한글이 latin-1로 잘못 인코딩되는 경우 자동 복구.
2. **판매단위 soft-delete**: `isActive=false` + `deactivatedAt` 설정. hard-delete API 없음.
3. **광고비 mapping-seed**: 함께배송 카테고리 그룹핑 시 `normalizeBundledCategory()`로 "FREE", 사이즈 접미사 제거.
4. **광고 매핑 하드코딩 키워드**: `KEYWORD_MAPPING_RULES` 배열 — 사업 특화 키워드(한줄무릎보호대, 두줄무릎보호대, 런닝양말, 다이어트양말, 유쉴드, 카프슬리브, 카탈로그, 키워드타겟). `findKeywordMappingRule`은 3단계(specific → "무릎보호대"/"인피니티가드" standalone → broad store-level)로 탐색.
5. **손익 이중계산 방지**: `calculateDailyProfitRows`에서 `includeGroupChildren=false`이면 그룹 자식 행을 최상위 결과에서 제외. 대시보드 요약이 이 경로를 사용.
6. **WeakMap 시그니처 인덱스**: DB 참조가 `write()` 호출마다 교체되므로 `getSignatureIndex(database)` 의 WeakMap 캐시가 자동 무효화됨.
7. **Operation 큐**: 스토어당 1개의 Promise 체인. 서버 재시작 시 미완료 작업은 FAILED 처리. Stale 감지 RUNNING 15분/QUEUED 5분.
8. **배송비**: `totalDeliveryFeeAmount`는 손익 계산(`estimatedNetProfit`, `roughProfit`)에 **포함되지 않음** (참고용만). 응답의 `deliveryFeeSummary`에 명시적으로 플래그됨.
9. **자격증명 보호**: clientSecret은 DB에 평문으로 저장하지 않음 — 반드시 `CryptoService`로 AES-256-GCM 암호화. 로그에는 `maskClientId` 적용.
10. **매핑 불변식 (`assertGroupInvariants`)**: isGroup인 유닛은 linkedProductIds/linkedOptionCodes/linkedManageCodes를 가질 수 없고, parentSalesUnitId가 그룹을 가리킬 수 없으며, isStoreLevel은 isGroup이 될 수 없다.

---

## 16. 자주 수정되는 파일 목록

| 파일 | 수정 이유 |
|------|-----------|
| `apps/backend/src/helpers.ts` | 손익 계산 로직, 유틸 함수, WeakMap 인덱스 |
| `apps/backend/src/sales-unit-auto-mapper.ts` | 주문 매핑 알고리즘, ID 기반 매핑 맵 |
| `apps/backend/src/ad-mapping-engine.ts` | 광고 매핑 알고리즘, 키워드 규칙 |
| `apps/backend/src/mapping-seed.service.ts` | 초기 판매단위 생성 로직, 병합 규칙 |
| `apps/backend/src/profit.service.ts` | 손익 API 응답 형태 (deliveryFeeSummary, excludedSummary, VAT/배송 분해 등) |
| `apps/backend/src/signature-enrichment.ts` | 표시명 폴백 체인 |
| `apps/backend/src/run-tests.ts` | 로직 변경 시 회귀 테스트 추가 |
| `packages/shared/src/types.ts` | 데이터 모델 변경 |
| `apps/frontend/lib/api/services.ts` | 프론트 데이터 조합 로직 |

---

## 17. 최근 변경 이력 (2026-04-21 ~ 2026-04-30)

이 문서가 처음 작성된 2026-04-20 이후 다음 변화가 있었음. 다음 세션에서 이 영역을 건드릴 때 주의:

- **부가세(VAT) 도입** (~2026-04-25): `DashboardSummary`/`DailySalesUnitProfit` 에 `totalVatAmount`, `vatAmount`, `vatAdjustedRevenue` 등 신규 필드. 14.4 참조.
- **배송비 회계 정밀화** (~2026-04-23): `uniquePackageCount`, `deliveryUnitCost`, `estimatedDeliveryBaseCost`, `customerPaidDeliveryFee`, `storeBorneDeliveryCost` 분리. `DailySalesUnitDetailDeliveryContext` 인터페이스 신설.
- **손익 상세 응답 분해 추가**: `DailySalesUnitDetailRevenueBreakdown` 등 신규 인터페이스.
- **가구매(테스트 주문) 관련 feat** — 세부 사항 미확인. 필요 시 git log 또는 관련 서비스 직접 조회.

> 광고 업로드 흐름은 본 문서 작성 시점 기준 PREVIEW_PARSED → CONFIRMED 2단계가 그대로 유지됨. 단, **`0430수정계획.md`** 에 따라 단일 단계 자동 적용 흐름으로의 전환이 예정되어 있음 (작업 미진행). 작업 후에는 본 문서 5.7 / 4.3 / 7.2 영역 동시 갱신 필요.
