# 2026-04-16 수정 계획 3 — UI/UX 전문가 관점 종합 개선안

> 기존코드관찰.md 및 전체 프런트엔드 코드를 UI/UX 전문가의 관점에서 분석하여 도출한 개선 사항 전체 목록.
> 0416수정계획(오토매퍼 트리거 제거), 0416수정계획2(매핑/주문 페이지 UX)와 중복되지 않는 항목만 수록.
> 마지막 갱신: 2026-04-16.

---

## 목차

1. [접근성(Accessibility)](#1-접근성accessibility)
2. [로딩 상태 & 피드백](#2-로딩-상태--피드백)
3. [에러 처리 & 복구](#3-에러-처리--복구)
4. [폼 & 입력 UX](#4-폼--입력-ux)
5. [데이터 테이블 & 목록](#5-데이터-테이블--목록)
6. [네비게이션 & 정보 구조](#6-네비게이션--정보-구조)
7. [반응형 & 모바일](#7-반응형--모바일)
8. [모달 & 오버레이](#8-모달--오버레이)
9. [시각 디자인 & 일관성](#9-시각-디자인--일관성)
10. [성능 체감 개선](#10-성능-체감-개선)
11. [페이지별 개선](#11-페이지별-개선)
12. [우선순위 로드맵](#12-우선순위-로드맵)

---

## 1. 접근성(Accessibility)

### 1-1. Skip-to-Main 링크 누락

**현재 상태**: 루트 레이아웃(`app/layout.tsx`)과 `app-shell.tsx`에 skip navigation 링크가 없다. 키보드 사용자가 매번 사이드바 9개 링크를 탭으로 통과해야 본문에 도달한다.

**개선**:
- `app-shell.tsx`의 `<body>` 직후에 시각적으로 숨겨진(focus 시 노출) "본문으로 건너뛰기" 링크 추가.
- 메인 콘텐츠 영역에 `id="main-content"` 부여.

```tsx
<a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:z-50 focus:p-3 focus:bg-ink focus:text-white focus:rounded-lg">
  본문으로 건너뛰기
</a>
```

**영향 파일**: `components/layout/app-shell.tsx`

---

### 1-2. 모달 포커스 트랩 & 복원 부재

**현재 상태**: `mappings-view.tsx`, `sales-units-view.tsx`, `ad-uploads-view.tsx` 등의 커스텀 모달이 `div` 기반이다. 모달 열릴 때 포커스가 모달 내부로 이동하지 않고, Tab 키로 모달 밖 요소에 접근 가능하며, 닫힐 때 포커스가 트리거 버튼으로 복원되지 않는다.

**개선**:
- 공통 `Modal` 컴포넌트를 `components/shared/modal.tsx`에 생성.
- `useEffect`로 열릴 때 첫 번째 focusable 요소에 포커스 이동.
- `focusTrap` 로직 (Tab/Shift+Tab 순환).
- `Escape` 키로 닫기.
- 닫힐 때 트리거 요소로 포커스 복원 (`useRef`로 트리거 저장).
- `aria-modal="true"`, `role="dialog"`, `aria-labelledby` 속성 추가.
- 배경에 `aria-hidden="true"` (또는 `inert` 속성).

**영향 파일**: 신규 `components/shared/modal.tsx` + 모달 사용하는 모든 view 컴포넌트

---

### 1-3. 에러 메시지에 `role="alert"` 누락

**현재 상태**: 폼 에러 메시지가 일반 `<div>`로 표시된다. 스크린 리더가 에러 발생을 자동으로 감지하지 못한다.

**개선**:
- 에러 메시지 컨테이너에 `role="alert"` 또는 `aria-live="assertive"` 추가.
- 성공 메시지에는 `aria-live="polite"` 추가.

```tsx
{errorMessage && (
  <div role="alert" className="border border-red-300/40 bg-red-100/70 ...">
    {errorMessage}
  </div>
)}
```

**영향 파일**: 모든 view 컴포넌트의 에러/성공 메시지 영역 (profits, orders, mappings, sales-units, ads, costs, operations, stores — 8개 파일)

---

### 1-4. 상태 배지 색상 의존

**현재 상태**: `status-badge.tsx`의 6가지 톤(success, warning, danger 등)이 색상만으로 상태를 구분한다. 색각 이상 사용자에게 MAPPED/UNMAPPED/CONFLICT가 구분되지 않을 수 있다.

**개선**:
- 각 톤에 아이콘 또는 텍스트 패턴 보조 추가.
  - success: ✓ 체크 아이콘
  - warning: △ 경고 아이콘
  - danger: ✕ 엑스 아이콘
  - muted: — 대시 아이콘
- 선택적 `showIcon` prop으로 기존 UI 영향 최소화.

**영향 파일**: `components/shared/status-badge.tsx`

---

### 1-5. 폼 `<label>` 연결 불완전

**현재 상태**: 일부 뷰에서 `<input>`에 `id`가 없거나, `<label>`이 `htmlFor`를 사용하지 않고 텍스트만 옆에 배치한다. 특히 `costs-view.tsx`, `store-settings-view.tsx`의 입력 필드에서 발견.

**개선**:
- 모든 `<input>`, `<select>`, `<textarea>`에 고유 `id` 부여.
- 대응하는 `<label>`에 `htmlFor={id}` 연결.
- 인라인 텍스트만 있는 경우 `<label>` 태그로 래핑하거나 `aria-label` 추가.

**영향 파일**: `costs-view.tsx`, `store-settings-view.tsx`, `orders-view.tsx`, `profits-view.tsx`, `mappings-view.tsx`

---

## 2. 로딩 상태 & 피드백

### 2-1. 로딩 스켈레톤 부재

**현재 상태**: 페이지 SSR 로딩 중이거나 `router.refresh()` 실행 중 시각적 피드백이 없다. `useTransition`의 `isRefreshing`이 true인 동안 콘텐츠가 멈춘 채 보이거나, 버튼만 disabled 된다.

**개선**:
- `components/shared/skeleton.tsx` 공통 스켈레톤 컴포넌트 생성.
  - `SkeletonTable`: 테이블 형태 (rows × columns 파라미터).
  - `SkeletonStatCard`: 스탯 카드 형태.
  - `SkeletonText`: 텍스트 블록 형태.
- 각 view에서 `isRefreshing` 상태일 때 콘텐츠 위에 반투명 오버레이 + 스켈레톤 또는 opacity 전환.

```tsx
<div className={cn("transition-opacity duration-200", isRefreshing && "opacity-40 pointer-events-none")}>
  {/* 기존 콘텐츠 */}
</div>
```

**영향 파일**: 신규 `components/shared/skeleton.tsx` + 모든 view 컴포넌트

---

### 2-2. 버튼 로딩 인디케이터 미흡

**현재 상태**: 비동기 액션 버튼(주문 수집, 매핑 저장, 오토매핑 실행 등)이 `disabled` 속성만 걸리고 시각적 로딩 표시가 없다. 사용자가 버튼이 작동 중인지 알기 어렵다.

**개선**:
- 로딩 중인 버튼에 스피너 아이콘(lucide-react의 `Loader2`) + 텍스트 변경.
- 공통 `LoadingButton` 컴포넌트 또는 패턴 확립.

```tsx
<button disabled={isSubmitting} className="button-primary flex items-center gap-2">
  {isSubmitting && <Loader2 className="w-4 h-4 animate-spin" />}
  {isSubmitting ? "저장 중…" : "저장"}
</button>
```

**영향 파일**: 모든 view의 form submit 버튼

---

### 2-3. 토스트 알림 시스템 부재

**현재 상태**: 성공/에러 메시지가 각 폼 영역에 인라인으로 표시된다. 스크롤 위치에 따라 메시지가 보이지 않을 수 있다. 또한 `router.refresh()` 후 메시지가 사라진다.

**개선**:
- `components/shared/toast.tsx` 전역 토스트 시스템 구축.
- 화면 우상단 또는 하단에 고정 위치(`fixed`).
- 자동 사라짐 (3~5초) + 수동 닫기.
- 성공(sage/green), 에러(red), 경고(amber) 3종.
- React Context 기반 `useToast()` 훅으로 어디서든 호출 가능.
- 인라인 메시지는 폼 검증 에러용으로만 유지.

**영향 파일**: 신규 `components/shared/toast.tsx`, `components/shared/toast-provider.tsx` + `app/layout.tsx` (Provider 래핑) + 모든 view

---

### 2-4. 장시간 작업 진행률 표시 없음

**현재 상태**: 주문 수집(`order-sync`)은 수분이 걸릴 수 있는데 "수집 중…"이라는 텍스트만 보인다. operations 페이지에서 확인해야 진행 상태를 알 수 있다.

**개선**:
- 주문 수집 버튼 클릭 후 `operationId`를 받아 폴링 또는 간단한 프로그레스 바 표시.
- top-header.tsx의 "최근 수집" 영역에 진행 중 표시 (파란색 펄스 아이콘 + "수집 중" 텍스트).
- operation 완료 시 토스트로 결과 알림.

**영향 파일**: `components/layout/top-header.tsx`, `components/orders/orders-view.tsx`

---

## 3. 에러 처리 & 복구

### 3-1. 페이지 레벨 Error Boundary 부재

**현재 상태**: SSR 데이터 fetch 실패(`fetchApi` timeout 2.5s, fallback 없는 경우) 시 Next.js 기본 에러 페이지가 뜬다. 사용자에게 맥락 없는 에러 화면이 노출된다.

**개선**:
- `app/(main)/error.tsx` 커스텀 에러 바운더리 생성.
- 사용자 친화적 메시지: "데이터를 불러오지 못했습니다. 백엔드 서버가 실행 중인지 확인해 주세요."
- "다시 시도" 버튼 → `router.refresh()`.
- 각 도메인 폴더에도 `error.tsx` 옵션 제공 (세분화된 에러 메시지).

**영향 파일**: 신규 `app/(main)/error.tsx`, 선택적으로 각 도메인 폴더별 `error.tsx`

---

### 3-2. fetchApi timeout 2.5s가 너무 짧음

**현재 상태**: `client.ts`의 `AbortSignal.timeout(2500)`이 모든 API 호출에 적용된다. 손익 계산(`calculateDailyProfitRows`)이나 주문 목록 같은 무거운 쿼리도 2.5초 컷이다. 수정계획3-1에서 성능을 개선했지만 데이터 증가 시 다시 문제될 수 있다.

**개선**:
- `fetchApi`에 호출별 timeout 오버라이드 옵션 추가.
- 기본값 5초로 상향, 무거운 쿼리(profits, dashboard)는 10초.
- `services.ts`의 각 호출에 적절한 timeout 명시.

```ts
// client.ts
export async function fetchApi<T>({
  label, path, options, fallback, timeout = 5000,
}: FetchApiOptions<T>): Promise<...> {
  signal: AbortSignal.timeout(timeout),
  ...
}

// services.ts
fetchApi({ label: "profits/daily-sales-units", path: "...", timeout: 10000 })
```

**영향 파일**: `lib/api/client.ts`, `lib/api/services.ts`

---

### 3-3. API 실패 시 재시도(Retry) 메커니즘 없음

**현재 상태**: 네트워크 일시 오류로 API 호출이 실패하면 사용자가 페이지를 수동 새로고침해야 한다. 자동 재시도가 없다.

**개선**:
- `fetchApi`에 `retries` 옵션 추가 (기본 0, SSR 호출은 1).
- 지수 백오프(exponential backoff): 1초 → 2초.
- 네트워크 에러(TypeError)와 5xx 에러에만 재시도, 4xx는 즉시 실패.

**영향 파일**: `lib/api/client.ts`

---

### 3-4. 에러 메시지 자동 소멸 문제

**현재 상태**: 에러 메시지가 새 액션 시작 시 `setErrorMessage(null)`로 지워진다. 사용자가 에러를 읽기 전에 다른 동작을 하면 메시지가 사라질 수 있다.

**개선**:
- 에러 메시지에 수동 닫기(X) 버튼 추가.
- 폼 제출 시에만 이전 에러를 클리어 (다른 UI 인터랙션으로는 유지).
- 토스트 시스템(2-3) 도입 시 자연스럽게 해결.

**영향 파일**: 모든 view의 에러 메시지 영역

---

## 4. 폼 & 입력 UX

### 4-1. 필드 레벨 유효성 검증 메시지 없음

**현재 상태**: 모든 뷰에서 에러가 폼 상단에 단일 문자열로 표시된다. 어떤 필드가 문제인지 사용자가 직접 찾아야 한다. 특히 `costs-view.tsx`(unitCost, feeRate, otherCost 3개 필드)와 `store-settings-view.tsx`(clientId, clientSecret)에서 불편.

**개선**:
- 각 입력 필드 아래에 개별 에러 메시지 영역.
- 에러 있는 필드의 border를 빨간색으로 변경.
- 첫 번째 에러 필드로 자동 포커스.

```tsx
<div>
  <label htmlFor="unitCost">개당 원가</label>
  <input
    id="unitCost"
    className={cn("input-shell", errors.unitCost && "border-red-400")}
    ...
  />
  {errors.unitCost && (
    <p className="text-sm text-red-600 mt-1">{errors.unitCost}</p>
  )}
</div>
```

**영향 파일**: `costs-view.tsx`, `store-settings-view.tsx`, `sales-units-view.tsx`, `ad-uploads-view.tsx`

---

### 4-2. 숫자 입력 UX 개선

**현재 상태**: 원가(`unitCost`), 수수료율(`feeRate`), 기타비용(`otherCost`) 등 숫자 필드가 `type="text"`이다. 사용자가 직접 숫자를 타이핑해야 하고, 입력 중 포맷팅이 없다. 수수료율은 소수점으로 입력(0.035 = 3.5%)해야 하는데, 이 규칙이 placeholder에만 의존한다.

**개선**:
- 숫자 필드에 `inputMode="decimal"` 추가 (모바일 숫자 키패드 유도).
- 수수료율 필드: 퍼센트(%) 단위로 입력받고 내부적으로 소수 변환. "3.5" 입력 → 0.035 저장.
- 통화 필드: blur 시 천단위 포맷팅 표시 (focus 시 원래 숫자로 복원).
- 필드 옆에 단위 레이블 ("원", "%") 시각적 표시.

```tsx
<div className="relative">
  <input inputMode="decimal" ... />
  <span className="absolute right-3 top-1/2 -translate-y-1/2 text-ink/40 text-sm">%</span>
</div>
```

**영향 파일**: `costs-view.tsx`, `ad-uploads-view.tsx`

---

### 4-3. 날짜 입력 커스텀 스타일링 필요

**현재 상태**: `<input type="date">`가 브라우저 기본 스타일을 사용한다. 앱의 glass-morphism 디자인 언어와 시각적 불일치. 특히 profits-view, orders-view에서 눈에 띈다.

**개선**:
- `input[type="date"]`에 대한 커스텀 스타일을 `globals.css`에 추가.
- `::-webkit-calendar-picker-indicator` 스타일링으로 아이콘 커스터마이징.
- 또는 간단한 커스텀 DatePicker 컴포넌트 래퍼 (lucide Calendar 아이콘 + native input).

```css
input[type="date"] {
  color-scheme: light;
  appearance: none;
  /* glass-panel과 조화되는 스타일링 */
}
```

**영향 파일**: `globals.css` 또는 신규 `components/shared/date-input.tsx`

---

### 4-4. 폼 리셋 기능 없음

**현재 상태**: `sales-units-view.tsx`, `costs-view.tsx`, `store-settings-view.tsx`의 생성/수정 폼에 "초기화" 또는 "취소" 버튼이 없다. 사용자가 입력 중 실수하면 필드를 하나씩 지워야 한다.

**개선**:
- 모든 CRUD 폼에 "취소" / "초기화" 버튼 추가 (`button-ghost` 스타일).
- 수정 모드일 때는 원래 값으로 복원, 생성 모드일 때는 빈 상태로 리셋.
- 수정 중인 데이터가 있으면 페이지 이탈 시 확인 (선택적 `beforeunload`).

**영향 파일**: `sales-units-view.tsx`, `costs-view.tsx`, `store-settings-view.tsx`

---

### 4-5. 확인 다이얼로그 불일치

**현재 상태**: 삭제/비활성화 등 위험 작업 시 일부는 `window.confirm()`을 사용하고 일부는 확인 없이 즉시 실행된다. UX 일관성이 없다.

**개선**:
- 모든 위험 액션(삭제, 비활성화, 해산 등)에 커스텀 확인 다이얼로그 사용.
- `Modal` 컴포넌트(1-2)를 활용한 `ConfirmDialog` 변형.
- 위험도에 따른 시각 구분: danger(빨간 버튼), warning(노란 배경).

**영향 파일**: 신규 `components/shared/confirm-dialog.tsx` + `sales-units-view.tsx`, `ad-uploads-view.tsx`, `costs-view.tsx`

---

## 5. 데이터 테이블 & 목록

### 5-1. DataTable 정렬 기능 없음

**현재 상태**: `data-table.tsx`가 정적 렌더링만 한다. 컬럼 헤더를 클릭해서 정렬할 수 없다. 주문 목록(결제금액순), 손익(수익순), 시그니처(주문건수순) 등에서 정렬 니즈가 크다.

**개선**:
- `DataTable` 컬럼 정의에 `sortable?: boolean` 옵션 추가.
- 정렬 가능 컬럼 헤더에 화살표 아이콘 (↑↓) + 클릭 핸들러.
- 정렬 상태: `{ key: string, direction: "asc" | "desc" } | null`.
- 클라이언트 사이드 정렬 (데이터가 이미 전체 로드되어 있으므로).

```tsx
interface Column<T> {
  key: string;
  title: string;
  sortable?: boolean;
  sortValue?: (row: T) => string | number;  // 정렬 기준값 추출
  render?: (row: T) => ReactNode;
}
```

**영향 파일**: `components/shared/data-table.tsx` + 정렬 필요한 모든 view

---

### 5-2. 페이지네이션 미구현

**현재 상태**: 모든 테이블이 전체 데이터를 한 번에 렌더링한다. 주문 아이템이 수천 건이 되면 렌더 성능이 저하되고, 사용자가 스크롤로 원하는 항목을 찾기 어렵다.

**개선**:
- `components/shared/pagination.tsx` 공통 컴포넌트 구축.
- 기본 페이지 크기: 50건 (20 / 50 / 100 선택 가능).
- 총 건수 + 현재 범위 표시: "1–50 / 전체 1,234건".
- 이전/다음 + 페이지 번호 네비게이션.
- 우선 적용: `orders-view.tsx`, `mappings-view.tsx`의 시그니처 테이블.

**영향 파일**: 신규 `components/shared/pagination.tsx` + `orders-view.tsx`, `mappings-view.tsx`

---

### 5-3. 테이블 행 선택/상세보기 패턴 불일치

**현재 상태**:
- `profits-view.tsx`: 행 클릭 → 우측 패널에 상세.
- `operations-view.tsx`: 행 클릭 → 하단에 상세.
- `orders-view.tsx`: 행 클릭 기능 없음.
- `costs-view.tsx`: 행 클릭 → 상단 폼에 편집 데이터 채움.

패턴이 페이지마다 달라 사용자 학습 비용이 높다.

**개선**:
- **마스터-디테일 패턴 통일**: 데스크탑에서 목록 + 우측 패널, 모바일에서 목록 → 상세 전체 화면.
- 또는 **행 확장 패턴**: 행 클릭 시 아래로 상세 영역 펼침 (accordion).
- 모든 테이블의 상세보기가 동일한 인터랙션 모델을 따르도록 통일.

**영향 파일**: 장기 과제 — 모든 view 컴포넌트 리팩토링

---

### 5-4. 빈 상태(Empty State) 액션 연결 미흡

**현재 상태**: `EmptyState` 컴포넌트가 "데이터가 없습니다" 류의 메시지만 보여주고, 다음 행동을 안내하지 않는 경우가 많다. 예: 판매단위가 없을 때 "판매단위를 먼저 등록하세요" + 등록 폼으로 스크롤/링크 안내가 부족하다.

**개선**:
- 빈 상태별 맥락적 CTA 추가:
  - 스토어 없음 → "스토어 등록" 버튼
  - 주문 없음 → "주문 수집 시작" 버튼
  - 매핑 없음 → "매핑 페이지로 이동" 링크
  - 원가 없음 → "원가 설정" 폼으로 스크롤
- 빈 상태에 상황별 일러스트 또는 아이콘 추가 (lucide-react).

**영향 파일**: 각 view의 EmptyState 사용 부분

---

### 5-5. 테이블 셀 텍스트 오버플로 처리 미비

**현재 상태**: 상품명, 옵션정보 등 긴 텍스트가 테이블 셀에서 줄바꿈 없이 넘친다. `table-scroll` 클래스로 가로 스크롤은 가능하지만, 셀 내 텍스트가 잘리는 기준이 없다.

**개선**:
- 긴 텍스트 컬럼에 `max-w-[200px] truncate` + 호버 시 풀 텍스트 툴팁.
- 또는 `line-clamp-2`로 2줄까지 표시 + "더 보기" 확장.
- 상품명 컬럼의 `min-w` / `max-w` 명시.

```tsx
<td className="max-w-[200px] truncate" title={fullText}>
  {fullText}
</td>
```

**영향 파일**: `data-table.tsx`에 truncate 옵션 추가, `orders-view.tsx`, `mappings-view.tsx`

---

## 6. 네비게이션 & 정보 구조

### 6-1. 현재 위치 시각적 피드백 강화

**현재 상태**: `sidebar-nav.tsx`의 활성 링크가 흰 배경 + 그림자로 표시된다. 상대적으로 미묘해서 빠르게 현재 위치를 파악하기 어려울 수 있다.

**개선**:
- 활성 링크 좌측에 4px 두께의 accent 색상 바(indicator) 추가.
- 아이콘 색상도 활성 시 accent 컬러로 변경.
- 비활성 링크와의 대비 강화.

```tsx
<Link className={cn(
  "flex items-center gap-3 px-4 py-2.5 rounded-xl transition-colors relative",
  isActive
    ? "bg-white/100 text-ink shadow-sm before:absolute before:left-0 before:top-2 before:bottom-2 before:w-1 before:bg-coral before:rounded-r"
    : "text-white/60 hover:text-white/90 hover:bg-white/8"
)}>
```

**영향 파일**: `components/layout/sidebar-nav.tsx`

---

### 6-2. 모바일 사이드바 토글 없음

**현재 상태**: `sidebar-nav.tsx`가 `lg:` 이하에서 `hidden`이다. 모바일에서 네비게이션 접근 방법이 없다. URL 직접 입력 외에는 페이지 이동이 불가능하다.

**개선**:
- 모바일 상단에 햄버거 메뉴 버튼 추가.
- 클릭 시 사이드바가 오버레이로 슬라이드인.
- 배경 딤 처리 + 바깥 클릭/스와이프로 닫기.
- `app-shell.tsx`에 모바일 네비 상태 관리 추가.

```tsx
// app-shell.tsx
const [isMobileNavOpen, setIsMobileNavOpen] = useState(false);

// 모바일 전용 햄버거 버튼 (lg:hidden)
<button onClick={() => setIsMobileNavOpen(true)} className="lg:hidden">
  <Menu className="w-6 h-6" />
</button>

// 오버레이 사이드바
{isMobileNavOpen && (
  <div className="fixed inset-0 z-40 lg:hidden">
    <div className="absolute inset-0 bg-black/50" onClick={close} />
    <SidebarNav onLinkClick={close} />
  </div>
)}
```

**영향 파일**: `components/layout/app-shell.tsx`, `components/layout/sidebar-nav.tsx`

---

### 6-3. 브레드크럼 부재

**현재 상태**: `top-header.tsx`의 `resolveMeta()`가 각 라우트의 제목/설명을 제공하지만, 사용자에게 페이지 계층 구조를 보여주는 브레드크럼이 없다. 특히 `settings/stores`처럼 2단계 경로에서 상위 맥락이 보이지 않는다.

**개선**:
- `top-header.tsx`에 경량 브레드크럼 추가.
- 형식: "대시보드 > 원가 설정" (현재 페이지는 텍스트, 상위는 링크).
- `resolveMeta`의 계층 정보 활용.

**영향 파일**: `components/layout/top-header.tsx`

---

### 6-4. 페이지 간 맥락 전환 안내 부족

**현재 상태**: 매핑 페이지에서 "이 시그니처에 대응하는 주문을 보려면 주문 페이지로" 같은 크로스링크가 없다. 사용자가 도메인 간 관계를 파악하기 어렵다. (0415수정계획3에서 딥링크 자리만 잡고 미구현 상태)

**개선**:
- 시그니처 상세에서 "이 시그니처의 주문 보기" → 주문 페이지 필터 적용 링크.
- 판매단위 상세에서 "이 판매단위의 손익 보기" → 손익 페이지 링크.
- 광고 캠페인에서 "매핑된 판매단위 보기" → 판매단위 페이지 링크.
- 손익 페이지에서 "미매핑 항목 매핑하기" → 매핑 페이지 링크.

**영향 파일**: `mappings-view.tsx`, `sales-units-view.tsx`, `profits-view.tsx`, `orders-view.tsx`

---

## 7. 반응형 & 모바일

### 7-1. 테이블 모바일 대체 뷰 없음

**현재 상태**: 모든 테이블이 가로 스크롤(`table-scroll`)로 모바일을 대응한다. 10개 이상 컬럼 테이블(주문, 손익)에서는 가로 스크롤이 과도하여 데이터 파악이 어렵다.

**개선**:
- 모바일에서 테이블 대신 **카드 목록** 뷰 전환.
- `DataTable`에 `mobileRender` prop 또는 `useMediaQuery` 기반 분기.
- 카드 뷰: 주요 정보(상품명, 금액, 상태)를 수직 배치.

```tsx
// DataTable 내부
const isMobile = useMediaQuery("(max-width: 768px)");
return isMobile ? <CardListView ... /> : <TableView ... />;
```

**영향 파일**: `components/shared/data-table.tsx` (장기 과제)

---

### 7-2. 터치 타겟 크기 부족

**현재 상태**: 일부 체크박스, 작은 버튼(삭제 아이콘 등)의 터치 영역이 44px × 44px (WCAG 권장) 미만이다. 모바일에서 정확한 탭이 어려울 수 있다.

**개선**:
- 모든 인터랙티브 요소의 최소 터치 영역 44px 보장.
- 작은 아이콘 버튼에 투명 패딩 추가.
- `min-h-[44px] min-w-[44px]` 유틸 클래스 또는 공통 래퍼.

**영향 파일**: `mappings-view.tsx`(체크박스), `ad-uploads-view.tsx`(삭제 버튼), `costs-view.tsx`(액션 버튼)

---

### 7-3. top-header 3칸 정보 그리드 모바일 레이아웃

**현재 상태**: `top-header.tsx`의 기준일/스토어/최근수집 3칸 그리드가 모바일에서 `flex flex-wrap`으로 처리되지만 각 항목이 작은 화면에서 잘리거나 줄바꿈이 어색할 수 있다.

**개선**:
- 모바일에서 3칸 → 1칸 세로 배치.
- 또는 가장 중요한 정보(기준일)만 표시, 나머지는 접기/펼치기.
- 디바이더(구분선) 모바일에서 제거.

**영향 파일**: `components/layout/top-header.tsx`

---

## 8. 모달 & 오버레이

### 8-1. 모달 닫기 인터랙션 불완전

**현재 상태**: 커스텀 모달에 배경 클릭으로 닫기, Escape 키로 닫기가 없다. X 버튼만으로 닫아야 한다.

**개선**: 1-2(모달 포커스 트랩) 항목과 함께 해결. 공통 Modal 컴포넌트에 포함:
- 배경(backdrop) 클릭 → 닫기.
- Escape 키 → 닫기.
- 열기/닫기 트랜지션 애니메이션 (fade + scale).

**영향 파일**: 신규 `components/shared/modal.tsx`

---

### 8-2. 모달 크기 & 위치 일관성 없음

**현재 상태**: `mappings-view.tsx`의 그룹 생성 모달, `sales-units-view.tsx`의 하위 관리 모달, `ad-uploads-view.tsx`의 프리뷰 모달이 각각 다른 크기와 패딩을 가진다.

**개선**:
- Modal 컴포넌트에 사이즈 variant: `sm`(400px), `md`(560px), `lg`(720px), `xl`(900px).
- 일관된 padding(p-6), 헤더(제목 + 닫기), 바디(스크롤), 푸터(버튼) 영역.
- `max-h-[85vh]` + body 내부 스크롤.

**영향 파일**: 신규 `components/shared/modal.tsx`

---

## 9. 시각 디자인 & 일관성

### 9-1. 한국어/영어 혼용

**현재 상태**: 대시보드(`dashboard-view.tsx`)는 영어("Product Revenue", "Excluded Totals"), 다른 페이지는 한국어("매핑 상태", "결제 금액")이다. top-header의 일부 텍스트도 영어("Last Order Sync")와 한국어가 섞여 있다. 사용자 언어 경험이 일관되지 않다.

**개선**:
- 전체 UI를 한국어로 통일 (대시보드 포함).
- 또는 i18n 라이브러리(next-intl 등) 도입하여 언어 전환 지원.
- 최소한 사용자 대면 텍스트(라벨, 제목, 설명, 버튼)는 한 언어로 통일.
- 기술적 용어(API 에러 메시지 등)는 영어 허용.

**영향 파일**: `dashboard-view.tsx`, `top-header.tsx`, `source-banner.tsx` 등

---

### 9-2. 버튼 스타일 변형 불일치

**현재 상태**: `globals.css`에 `.button-primary`, `.button-secondary`, `.button-ghost` 3종이 정의되어 있지만, view 컴포넌트에서 인라인 Tailwind로 직접 스타일링하는 버튼도 있다. 동일 역할의 버튼이 다른 모양을 가진다.

**개선**:
- 모든 버튼을 CSS 변수 기반 `.button-*` 클래스로 통일.
- 추가 변형 필요 시: `.button-danger` (빨간, 삭제/비활성화용), `.button-link` (텍스트만, 밑줄).
- 사이즈 변형: `.button-sm`, `.button-lg`.
- view 컴포넌트의 인라인 버튼 스타일 → 공통 클래스로 교체.

**영향 파일**: `globals.css`, 모든 view 컴포넌트

---

### 9-3. 간격/패딩 불일치

**현재 상태**: Panel, StatCard, 섹션 간 간격이 `gap-4`, `gap-5`, `gap-6`, `mb-4`, `mb-5`, `mb-6` 등 혼재한다. 시각적 리듬이 일관되지 않다.

**개선**:
- 간격 시스템 정의: 섹션 간 `gap-6`(24px), 섹션 내 `gap-4`(16px), 요소 내 `gap-2`(8px).
- 공통 래퍼 컴포넌트 또는 Tailwind 플러그인으로 강제.
- 기존 코드에서 불일치하는 간격을 일괄 정리.

**영향 파일**: 모든 view 컴포넌트 (점진적 정리)

---

### 9-4. glass-panel 남용

**현재 상태**: 거의 모든 컨테이너가 `glass-panel` 클래스를 사용한다. 패널 안에 패널이 중첩되는 경우(예: Panel 안의 DataTable)에서 시각적 계층이 불명확해진다.

**개선**:
- 시각적 계층 가이드라인 수립:
  - Level 0: 페이지 배경 (그래디언트).
  - Level 1: 메인 패널 (`glass-panel`).
  - Level 2: 패널 내부 섹션 (배경 없음 또는 미세한 구분선).
  - Level 3: 인라인 강조 (연한 배경색).
- 중첩 glass-panel 제거, 구분선(`border-b`) 또는 간격으로 대체.

**영향 파일**: `profits-view.tsx`, `dashboard-view.tsx`, `mappings-view.tsx` 등

---

### 9-5. 인터랙션 트랜지션/애니메이션 부재

**현재 상태**: 상태 변경(필터 적용, 탭 전환, 패널 열기/닫기)에 트랜지션이 없다. 콘텐츠가 즉각적으로 나타나거나 사라져 사용자가 변경을 인지하기 어려울 수 있다.

**개선**:
- 리스트 항목 등장: `animate-fadeIn` (opacity 0→1, 150ms).
- 패널 열기/닫기: `transition-all duration-200`.
- 필터 적용 결과: 콘텐츠 교체 시 미세한 fade (opacity 전환).
- `globals.css`에 공통 키프레임 정의.

```css
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(4px); }
  to { opacity: 1; transform: translateY(0); }
}
.animate-fadeIn { animation: fadeIn 150ms ease-out; }
```

**영향 파일**: `globals.css`, 주요 상태 전환이 있는 view 컴포넌트

---

## 10. 성능 체감 개선

### 10-1. Optimistic UI 적용

**현재 상태**: 매핑 저장, 판매단위 수정 등 CUD 작업 시 API 응답을 기다린 후 `router.refresh()`로 전체 데이터를 다시 불러온다. 사용자가 1~3초간 아무 변화 없이 대기한다.

**개선**:
- 매핑 저장: 클릭 즉시 UI에서 MAPPED 상태로 전환 → 백그라운드에서 API 호출 → 실패 시 롤백.
- 판매단위 수정: 즉시 반영 → API 확인 → 실패 시 롤백 + 에러 토스트.
- React 19의 `useOptimistic` 훅 활용 가능.

**영향 파일**: `mappings-view.tsx`, `sales-units-view.tsx`, `costs-view.tsx` (고급 과제)

---

### 10-2. 무거운 컴포넌트 React.memo/useMemo 적용

**현재 상태**: 300~500줄 view 컴포넌트 내에서 `useMemo`, `useCallback`이 거의 사용되지 않는다. 필터 변경이나 선택 변경 시 전체 컴포넌트가 리렌더된다.

**개선**:
- 데이터 가공 로직(필터링, 정렬, 통계 계산)에 `useMemo` 적용.
- 이벤트 핸들러에 `useCallback` 적용 (자식 컴포넌트 리렌더 방지).
- 테이블 행 컴포넌트를 `React.memo`로 래핑.

```tsx
const filteredOrders = useMemo(
  () => data.orderItems.filter(item => matchesFilters(item, filters)),
  [data.orderItems, filters]
);
```

**영향 파일**: 모든 view 컴포넌트 (점진적 최적화)

---

### 10-3. services.ts의 Promise.all 실패 전파

**현재 상태**: `services.ts`의 페이지별 데이터 fetch가 `Promise.all`로 병렬 호출한다. 하나라도 실패하면 전체가 실패한다. 부분적으로 사용 가능한 데이터도 보여주지 못한다.

**개선**:
- `Promise.all` → `Promise.allSettled`로 변경.
- 성공한 데이터는 렌더링, 실패한 부분은 해당 섹션에만 에러 표시.
- 페이지 레벨에서 어떤 데이터가 실패했는지 `SourceBanner`에 표시.

```ts
const results = await Promise.allSettled([
  fetchApi({ label: "profits/daily", ... }),
  fetchApi({ label: "dashboard/summary", ... }),
]);
return {
  dailyProfits: results[0].status === "fulfilled" ? results[0].value : null,
  summary: results[1].status === "fulfilled" ? results[1].value : null,
  errors: results.filter(r => r.status === "rejected").map(r => r.reason),
};
```

**영향 파일**: `lib/api/services.ts`, 각 page.tsx

---

## 11. 페이지별 개선

### 11-1. 대시보드 (dashboard-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 전체 영문 텍스트 → 한국어 통일 필요 | 모든 라벨/제목 한국어로 변경 |
| b | 스탯 카드 5개가 한 줄 → 모바일에서 좁음 | `grid-cols-2 md:grid-cols-3 xl:grid-cols-5` |
| c | "Excluded Totals" 8칸 그리드가 맥락 설명 없이 숫자만 나열 | 각 항목에 툴팁 또는 작은 설명 텍스트 추가 |
| d | 최근 작업 패널이 5건만 표시, "더 보기" 없음 | "전체 보기 →" 링크로 operations 페이지 연결 |
| e | 날짜 기준 선택 불가 — 항상 최신 날짜 | 날짜 셀렉터 추가하여 과거 날짜 대시보드 조회 |

**영향 파일**: `components/dashboard/dashboard-view.tsx`

---

### 11-2. 손익 (profits-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 날짜 입력 후 "적용" 버튼을 눌러야 반영 | 날짜 변경 시 즉시 적용 (0416수정계획2의 주문 패턴과 통일) |
| b | 상세 패널(우측)이 선택 없으면 빈 공간 | 빈 상태 안내: "좌측 목록에서 판매단위를 선택하세요" + 아이콘 |
| c | 그룹 행 펼치기/접기 화살표가 작음 | 시각적 크기 확대 + 터치 영역 확보 |
| d | 미매핑 금액이 별도 섹션인데 눈에 잘 안 띔 | warning 톤 배경 + "매핑 페이지에서 처리하기" 링크 |
| e | 수익률(%) 시각화 없음 | 간단한 프로그레스 바 또는 색상 그래디언트로 수익률 직관적 표시 |

**영향 파일**: `components/profits/profits-view.tsx`

---

### 11-3. 주문 (orders-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 필터 5개 이상 → "필터 초기화" 버튼 필요 | 필터 영역 우측 상단에 "초기화" 텍스트 버튼 |
| b | 주문 수집 버튼 2개(최근 30일, 선택 날짜) 구분 불명확 | 버튼 라벨 개선 + 아이콘 구분 + 설명 툴팁 |
| c | 시그니처 테이블에 fallbackProductName 표시가 복잡 | 보조 라벨을 원본 이름과 시각적으로 구분 (작은 글씨, 연한 색) |
| d | 테이블 컬럼 순서 — 결제금액이 오른쪽 끝에 위치 | 결제금액을 더 눈에 띄는 위치(좌측 3~4번째)로 이동 |

**영향 파일**: `components/orders/orders-view.tsx`

---

### 11-4. 매핑 (mappings-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 3개 박스(주문 시그니처, 캠페인, 광고비)가 한 페이지에 → 긴 스크롤 | 탭 또는 아코디언으로 영역 분리, 동시에 2개 이상 볼 필요 시 탭 유지 |
| b | 드래그 선택(pointer 기반)이 모바일에서 동작 불명확 | 모바일에서 개별 체크박스 탭으로 전환 |
| c | 배치 매핑 결과 피드백 — "N건 매핑 완료" 메시지가 스크롤 상단 | 토스트 알림(2-3)으로 어디서든 확인 가능하게 |
| d | 선택된 시그니처 수 표시가 섹션 제목 옆에만 → 눈에 안 띔 | 하단 고정 액션 바(floating action bar)에 선택 수 + 매핑 버튼 배치 |

**영향 파일**: `components/mappings/mappings-view.tsx`

---

### 11-5. 판매단위 (sales-units-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 그룹/하위 계층 구조가 테이블에서 들여쓰기 없이 표시 | 하위 항목에 좌측 패딩 또는 들여쓰기 라인으로 계층 시각화 |
| b | matchAliases 입력이 한 줄 텍스트 → 다수 별칭 입력 불편 | `textarea` 또는 태그 입력 UI (칩/태그)로 변경 |
| c | 비활성 판매단위가 활성 항목과 같은 시각 비중 | 비활성 항목 opacity 낮추기 + 접기 옵션 |
| d | 원가 설정 완료 여부 확인이 번거로움 | 각 판매단위 행에 원가 설정 상태 인디케이터 (아이콘/배지) |

**영향 파일**: `components/sales-units/sales-units-view.tsx`

---

### 11-6. 광고비 (ad-uploads-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 파일 업로드 영역이 단순 file input → 드래그앤드롭 미지원 | 드래그앤드롭 영역 + 파일 형식 안내 + 아이콘 |
| b | 업로드 후 프리뷰 → 확정 2단계가 명확하지 않음 | 스텝 인디케이터(Step 1: 업로드, Step 2: 검토, Step 3: 확정) 추가 |
| c | weekdayValidation 에러 표시가 텍스트만 | 달력 시각화로 어떤 요일이 누락/불일치인지 표시 |
| d | 삭제된 업로드가 목록에 남아 혼란 | 삭제됨 상태를 별도 섹션으로 분리하거나 기본 숨김 |

**영향 파일**: `components/ads/ad-uploads-view.tsx`

---

### 11-7. 원가 (costs-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 판매단위 선택 → 원가 입력이 한 폼 → 판매단위별 빠른 전환 불편 | 좌측에 판매단위 리스트, 우측에 원가 폼으로 분리된 마스터-디테일 레이아웃 |
| b | 유효기간(effectiveFrom/To) 입력이 텍스트 → 날짜 겹침 검증 없음 | 기존 원가 기간과 겹침 시 시각적 경고 표시 |
| c | 수수료율 0.035 형식이 직관적이지 않음 | 4-2(숫자 입력 UX) 참고 — 퍼센트 입력 UI |
| d | 원가 이력이 시간순 나열 → 현재 활성 항목 구분 어려움 | 활성 원가를 상단에 하이라이트 표시, 이력은 아래로 분리 |

**영향 파일**: `components/costs/costs-view.tsx`

---

### 11-8. 설정 (store-settings-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 초기 설정(스토어 생성)과 이후 관리가 같은 폼 → 맥락 혼란 | 스토어 없을 때: 온보딩 위저드 (단계별 안내). 있을 때: 관리 UI |
| b | API 키(clientId/clientSecret) 입력 시 마스킹 없음 | `type="password"` + "보기" 토글 버튼 |
| c | 키 테스트 결과가 성공/실패만 → 상세 없음 | 성공 시 연결된 스토어명 표시, 실패 시 구체적 원인 안내 |
| d | 멀티 스토어 전환이 번거로움 | primary 스토어 전환 시 전체 앱의 데이터가 바뀐다는 안내 |

**영향 파일**: `components/stores/store-settings-view.tsx`

---

### 11-9. 작업 큐 (operations-view.tsx)

| # | 이슈 | 개선 |
|---|---|---|
| a | 작업 상태 자동 갱신 없음 → 수동 새로고침 필요 | 진행 중 작업이 있으면 10초 간격 자동 폴링 |
| b | 실패 작업의 에러 메시지가 기술적 → 사용자 이해 어려움 | 에러 코드별 사용자 친화적 한국어 메시지 매핑 |
| c | 재시도 버튼만 있고 "건너뛰기"/"무시" 옵션 없음 | 실패 작업을 닫기/무시하는 옵션 추가 |

**영향 파일**: `components/operations/operations-view.tsx`

---

## 12. 우선순위 로드맵

### Phase 1 — 즉시 적용 가능 (난이도 낮음, 효과 높음)

| # | 항목 | 참조 |
|---|---|---|
| 1 | 에러 메시지에 `role="alert"` 추가 | §1-3 |
| 2 | 버튼 로딩 스피너 (Loader2 아이콘) | §2-2 |
| 3 | 한국어/영어 혼용 통일 (대시보드 한국어화) | §9-1 |
| 4 | fetchApi timeout 5초 상향 + 호출별 오버라이드 | §3-2 |
| 5 | 버튼 스타일 `.button-danger` 추가 + 인라인 정리 | §9-2 |
| 6 | 폼 `<label>` htmlFor 연결 | §1-5 |
| 7 | 테이블 셀 truncate + title 툴팁 | §5-5 |
| 8 | 리프레시 중 opacity 전환 피드백 | §2-1 (간이 버전) |
| 9 | 대시보드 "작업 더 보기" 링크 | §11-1d |
| 10 | 활성 네비 링크 인디케이터 강화 | §6-1 |

### Phase 2 — 1~2주 내 (중간 난이도)

| # | 항목 | 참조 |
|---|---|---|
| 11 | 공통 Modal 컴포넌트 (포커스 트랩, Escape, 배경 닫기) | §1-2, §8-1, §8-2 |
| 12 | 토스트 알림 시스템 | §2-3 |
| 13 | DataTable 정렬 기능 | §5-1 |
| 14 | 페이지네이션 컴포넌트 (주문, 시그니처) | §5-2 |
| 15 | Skip-to-Main 링크 | §1-1 |
| 16 | 모바일 사이드바 햄버거 메뉴 | §6-2 |
| 17 | 필드 레벨 유효성 검증 메시지 | §4-1 |
| 18 | 확인 다이얼로그 통일 | §4-5 |
| 19 | 숫자 입력 UX (단위 레이블, 수수료 % 입력) | §4-2 |
| 20 | Promise.allSettled 부분 실패 대응 | §10-3 |

### Phase 3 — 중장기 (높은 난이도, 큰 효과)

| # | 항목 | 참조 |
|---|---|---|
| 21 | 스켈레톤 로딩 컴포넌트 | §2-1 |
| 22 | 페이지 간 크로스링크 (딥링크) | §6-4 |
| 23 | 판매단위 계층 시각화 (들여쓰기/트리) | §11-5a |
| 24 | 광고 업로드 드래그앤드롭 + 스텝 인디케이터 | §11-6a, §11-6b |
| 25 | 상태 배지 아이콘 보조 (색각 이상 대응) | §1-4 |
| 26 | 트랜지션/애니메이션 시스템 | §9-5 |
| 27 | 날짜 입력 커스텀 스타일링 | §4-3 |
| 28 | 매핑 페이지 영역 분리 (탭/아코디언) | §11-4a |
| 29 | 원가 마스터-디테일 레이아웃 | §11-7a |
| 30 | 설정 온보딩 위저드 | §11-8a |

### Phase 4 — 선택적 고급 과제

| # | 항목 | 참조 |
|---|---|---|
| 31 | Optimistic UI | §10-1 |
| 32 | useMemo/useCallback 최적화 | §10-2 |
| 33 | 모바일 카드 뷰 대체 렌더링 | §7-1 |
| 34 | 작업 큐 자동 폴링 | §11-9a |
| 35 | 상세보기 패턴 통일 (마스터-디테일) | §5-3 |
| 36 | API 자동 재시도 | §3-3 |
| 37 | 브레드크럼 | §6-3 |
| 38 | 간격/패딩 일관성 정리 | §9-3 |
| 39 | glass-panel 계층 가이드라인 | §9-4 |
| 40 | 폼 리셋/취소 버튼 | §4-4 |

---

## 변경 파일 영향도 요약

| 파일 | 관련 항목 수 |
|---|---|
| `components/shared/` (신규 포함) | 15개+ (modal, toast, skeleton, pagination, confirm-dialog 등) |
| `components/mappings/mappings-view.tsx` | 8개 |
| `components/profits/profits-view.tsx` | 6개 |
| `components/orders/orders-view.tsx` | 5개 |
| `components/dashboard/dashboard-view.tsx` | 5개 |
| `components/sales-units/sales-units-view.tsx` | 5개 |
| `components/ads/ad-uploads-view.tsx` | 5개 |
| `components/costs/costs-view.tsx` | 5개 |
| `components/stores/store-settings-view.tsx` | 4개 |
| `components/operations/operations-view.tsx` | 3개 |
| `components/layout/app-shell.tsx` | 3개 |
| `components/layout/top-header.tsx` | 3개 |
| `components/layout/sidebar-nav.tsx` | 2개 |
| `lib/api/client.ts` | 3개 |
| `lib/api/services.ts` | 2개 |
| `globals.css` | 3개 |
| `app/(main)/error.tsx` (신규) | 1개 |
| `app/layout.tsx` | 1개 |

---

## 비파괴 원칙

- 백엔드 API 변경 없음 (프런트엔드 전용 개선).
- 기존 기능 회귀 없음을 각 Phase 완료 시 수동/자동 테스트로 검증.
- 기존 코드 스타일(Tailwind, lucide-react, CSS 변수, glass-panel 디자인 언어) 준수.
- 공통 컴포넌트(Modal, Toast, Pagination 등)는 기존 shared 패턴을 따라 `components/shared/`에 추가.
- 각 항목은 독립적으로 적용 가능 (의존 관계가 있는 경우 명시).
