# 0504 비용/판매단위 통합 엑셀 관리 + 비용 시점 스냅샷 모델 작업계획서

## 0. 이 문서의 위치

이 파일 하나만 보고 다음 세션에서 바로 작업에 들어갈 수 있도록 작성됨. 사용자의 방향성, 데이터 모델 변경, 영향받는 실제 파일/라인, 변경 전/후 로직, 회귀 위험 포인트, 작업 순서, 결정된 정책을 모두 담음. 0430 광고 업로드 단순화 계획서와 같은 형식.

작성 기준일: 2026-05-04

---

## 1. 사용자 방향성 (의사결정 근거)

### 1.1 현재 페인포인트 (사용자 진술 기반)

- 비용설정 탭의 "시작일·종료일을 판매단위 개별로 하나하나 지정" 구조가 운영상 너무 번거로움.
- 한 번 적용된 비용 row는 직접 수정/비활성화 불가 (`COST_SETTING_NOT_EDITABLE`, `COST_SETTING_ALREADY_APPLIED`). 원가 변동이 자주 일어나면 N개 판매단위에 대해 N개 row를 끊어주는 작업이 누적됨.
- "기간 한가운데 삽입 불가" (`COST_PERIOD_SPLIT_NOT_SUPPORTED`), "다음 구간 충돌" (`COST_PERIOD_OVERLAP`) 등 시작일 기준 제약이 많아서 일괄 import 가 사실상 불가.
- 비용설정 탭과 판매단위 탭이 사용자 작업 흐름상 한 묶음(상품 등록 → 매핑 → 원가 입력)인데 두 화면에 흩어져 있음.

### 1.2 변경 후 방향

- **비용 데이터 모델을 "기간 row" 에서 "시점 스냅샷" 으로 전환.** 한 스냅샷 = "이 시점부터 적용되는 전체 판매단위의 비용 표". 다음 스냅샷의 effectiveFrom 이 자동으로 이전 스냅샷의 종료점이 됨. effectiveTo 자체가 모델에서 사라짐.
- **판매단위 + 비용을 한 엑셀로 통합 관리.** 행 = 판매단위, 컬럼 = `salesUnitId | displayName | matchAliases | linkedProductIds | linkedOptionCodes | linkedManageCodes | unitCost | feeRate | otherCost`. 다운로드 / 업로드 모두 한 파일.
- **백엔드 매핑 시스템·광고 매핑·그룹 구조·손익 계산 본체는 손대지 않음.** (옵션 B 채택 — 사용자 UX 는 통합, 데이터 모델은 분리. 매핑 시스템에 시점 인식을 도입하지 않음.)
- 손익 계산 lookup 만 한 곳 변경: `helpers.ts` 의 `getCostSettingForDate()` 가 새 스냅샷 모델을 조회하도록.

### 1.3 핵심 결정 사항 (사용자와 합의 완료)

1. **비용 모델은 시점 스냅샷.** `SalesUnitCostSnapshot` (헤더) + `SalesUnitCostSnapshotEntry` (판매단위별 비용 셀) 두 엔티티로 분해.
2. **엑셀 한 장 = 그 시점의 전체 표 (full snapshot).** 부분 업데이트 / 변경분만 입력 같은 모드는 제공하지 않음. effectiveFrom 1개를 입력하고 행 전체를 통째로 박음.
3. **판매단위 메타는 현 모델 그대로.** `canonicalSalesUnits` 엔티티 유지. 엑셀의 메타 컬럼 변경은 즉시 update (시점 무관). 비용 컬럼만 effectiveFrom 시점 스냅샷에 들어감.
4. **그룹 / `parentSalesUnitId` / `isStoreLevel` 은 엑셀에서 읽기 전용.** 그룹 생성 / 해체 / 자식 부착은 기존 판매단위 탭의 전용 액션으로만 가능. 엑셀 한 장이 그룹 구조를 흔들지 않게 함 (광고 매핑 승계 / 그룹 해체 시 INTENTIONALLY_UNMAPPED 변경 등 부가 효과 보호).
5. **케이스 1 (가장 오래된 스냅샷보다 더 과거 시점 주문)** 은 `INCOMPLETE_COST` 로 표시. 별도 정책 추가 없음 (기존 동작 그대로).
6. **케이스 2 (실수 누락 / 판매단위는 있는데 비용 entry 가 없음)** 은 **차단하지 않음.** 업로드는 그대로 성공시키되, **5초간 자동 사라지는 토글(toast) 형태로 "판매단위와 비용 항목의 갯수가 맞지 않습니다" 만 표시.** 빨간 강조, 명시 확인, 거부 등 강한 방어선은 두지 않음. 누락된 판매단위는 그 스냅샷에서 비용 없음 → 손익에서 `INCOMPLETE_COST`.
7. **케이스 3 (같은 effectiveFrom 으로 재업로드)** 은 **덮어쓰기만.** 직전 스냅샷은 hard delete 후 재생성. `replacedBy` 같은 보존 메커니즘은 만들지 않음. 1-click 되돌리기 같은 기능도 없음.
8. **손익 계산 방식은 변경하지 않음.** 이미 호출 시점 lazy 계산 구조라 변경 가치 없음. "계산하기 버튼" UX 도입은 이번 스코프 밖.

### 1.4 명시적으로 작업 스코프에서 제외

- 매핑 시스템 시점 인식화 (sales-unit-auto-mapper / ad-mapping-engine)
- 그룹 구조 시점 박제
- 판매단위 메타 (displayName / linkedXxx) 의 시점별 버전 관리
- 광고 매핑 / 캠페인 매핑 / 그룹 광고 승계 로직
- 손익 계산 본체 (`calculateDailyProfitRows` / `calculateDashboardSummary`) — 비용 lookup 한 곳만 교체

이 항목들은 향후 별도 프로젝트로 검토. 이번 작업이 만약 매핑 시스템에 영향을 준다면 그건 회귀이며 즉시 수정 대상.

---

## 2. 영향받는 파일 목록

### 백엔드 (NestJS)

| 파일 | 역할 | 수정 범위 |
|---|---|---|
| `packages/shared/src/types.ts` | 공유 타입 | **신규 엔티티 2개 추가**, `DatabaseShape` 컬렉션 추가, `SalesUnitCostSetting` 은 deprecated 마크하되 타입은 잔존 (마이그레이션용) |
| `apps/backend/src/cost.service.ts` | 비용 비즈니스 로직 | **거의 전면 재작성** — 스냅샷 import / list / delete / lookup 로 재설계. 기존 create / update / close / deactivate 메서드는 deprecated 처리 (호출 경로 끊기). |
| `apps/backend/src/helpers.ts` | 손익 계산 + 유틸 | `getCostSettingForDate()` (라인 504~519) 재작성. `calculateDailyProfitRows()` 안의 `costSettingsByUnit` Map 빌드 부분 (라인 633~642) 새 모델로 대응. `createEmptyDatabase()` (라인 30~45) 에 신규 컬렉션 추가. |
| `apps/backend/src/app.controller.ts` | API 엔드포인트 | **기존 cost setting 5개 엔드포인트 유지하되 deprecated.** 신규 엔드포인트 추가: `POST /sales-unit-cost-snapshots/import` (엑셀 업로드 + 메타 update + 스냅샷 생성), `GET /sales-unit-cost-snapshots` (스냅샷 목록), `GET /sales-unit-cost-snapshots/export` (현 시점 또는 지정 시점 엑셀 다운로드), `DELETE /sales-unit-cost-snapshots/:snapshotId` (수동 삭제). |
| `apps/backend/src/database.service.ts` | DB 영속화 | 라인 27 부근의 컬렉션 매핑 테이블에 신규 컬렉션 2개 (`salesUnitCostSnapshots`, `salesUnitCostSnapshotEntries`) 추가. |
| `apps/backend/src/sales-unit.service.ts` | 판매단위 CRUD | **변경 없음** — 엑셀 import 가 기존 `update`/`create` 메서드를 그대로 호출하는 식으로 짤 것. |
| `apps/backend/src/audit-log.service.ts` | 감사 로그 | **변경 없음** — 호출만 추가 |
| `apps/backend/src/run-tests.ts` | 테스트 하네스 | 새 lookup 함수 / 마이그레이션 / 토글 누락 케이스 / 덮어쓰기 시나리오 테스트 추가 |

### 프론트엔드 (Next.js)

| 파일 | 역할 | 수정 범위 |
|---|---|---|
| `apps/frontend/components/costs/costs-view.tsx` | 비용 설정 화면 | **거의 전면 재작성** — 단일 row 입력 폼 → 엑셀 다운로드 / 업로드 + 스냅샷 히스토리 테이블. 기존 `selectedCostSettingId` / `draft` / `useEffect` 동기화 / 폼 onSubmit 로직 모두 삭제. |
| `apps/frontend/app/(main)/costs/page.tsx` | 페이지 entry | 그대로 유지 (`getCostsPageData()` 호출 한 줄). |
| `apps/frontend/lib/api/services.ts` | 페이지 데이터 페칭 | `getCostsPageData()` (라인 847~868) 변경 — 기존 `getCostSettings()` 대신 `getCostSnapshots()` 호출. |
| `apps/frontend/lib/api/types.ts` | 타입 정의 | `CostsPageData` 의 `costSettings: CostSettingListItem[]` → `costSnapshots: CostSnapshotListItem[]` (또는 두 필드 병기 후 마이그레이션). |
| `apps/frontend/app/api/sales-unit-cost-settings/**` | 기존 프록시 라우트 | 일단 유지 (호출 경로 끊김). |
| `apps/frontend/app/api/sales-unit-cost-snapshots/**` | 신규 프록시 라우트 | 4개 엔드포인트 분량 신설: `import`, list (root), `export`, `[snapshotId]`. |

### 변경 없음 (영향 모니터링만)

- `packages/shared/src/normalization.ts`
- `apps/backend/src/sales-unit-auto-mapper.ts`
- `apps/backend/src/ad-mapping-engine.ts`
- `apps/backend/src/profit.service.ts` (helpers 만 통해서 변경 흡수됨)
- `apps/backend/src/order-sync.service.ts`
- `apps/backend/src/mapping-seed.service.ts`
- `apps/backend/src/operation.service.ts`
- 손익 페이지 / 대시보드 페이지 / 매핑 페이지 / 광고 페이지 (모두 lookup 결과만 받아 표시)

---

## 3. 데이터 모델 변경 상세

### 3.1 신규 엔티티 (`packages/shared/src/types.ts`)

```typescript
/**
 * 특정 시점(effectiveFrom)부터 적용되는 비용 표 헤더.
 * 한 스냅샷이 그 시점 이후의 모든 판매단위 비용을 표현.
 * 다음 스냅샷의 effectiveFrom 이 자동으로 이 스냅샷의 종료점.
 */
export interface SalesUnitCostSnapshot {
  id: string;
  storeId: string;
  effectiveFrom: string;          // YYYY-MM-DD (KST 기준)
  sourceFileName: string | null;  // 업로드 원본 파일명 (감사용)
  createdAt: string;
  updatedAt: string;
}

/**
 * 한 스냅샷 안의 한 판매단위에 대한 비용 셀.
 * (snapshotId, canonicalSalesUnitId) 가 사실상의 복합 키 — 코드에서 unique 보장.
 */
export interface SalesUnitCostSnapshotEntry {
  id: string;
  snapshotId: string;
  storeId: string;                       // 동일 스토어 보장용 (cross-store 차단)
  canonicalSalesUnitId: string;
  unitCost: number;
  feeRate: number | null;                // null = API 실측 수수료 폴백
  otherCost: number;
  createdAt: string;
  updatedAt: string;
}
```

`DatabaseShape` 에 두 컬렉션 추가:

```typescript
export interface DatabaseShape {
  // ... 기존 14개 컬렉션 ...
  salesUnitCostSettings: SalesUnitCostSetting[];   // ← deprecated, 마이그레이션 후 비우기 권장
  salesUnitCostSnapshots: SalesUnitCostSnapshot[];          // 신규
  salesUnitCostSnapshotEntries: SalesUnitCostSnapshotEntry[]; // 신규
  operations: OperationRecord[];
  auditLogs: AuditLog[];
}
```

`SalesUnitCostSetting` 인터페이스는 **타입 자체는 남겨둠.** 마이그레이션 스크립트와 deprecated 메서드가 참조하므로. 일정 운영 후 전체 0 건 확인되면 별도 PR 로 제거.

### 3.2 PostgreSQL 매핑 (`apps/backend/src/database.service.ts`)

라인 27 부근의 `TABLE_DEFINITIONS` 같은 매핑 테이블에 다음 2건 추가:

```typescript
{ key: "salesUnitCostSnapshots", tableName: "sales_unit_cost_snapshots" },
{ key: "salesUnitCostSnapshotEntries", tableName: "sales_unit_cost_snapshot_entries" },
```

기존 `sales_unit_cost_settings` 테이블은 그대로 둠 — JSONB 패턴이라 마이그레이션 영향 적음.

### 3.3 마이그레이션 정책

- 첫 배포 시 **일회성 마이그레이션 스크립트** 또는 부팅 시 자동 마이그레이션 hook 으로 기존 `salesUnitCostSettings` → `salesUnitCostSnapshots` + `entries` 변환.
- 같은 `effectiveFrom` 의 active row 들을 한 스냅샷으로 묶음. effectiveTo 는 무시 (다음 스냅샷이 자동으로 종료점).
- `isActive=false` row 는 마이그레이션 대상에서 제외.
- 마이그레이션 완료 후 `salesUnitCostSettings` 컬렉션은 **비우지 말고 그대로 둠** (이력 보존). 코드가 더 이상 읽지 않을 뿐.
- 이 작업은 **계획서 본 PR 직후 별도 PR** 로 진행 권장. 본 PR 에서는 신규 모델만 동작하면 되고, 빈 신규 컬렉션 + 빈 옛 컬렉션 상태에서 정상 동작하는지 (= 손익은 INCOMPLETE_COST 로 떨어지지만 시스템은 안 죽음) 만 확인.

### 3.4 lookup 알고리즘 (변경 후 `helpers.ts`)

```typescript
/**
 * paymentDate 기준으로 적용할 비용 entry 를 찾는다.
 * 알고리즘: paymentDate ≤ effectiveFrom 인 가장 최근 스냅샷을 고른 뒤,
 *           그 스냅샷에서 canonicalSalesUnitId 매칭 entry 반환.
 * 매칭 entry 없으면 null → 손익에서 INCOMPLETE_COST 처리.
 */
export const getCostSettingForDate = (
  snapshotsForUnit: Array<{ snapshot: SalesUnitCostSnapshot; entry: SalesUnitCostSnapshotEntry | null }>,
  targetDate: string | null,
): SalesUnitCostSnapshotEntry | null => {
  if (!targetDate) {
    return null;
  }

  // snapshotsForUnit 은 calculateDailyProfitRows 가 미리 효과 시작일 오름차순으로 빌드해서 넘김.
  // 가장 최근부터 역방향으로 첫 매칭을 찾음.
  for (let i = snapshotsForUnit.length - 1; i >= 0; i -= 1) {
    const { snapshot, entry } = snapshotsForUnit[i];
    if (snapshot.effectiveFrom <= targetDate) {
      return entry; // entry 가 null 이면 그 스냅샷에 해당 판매단위가 없음 → INCOMPLETE_COST
    }
  }

  return null;
};
```

> 시그니처 변경됨: 기존 `(SalesUnitCostSetting[], string | null) → SalesUnitCostSetting | null` → 신규 `(매칭 페어 배열, string | null) → SalesUnitCostSnapshotEntry | null`. 이 함수의 호출자는 `helpers.ts` 안의 `calculateDailyProfitRows()` 한 곳뿐이므로 양쪽을 같이 수정. 외부 노출은 거의 없음 (`helpers.ts` 내부 사용 위주).

### 3.5 `calculateDailyProfitRows()` 안의 빌드 변경 (라인 633~642 부근)

현재 코드:
```typescript
const costSettingsByUnit = new Map<string, SalesUnitCostSetting[]>();
database.salesUnitCostSettings
  .filter((item) => item.storeId === storeId)
  .forEach((item) => {
    const list = costSettingsByUnit.get(item.canonicalSalesUnitId) ?? [];
    list.push(item);
    list.sort((left, right) => left.effectiveFrom.localeCompare(right.effectiveFrom));
    costSettingsByUnit.set(item.canonicalSalesUnitId, list);
  });
```

변경 후:
```typescript
// 스토어의 스냅샷을 effectiveFrom 오름차순으로 정렬
const snapshots = database.salesUnitCostSnapshots
  .filter((item) => item.storeId === storeId)
  .sort((left, right) => left.effectiveFrom.localeCompare(right.effectiveFrom));

// 판매단위별로 (snapshot, entry|null) 페어 배열 빌드
// entry 가 null 인 경우는 그 스냅샷에 해당 판매단위가 없는 경우 (케이스 2 누락 / 신규 판매단위)
const entryIndex = new Map<string, Map<string, SalesUnitCostSnapshotEntry>>(); // snapshotId → unitId → entry
database.salesUnitCostSnapshotEntries
  .filter((item) => item.storeId === storeId)
  .forEach((entry) => {
    const sub = entryIndex.get(entry.snapshotId) ?? new Map();
    sub.set(entry.canonicalSalesUnitId, entry);
    entryIndex.set(entry.snapshotId, sub);
  });

const costSettingsByUnit = new Map<string, Array<{ snapshot: SalesUnitCostSnapshot; entry: SalesUnitCostSnapshotEntry | null }>>();
database.canonicalSalesUnits
  .filter((unit) => unit.storeId === storeId)
  .forEach((unit) => {
    const pairs = snapshots.map((snapshot) => ({
      snapshot,
      entry: entryIndex.get(snapshot.id)?.get(unit.id) ?? null,
    }));
    costSettingsByUnit.set(unit.id, pairs);
  });
```

이후 라인 694 의 `getCostSettingForDate(...)` 호출은 시그니처가 새 형태로 바뀌었으니 그대로 호출 가능 (이제 `costSettingsByUnit.get(item.canonicalSalesUnitId!)` 가 페어 배열을 줌). 라인 704~706 의 `costSetting?.unitCost` 등 필드 접근은 `SalesUnitCostSnapshotEntry` 가 같은 필드명 (`unitCost`, `feeRate`, `otherCost`) 을 갖도록 설계했으므로 그대로 동작. `calculateFee()` 시그니처도 같음.

### 3.6 `createEmptyDatabase()` (라인 30~45)

신규 컬렉션 2개 추가:

```typescript
salesUnitCostSnapshots: [],
salesUnitCostSnapshotEntries: [],
```

---

## 4. 백엔드 비즈니스 로직 (`apps/backend/src/cost.service.ts`)

### 4.1 신규 메서드 — `importExcelSnapshot`

**시그니처**:
```typescript
importExcelSnapshot(
  storeId: string,
  effectiveFrom: string,
  file: Express.Multer.File,
): Promise<{
  snapshotId: string;
  effectiveFrom: string;
  metaUpdated: { created: number; updated: number };
  costEntries: { count: number };
  mismatch: { salesUnitCount: number; entryCount: number; missingSalesUnitIds: string[] };
  replacedSnapshot: { snapshotId: string; effectiveFrom: string } | null;
}>
```

**처리 순서**:
1. `storeService.ensureWritable(storeId)`.
2. 파일 형식 검증 (`/\.xlsx$/i`). 미통과 시 `BadRequestException` (`fileName: originalFileName` 포함, 0430 광고 단순화 규칙 동일).
3. XLSX 파싱. 첫 행 = 헤더. 필수 헤더: `salesUnitId | displayName | unitCost | feeRate | otherCost`. 선택 헤더: `matchAliases | linkedProductIds | linkedOptionCodes | linkedManageCodes | memo`. 다른 컬럼은 무시.
4. **메타 영역 적용 (즉시, 시점 무관)**:
   - 각 행에 대해 `salesUnitId` 가 비어있으면 신규 → `salesUnitService.create()` (또는 동등 로직) 호출.
   - 비어있지 않으면 기존 → `salesUnitService.update()` 호출.
   - 그룹 / `parentSalesUnitId` / `isStoreLevel` 컬럼은 **읽기 전용**. 엑셀에 와도 무시. (이 정책은 안내 메시지로도 사용자에게 표시.)
   - 비활성화는 `status` 컬럼에 `INACTIVE` 같은 명시 키워드가 있을 때만 (스코프 외 — 향후 결정).
   - 메타 변경 카운트 (`created`, `updated`) 기록.
5. **비용 영역 적용 (시점 스냅샷)**:
   - 각 행의 `unitCost`, `feeRate`, `otherCost` 가 모두 비어있으면 → 그 행은 비용 entry 생성 스킵 (= 메타만 처리).
   - `feeRate` 만 비어있으면 → null 로 저장 (현 모델 동일 의미: API 실측 수수료 사용).
   - `unitCost` 또는 `otherCost` 가 비어있으면 → 그 행도 entry 생성 스킵 + mismatch.missingSalesUnitIds 에 추가.
   - 값이 0 이면 → 정상 entry 로 저장 (사용자가 명시적으로 0 을 입력한 의도 존중).
6. **덮어쓰기 처리**:
   - 같은 (`storeId`, `effectiveFrom`) 의 기존 스냅샷이 있으면 → 그 스냅샷과 그 모든 entry 를 `databaseService.write` 안에서 **hard delete**, 새 스냅샷 push. 응답의 `replacedSnapshot` 에 정보 담아 반환.
   - audit log 도 별도 `DELETE` + `CREATE` 두 건 기록.
7. **mismatch 체크**:
   - 활성 판매단위 수 (= store 의 isActive 인 canonicalSalesUnits) vs 새 스냅샷에 들어간 entry 수 비교.
   - 일치하지 않으면 응답에 `mismatch.salesUnitCount`, `mismatch.entryCount`, `mismatch.missingSalesUnitIds` 채움. 차단하지 않음.
8. 단일 `databaseService.write` 트랜잭션으로 (a) 메타 update/create, (b) 기존 동시점 스냅샷 delete, (c) 신규 스냅샷 + entries push 를 한 번에 처리.
9. audit log 2건 기록 (메타 부분, 비용 부분 분리).
10. 응답 반환.

### 4.2 신규 메서드 — `listSnapshots`

```typescript
listSnapshots(storeId: string, page?: number, pageSize?: number)
```

스냅샷을 effectiveFrom 내림차순으로 반환. 각 항목에 `entryCount`, `missingSalesUnitCount` (= 그 시점 활성 판매단위 수 - entryCount) 포함. paginate.

### 4.3 신규 메서드 — `exportExcel`

```typescript
exportExcel(storeId: string, effectiveFrom?: string): Buffer
```

- `effectiveFrom` 이 주어지면 그 시점 직전까지 적용된 표를 만들어 반환 (= `effectiveFrom` ≤ targetDate 인 가장 최근 스냅샷의 entry 들 + 해당 스토어의 모든 활성 판매단위 메타).
- 주어지지 않으면 "오늘자 적용 표" = 가장 최근 스냅샷.
- XLSX 생성. 헤더는 import 와 정확히 같은 컬럼 셋. salesUnitId 컬럼 채워서 내려보냄으로써 사용자가 그대로 다시 올리면 update 처리.

### 4.4 신규 메서드 — `deleteSnapshot`

```typescript
deleteSnapshot(snapshotId: string)
```

- 단순 hard delete (스냅샷 1건 + 그 entries 전부).
- 사용자가 실수로 만든 스냅샷을 정리할 유일한 수단.
- 광고 업로드 단순화의 `deleteUpload` 와 같은 정신.
- audit log 기록.

### 4.5 deprecated 메서드 (호출 경로 끊김, 유지)

기존 5개 메서드 (`list`, `create`, `update`, `close`, `deactivate`) 는 **삭제하지 말고 그대로 둠.** 이유:
- 컨트롤러 엔드포인트 5개가 이를 호출 (라인 379~417).
- 마이그레이션 기간 중 데이터 정합성이 의심스러우면 이 옛 API 로 잠깐 들어가서 확인할 수 있음.
- 광고 업로드 단순화의 옵션 A 정신과 같음 — **변경 최소화 원칙. 호출 경로가 끊기면 그만.**

단, `cost.service.ts` 의 `list()` 는 새 프론트가 호출하지 않으므로 안전. 만약 누군가 다른 코드에서 `costService.list()` 를 호출한다면 마이그레이션 기간 동안은 빈 배열 반환에 가까운 결과가 나올 것 (마이그레이션 후 옛 `salesUnitCostSettings` 컬렉션이 비어가니까). 이건 의도된 동작.

> **주의**: TypeScript strict 빌드에서 미사용 import / 미사용 메서드가 에러를 낼 수 있음. 빌드 후 점검. 0430 광고 업로드 단순화에서 같은 이슈가 있었음.

---

## 5. 컨트롤러 (`apps/backend/src/app.controller.ts`)

### 5.1 신규 엔드포인트

```typescript
@Post("sales-unit-cost-snapshots/import")
@UseInterceptors(FileInterceptor("file"))
importCostSnapshot(
  @Body() body: { storeId: string; effectiveFrom: string },
  @UploadedFile() file: Express.Multer.File,
) {
  return this.costService.importExcelSnapshot(body.storeId, body.effectiveFrom, file);
}

@Get("sales-unit-cost-snapshots")
listCostSnapshots(
  @Query("storeId") storeId: string,
  @Query("page") page?: string,
  @Query("pageSize") pageSize?: string,
) {
  return this.costService.listSnapshots(storeId, page ? Number(page) : undefined, pageSize ? Number(pageSize) : undefined);
}

@Get("sales-unit-cost-snapshots/export")
exportCostSnapshot(@Query("storeId") storeId: string, @Query("effectiveFrom") effectiveFrom?: string, @Res() res: Response) {
  const buffer = this.costService.exportExcel(storeId, effectiveFrom);
  res.setHeader("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
  res.setHeader("Content-Disposition", `attachment; filename=cost-snapshot-${storeId}-${effectiveFrom ?? "current"}.xlsx`);
  res.send(buffer);
}

@Delete("sales-unit-cost-snapshots/:snapshotId")
deleteCostSnapshot(@Param("snapshotId") snapshotId: string) {
  return this.costService.deleteSnapshot(snapshotId);
}
```

> ad-uploads 의 `previewUpload()` 가 `@UseInterceptors(FileInterceptor(...))` 등을 어떻게 처리했는지 그대로 따라하면 됨. 0430 작업에서 이미 패턴 잡혀 있음.

### 5.2 기존 엔드포인트 (라인 379~417)

**전부 그대로 유지.** 호출 경로만 끊기면 됨. 단, 응답 자체는 deprecated 한 옛 데이터(빈 배열 또는 마이그레이션 전 잔존)를 그대로 보여줄 것이므로 운영상 인지.

---

## 6. 프론트엔드 상세 (`apps/frontend/components/costs/costs-view.tsx`)

### 6.1 제거 대상

| 라인 | 제거 항목 | 사유 |
|---|---|---|
| 16~24 | `emptyDraft()` | 단일 row 입력 폼 폐기 |
| 28~32 | `selectedCostSettingId`, `draft`, `isDeactivating` state | 폼 폐기 |
| 36~57 | `useMemo` (selectedCostSetting), `useEffect` (draft 동기화) | 폼 폐기 |
| 70~71 | `isBusy`, `isEditing` 계산 (재구성 필요) | 폼 폐기 — 새 isBusy 만 다시 계산 |
| 80~86 | `parseNumberValue` 헬퍼 | 폼 폐기 |
| 122~199 (대부분) | "비용 입력" Panel 의 form | 통째로 교체 (엑셀 다운로드 / 업로드 + mismatch 토글) |
| 99~115 부근 | "신규 비용 row" 버튼 | 더 이상 의미 없음 |

### 6.2 신규 추가

**파일 다운로드 영역**:
```tsx
<Panel title="현재 비용 표 다운로드">
  <button onClick={() => window.location.href = `/api/sales-unit-cost-snapshots/export?storeId=${primaryStore.id}`}>
    현재 적용 중인 표 .xlsx 로 다운로드
  </button>
</Panel>
```

**파일 업로드 영역**:
```tsx
<Panel title="비용 표 업로드">
  <form onSubmit={...}>
    <input type="date" value={effectiveFrom} onChange={...} />  {/* effectiveFrom 입력 */}
    <input type="file" accept=".xlsx" onChange={...} />
    <button type="submit">업로드</button>
  </form>

  {/* 5초 토글 토스트 */}
  {mismatchToast ? <Toast message="판매단위와 비용 항목의 갯수가 맞지 않습니다." duration={5000} /> : null}
</Panel>
```

> 토스트 컴포넌트는 신규로 작성하거나 (`components/shared/toast.tsx`), 기존에 있다면 재사용. 일반 형태:
```tsx
function Toast({ message, duration }: { message: string; duration: number }) {
  const [visible, setVisible] = useState(true);
  useEffect(() => {
    const timer = setTimeout(() => setVisible(false), duration);
    return () => clearTimeout(timer);
  }, [duration]);
  if (!visible) return null;
  return <div className="fixed bottom-4 right-4 rounded-2xl bg-amber-100 px-4 py-3 text-sm">{message}</div>;
}
```

**스냅샷 히스토리 테이블**:
```tsx
<Panel title="스냅샷 이력">
  <DataTable
    columns={[
      { key: "effectiveFrom", title: "적용 시작일", render: (row) => formatDate(row.effectiveFrom) },
      { key: "entryCount", title: "엔트리 수", render: (row) => formatNumber(row.entryCount) },
      { key: "missing", title: "누락 판매단위", render: (row) => row.missingSalesUnitCount > 0 ? `${row.missingSalesUnitCount}개` : "-" },
      { key: "createdAt", title: "업로드 시각", render: (row) => formatDateTime(row.createdAt) },
      { key: "actions", title: "관리", render: (row) => (
        <button onClick={() => handleDelete(row.id, row.effectiveFrom)}>삭제</button>
      ) },
    ]}
    rows={data.costSnapshots}
    getRowKey={(row) => row.id}
  />
</Panel>
```

### 6.3 onSubmit 흐름

```typescript
async (event) => {
  event.preventDefault();
  setMismatchToast(null);
  setErrorMessage(null);

  if (!effectiveFrom.trim() || !selectedFile) {
    setErrorMessage("시작일과 .xlsx 파일을 모두 선택해 주세요.");
    return;
  }

  setIsUploading(true);
  try {
    const formData = new FormData();
    formData.append("storeId", primaryStore.id);
    formData.append("effectiveFrom", effectiveFrom);
    formData.append("file", selectedFile);

    const response = await fetch("/api/sales-unit-cost-snapshots/import", {
      method: "POST",
      body: formData,
    });
    const payload = await response.json();
    if (!response.ok) {
      setErrorMessage(payload?.message ?? "업로드 실패");
      return;
    }

    const { mismatch, replacedSnapshot } = payload.data;

    let message = `${effectiveFrom} 시점 비용 표가 적용되었습니다.`;
    if (replacedSnapshot) {
      message += ` (같은 시점의 기존 스냅샷을 덮어썼습니다.)`;
    }
    setSuccessMessage(message);

    if (mismatch.salesUnitCount !== mismatch.entryCount) {
      setMismatchToast(true);  // 5초 후 자동 사라짐 (Toast 컴포넌트 내부에서 처리)
    }

    setSelectedFile(null);
    setEffectiveFrom("");
    startRefresh(() => router.refresh());
  } catch (error) {
    setErrorMessage(error instanceof Error ? error.message : "업로드 실패");
  } finally {
    setIsUploading(false);
  }
}
```

### 6.4 `lib/api/services.ts` `getCostsPageData()` 변경 (라인 847~868)

```typescript
export async function getCostsPageData(): Promise<CostsPageData> {
  const storeResponse = await getStores();
  const primaryStore = pickPrimaryStore(storeResponse.data);

  if (!primaryStore) {
    return {
      primaryStore: null,
      salesUnits: [],
      costSnapshots: [],
      sources: collectSources(storeResponse),
    };
  }

  const salesUnitResponse = await getSalesUnits(primaryStore.id);
  const snapshotResponse = await getCostSnapshots(primaryStore.id);
  return {
    primaryStore,
    salesUnits: salesUnitResponse.data,
    costSnapshots: snapshotResponse.data,
    sources: collectSources(storeResponse, salesUnitResponse, snapshotResponse),
  };
}
```

> `getCostSnapshots()` 도 `services.ts` 안에 신규 함수로 추가. `getCostSettings()` 와 같은 패턴.

### 6.5 `lib/api/types.ts`

```typescript
export interface CostSnapshotListItem {
  id: string;
  effectiveFrom: string;
  entryCount: number;
  missingSalesUnitCount: number;
  sourceFileName: string | null;
  createdAt: string;
}

export interface CostsPageData {
  primaryStore: Store | null;
  salesUnits: CanonicalSalesUnit[];
  costSnapshots: CostSnapshotListItem[];   // ← 변경
  sources: DataSource[];
}
```

기존 `costSettings` 필드는 한 시점 동안 둘 다 있는 형태로 호환성 유지하다가 (또는 즉시 제거하고 mock-data 같이 정리).

### 6.6 신규 프론트 API 라우트

`apps/frontend/app/api/sales-unit-cost-snapshots/` 아래 4개 라우트 생성:
- `import/route.ts` — POST 프록시 (multipart)
- `route.ts` — GET (list) 프록시
- `export/route.ts` — GET (binary stream) 프록시
- `[snapshotId]/route.ts` — DELETE 프록시

각각 `apps/frontend/app/api/_utils/proxy.ts` 의 패턴을 따라 작성. 광고 업로드의 `app/api/ad-uploads/preview/route.ts` 가 multipart 처리 참고용 예시.

---

## 7. 회귀 위험 포인트 (반드시 확인)

### 7.1 빈 신규 컬렉션 상태 동작

마이그레이션 전, `salesUnitCostSnapshots` / `salesUnitCostSnapshotEntries` 모두 빈 배열인 상태에서:
- `calculateDailyProfitRows()` 호출 시 `costSettingsByUnit` 가 모든 unitId 에 대해 `[]` 페어 배열 반환.
- `getCostSettingForDate()` 가 항상 null 반환.
- 손익 row 의 `fallbackIncomplete = true`, `profitStatus = INCOMPLETE_COST`, `totalUnitCost = 0`, `totalOtherCost = 0`.
- 시스템은 죽지 않음. 단 모든 손익이 INCOMPLETE_COST 로 표시됨 → 마이그레이션 빨리 돌릴 것.

### 7.2 같은 시점 덮어쓰기 시 audit log 누락 위험

`databaseService.write` 안에서 hard delete + push 를 한 트랜잭션으로 해야 부분 실패가 없음. audit log 는 그 트랜잭션이 끝난 뒤에 기록하되, 만약 audit 기록이 실패하면 데이터는 이미 갈아치워진 상태. → audit 실패는 그냥 로그로 남기고 사용자에게는 성공 응답. (현 시스템의 다른 도메인도 같은 패턴.)

### 7.3 lookup 알고리즘의 시간 복잡도

`getCostSettingForDate()` 가 페어 배열을 역방향 선형 탐색. 스냅샷 수가 N, 판매단위 수가 M, 주문 수가 O 라면 한 손익 계산은 O(O·N) = 보통 N 작아서 문제 없음. 만약 운영상 N 이 커지면 페어 배열 자체를 effectiveFrom desc 로 정렬해서 탐색하거나 binary search 적용.

### 7.4 마이그레이션 비대칭 위험

마이그레이션 스크립트가 일부만 실행되어 `salesUnitCostSettings` 와 `salesUnitCostSnapshots` 가 동시에 비어있지 않은 어중간한 상태가 될 가능성. → 마이그레이션 스크립트는 한 트랜잭션 안에서 (`databaseService.write` 한 번 호출) 모든 변환을 끝내거나 0건으로 끝나야 함. 부분 마이그레이션 절대 금지.

### 7.5 신규 판매단위 추가 시 lookup miss

엑셀로 신규 판매단위 한 줄을 추가했는데 그 행에 unitCost 가 없으면 → 메타만 생성, 비용 entry 미생성 → 그 시점 이후 손익에서 INCOMPLETE_COST. 정상. 그러나 "기존 활성 판매단위 N개 + 신규 1개" 인 경우 mismatch 카운트가 1 이상 → 토글 알림 발생. 이건 의도된 알림이며 사용자가 신규 판매단위에 비용 입력을 깜빡한 경우를 빠르게 인지하게 함.

### 7.6 그룹 판매단위에 대한 비용 entry 처리

`canonicalSalesUnits` 중 `isGroup=true` 인 것은 자체 비용을 갖지 않음 (자식 합산). 엑셀에 그룹 행이 나오더라도 비용 entry 는 만들지 말 것. 또는 만들더라도 손익 계산이 그룹 행을 자식 단위로 처리하는 현 로직(`calculateDailyProfitRows` step 2 롤업) 때문에 무시됨. → 안전하게는 import 시 `isGroup=true` 행은 비용 entry 스킵 + mismatch 에 카운트하지 않음.

### 7.7 광고 매핑 / 매핑 시스템 무영향 확인

작업 후 다음 시나리오 회귀 테스트:
- 주문 동기화 → 자동 매핑 → 손익 계산 (이번 변경 영향)
- 광고 엑셀 업로드 → CONFIRMED → 손익 계산 (이번 변경 영향)
- 그룹 생성/해체 → 자식 매핑 승계 (이번 변경 무관, 하지만 회귀 검증)
- mapping-seed → 초기 판매단위 자동 생성 (이번 변경 무관, 하지만 회귀 검증)

### 7.8 PostgreSQL 모드 직렬화

`SalesUnitCostSnapshot`, `SalesUnitCostSnapshotEntry` 모두 단순 객체라 기존 JSONB 직렬화 그대로 동작. 단 `database.service.ts` 의 `TABLE_DEFINITIONS` 에 등록 안 하면 PostgreSQL 모드에서는 저장이 안 됨. JSON 파일 모드는 자동 동작.

### 7.9 mock 데이터 호환성

`apps/frontend/lib/api/mock-data.ts` 에 `costSettings` 가 채워져 있다면 새 `costSnapshots` 로 변환하거나 빈 배열로 둠. mock 가 이상해도 운영 동작에는 영향 없음.

### 7.10 type 호환성 (deprecated 인터페이스)

`SalesUnitCostSetting` 을 import 하는 모든 곳을 grep 으로 찾아보고, 신규 작업이 기존 타입을 안 깨는지 확인. 현재 알려진 import 위치:
- `apps/backend/src/cost.service.ts` (deprecated 메서드 안에서 사용 — 계속 import)
- `apps/backend/src/helpers.ts` (lookup 시그니처 변경되므로 import 제거 가능)
- `apps/backend/src/run-tests.ts` (테스트 — 테스트 정리 필요)
- `apps/frontend/lib/api/types.ts` (`CostSettingListItem` 같은 파생 타입이 있다면 정리)

---

## 8. 작업 순서 (권장)

이 순서대로 진행하면 중간에 빌드가 깨지지 않게 가능.

### Step 1 — 스키마/타입 추가
1. `packages/shared/src/types.ts` 에 `SalesUnitCostSnapshot`, `SalesUnitCostSnapshotEntry` 인터페이스 추가.
2. `DatabaseShape` 에 `salesUnitCostSnapshots`, `salesUnitCostSnapshotEntries` 컬렉션 추가.
3. `apps/backend/src/helpers.ts` 의 `createEmptyDatabase()` 에 두 컬렉션 빈 배열 추가 (라인 30~45).
4. `apps/backend/src/database.service.ts` 의 TABLE_DEFINITIONS 매핑에 두 행 추가 (라인 27 부근).
5. **빌드 통과 확인.** (이 시점에서는 새 컬렉션이 생기기만 했고 사용처가 없으므로 빌드는 통과해야 함.)

### Step 2 — lookup 변경 (helpers.ts)
1. `getCostSettingForDate()` 시그니처 + 본문을 새 모델 기반으로 재작성 (3.4 참조).
2. `calculateDailyProfitRows()` 안의 `costSettingsByUnit` 빌드 부분을 새 형태로 재작성 (3.5 참조).
3. **빌드 통과 + 기존 단위 테스트 통과 확인.** 신규 컬렉션 비어있어도 시스템이 죽지 않음을 확인. 모든 손익이 INCOMPLETE_COST 로 떨어지는 게 정상.
4. 기존 `salesUnitCostSettings` 컬렉션은 이제 helpers.ts 가 읽지 않음. 코드 수정으로만 끊김 (데이터 자체는 잔존).

### Step 3 — 백엔드 신규 서비스 메서드
1. `cost.service.ts` 에 `importExcelSnapshot`, `listSnapshots`, `exportExcel`, `deleteSnapshot` 메서드 신설 (4.1~4.4).
2. 기존 `list`, `create`, `update`, `close`, `deactivate` 는 그대로 둠 (4.5).
3. 컨트롤러 (`app.controller.ts`) 에 신규 4개 엔드포인트 추가 (5.1).

### Step 4 — 프론트 API 라우트
1. `apps/frontend/app/api/sales-unit-cost-snapshots/` 아래 4개 라우트 파일 생성 (6.6).

### Step 5 — 프론트 데이터 페칭
1. `lib/api/services.ts` 에 `getCostSnapshots()` 신설.
2. `getCostsPageData()` 변경 (6.4).
3. `lib/api/types.ts` 에 `CostSnapshotListItem` / `CostsPageData` 타입 변경 (6.5).

### Step 6 — 프론트 컴포넌트
1. `components/costs/costs-view.tsx` 의 폼 / state / useEffect 제거 (6.1).
2. 다운로드 / 업로드 / 토스트 / 히스토리 영역 추가 (6.2).
3. onSubmit 흐름 작성 (6.3).
4. `Toast` 컴포넌트 신규 작성 또는 기존 것 재사용.

### Step 7 — 데이터 마이그레이션 (별도 PR 권장)
1. 기존 `salesUnitCostSettings` 데이터를 effectiveFrom 별로 묶어서 새 모델로 변환.
2. 변환 스크립트 작성 후 dev/staging 환경에서 테스트.
3. 운영 적용.

### Step 8 — 수동 테스트
1. 빈 상태에서 엑셀 다운로드 → 빈 표 + 헤더 + 모든 활성 판매단위 행 확인.
2. 엑셀 작성 후 업로드 → 스냅샷 1건 생성 확인. 손익 페이지에서 비용 반영 확인.
3. 같은 effectiveFrom 으로 재업로드 → 덮어쓰기 + replacedSnapshot 응답 확인.
4. 일부 판매단위 비용 비워두고 업로드 → mismatch 토글 5초 표시 확인.
5. 시점 다른 두 스냅샷 입력 후 손익 페이지에서 paymentDate 별로 다른 비용 적용되는지 확인.
6. 스냅샷 삭제 → 그 시점 손익이 다른 스냅샷에서 lookup 되는지 확인.
7. 광고 매핑 / 주문 매핑 / 그룹 / mapping-seed 회귀 테스트 (7.7 참조).

### Step 9 — 정리 (선택)
1. 충분한 운영 후 기존 `salesUnitCostSettings` 컬렉션 삭제.
2. `cost.service.ts` 의 deprecated 5개 메서드 제거.
3. 컨트롤러 옛 5개 엔드포인트 제거.
4. 프론트 옛 API 라우트 (`apps/frontend/app/api/sales-unit-cost-settings/**`) 제거.
5. `SalesUnitCostSetting` 타입 인터페이스 제거.

---

## 9. 체크리스트

- [ ] `packages/shared/src/types.ts` 신규 인터페이스 2개 + DatabaseShape 컬렉션 2개 추가
- [ ] `apps/backend/src/helpers.ts` `createEmptyDatabase()` 에 컬렉션 2개 추가
- [ ] `apps/backend/src/database.service.ts` TABLE_DEFINITIONS 에 2건 추가
- [ ] `apps/backend/src/helpers.ts` `getCostSettingForDate()` 새 모델로 재작성
- [ ] `apps/backend/src/helpers.ts` `calculateDailyProfitRows()` 안의 costSettingsByUnit 빌드 새 형태로 재작성
- [ ] `apps/backend/src/cost.service.ts` `importExcelSnapshot()` 신규
- [ ] `apps/backend/src/cost.service.ts` `listSnapshots()` 신규
- [ ] `apps/backend/src/cost.service.ts` `exportExcel()` 신규
- [ ] `apps/backend/src/cost.service.ts` `deleteSnapshot()` 신규
- [ ] `apps/backend/src/app.controller.ts` 신규 4개 엔드포인트 추가
- [ ] 프론트 API 라우트 4개 신설 (`sales-unit-cost-snapshots/**`)
- [ ] `apps/frontend/lib/api/services.ts` `getCostSnapshots()` + `getCostsPageData()` 갱신
- [ ] `apps/frontend/lib/api/types.ts` `CostSnapshotListItem` / `CostsPageData` 변경
- [ ] `apps/frontend/components/costs/costs-view.tsx` 폼 제거 + 업로드/다운로드/토스트/히스토리 영역 신규
- [ ] `Toast` 컴포넌트 (5초 자동 사라짐) 신규 또는 재사용
- [ ] 수동 테스트 시나리오 (빈 상태 / 정상 업로드 / 덮어쓰기 / mismatch / 다중 스냅샷 / 삭제)
- [ ] 회귀 검증 (광고 매핑, 주문 매핑, 그룹, mapping-seed)
- [ ] 마이그레이션 스크립트 (별도 PR)
- [ ] deprecated 코드 정리 (별도 PR, 운영 후)

---

## 10. 주의사항 (작업 시 명심)

1. **매핑 시스템 손대지 말 것.** sales-unit-auto-mapper.ts, ad-mapping-engine.ts 는 절대 건드리지 않음. 이 파일들에 import 가 추가되거나 변경되면 즉시 의심.
2. **그룹 / parentSalesUnitId / isStoreLevel 은 엑셀 import 에서 읽기 전용.** 이 컬럼이 엑셀에 와도 무시. 향후 사용자가 "그룹도 엑셀로 만들 수 있게 해달라" 고 하면 별도 결정 필요 (광고 매핑 승계 / 그룹 해체 INTENTIONALLY_UNMAPPED 변경 부가효과 보호).
3. **케이스 2 (mismatch) 는 절대 차단하지 말 것.** 토글 알림 5초만. 사용자 의사결정에 따라 빨간 강조나 명시 확인 같은 강한 방어선은 만들지 않기로 결정됨.
4. **케이스 3 (같은 시점 재업로드) 은 무조건 hard delete + 덮어쓰기.** replacedBy / 1-click 되돌리기 / 버전 보존 같은 메커니즘은 만들지 않음.
5. **손익 계산 본체 변경 금지.** `calculateDailyProfitRows()`, `calculateDashboardSummary()` 의 알고리즘 자체는 건드리지 않음. lookup helper 와 그 입력 빌드 부분만 변경.
6. **`databaseService.write` 단일 호출 안에서 모든 변경을 처리.** 메타 update + 기존 스냅샷 delete + 신규 스냅샷 push 가 한 트랜잭션. 부분 실패 방지.
7. **`originalFileName` 같은 BadRequestException 응답 필드는 0430 광고 단순화 패턴 재사용.** 모든 BadRequestException 에 `fileName` 필드 포함 (0430 작업의 평가자 결함 지적 사항이었음).
8. **TypeScript strict 빌드 호환.** deprecated 메서드의 미사용 import / private 메서드 경고 발생 시 광고 단순화와 같은 정신으로 처리 (변경 최소화 원칙).
9. **마이그레이션은 별도 PR.** 본 PR 에서는 신규 모델 동작만 보장하면 됨. 빈 신규 컬렉션 + 빈 옛 컬렉션 상태에서도 시스템 안 죽음을 확인하는 게 우선.
10. **광고 업로드 단순화 (0430) 와의 일관성.** 정신적으로 같은 정신: full snapshot 단위 처리, 변경 최소화, 옛 코드 hard delete 안 함, 사용자 정보용 토글/요약은 가볍게. 작업 진행 중 광고 업로드 단순화 코드 (apps/backend/src/ads.service.ts 의 previewUpload) 를 참고하면 비슷한 패턴 많음.

---

## 11. 작업 후 메모리 갱신

작업 완료 후 다음 항목을 메모리에 추가:
- `project_cost_snapshot_excel_0504.md` — 비용 모델을 시점 스냅샷으로 전환. 판매단위 + 비용 통합 엑셀 import/export. 매핑 시스템 무영향. mismatch 는 5초 토글 알림.

기존 메모리 인덱스에서 영향받는 항목:
- `project_sales_unit_reset_0415.md` — 판매단위 리셋과 무관. 그대로.
- `project_ad_upload_simplification_0430.md` — 광고 업로드와 무관. 그대로.
- 기존코드관찰.md 의 5장 비용 설정 / 9장 비용 계산 공식 / 13장 / 14장 손익 / 16장 자주 수정 파일 목록 등 — 작업 후 갱신 필요.

---

(끝)
