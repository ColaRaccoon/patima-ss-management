# 2026-04-22 수정 요구사항 ③: 가구매 금액 일자별 DB 영속화 및 손익 분석 탭 표시/수정 UX

## 1. 배경

0422수정요구사항(①)에서 손익 분석 탭(`/profits`)에 가구매 입력 UI를 추가했지만, 해당 값은 `profits-view.tsx`의 로컬 state(`manualFakePurchaseAmount`)로만 유지되어 **새로고침·날짜 변경 시 즉시 사라지는** 구조다. 실무에서는 특정 날짜의 가구매 총액을 기록·조회하는 수요가 반복되므로, 매번 손익을 확인할 때마다 사용자가 값을 다시 입력해야 하는 현재 UX는 비효율적이다.

따라서 가구매 금액을 **일자 단위로 DB에 영속화**하고, 손익 분석 탭에서 날짜를 선택하면 해당 일자의 저장된 가구매 값이 자동으로 불러와지도록 한다. 또한 저장/수정은 의도치 않은 덮어쓰기를 막기 위해 **기존 값이 있을 때만 확인 모달**을 띄우고, 없을 때는 바로 저장되는 단순한 흐름으로 처리한다.

## 2. 요구사항

### 2.1. 데이터 모델 신규 추가

`packages/shared/src/types.ts`에 가구매 일별 총액을 나타내는 새 엔티티를 정의하고, `DatabaseShape`에 컬렉션으로 추가한다.

```typescript
export interface DailyFakePurchaseAmount {
  id: string;                  // `${storeId}-${date}` 또는 uuid
  storeId: string;             // 스토어 격리 (cross-store 금지)
  date: string;                // ISO 날짜 "YYYY-MM-DD"
  amount: number;              // 원 단위 정수, 음수·0 허용
  memo: string | null;         // 선택 메모(사유, 업체명 등) — 1차 범위에서는 null 허용만
  createdAt: string;           // ISO 시각
  updatedAt: string;           // ISO 시각
}
```

`DatabaseShape`에 `dailyFakePurchaseAmounts: DailyFakePurchaseAmount[]` 추가. `database.service.ts`의 `createEmptyDatabase()`, `normalizeSnapshot()`에도 동일하게 반영하여 기존 저장소(JSON/PostgreSQL)와 호환되도록 한다.

PostgreSQL 테이블은 프로젝트 공통 패턴(`id TEXT PRIMARY KEY, payload JSONB`)을 그대로 따른다: `daily_fake_purchase_amounts`. 저장은 다른 테이블과 동일한 TRUNCATE + 재삽입 방식.

**복합 유일성 제약**: `(storeId, date)` 조합은 DB 레벨에서 반드시 유일해야 한다. 동일 날짜에 대한 레코드는 언제나 1개만 존재하도록 upsert 로직에서 강제한다(앱 레벨 검증 + id를 `${storeId}-${date}`로 결정적으로 생성해 구조적으로 방지).

### 2.2. 백엔드 API

`app.controller.ts`에 다음 엔드포인트를 추가하고, 전용 서비스(`fake-purchase.service.ts`)로 로직을 분리한다.

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `GET`  | `/api/v1/daily-fake-purchase-amounts?storeId=&date=` | 단일 조회. 없으면 `null` 반환(404 아님) |
| `GET`  | `/api/v1/daily-fake-purchase-amounts?storeId=&dateFrom=&dateTo=` | 기간 조회(후속 확장용, 1차 범위에서는 단일 조회만 우선 구현) |
| `POST` | `/api/v1/daily-fake-purchase-amounts` | upsert. body: `{ storeId, date, amount, memo? }` |
| `DELETE` | `/api/v1/daily-fake-purchase-amounts/:id` | 삭제(값 비우기 대안) — 1차 범위에서는 선택 구현 |

- upsert 로직: `(storeId, date)`로 기존 레코드 조회 → 있으면 `amount/memo/updatedAt` 갱신, 없으면 신규 생성(`createdAt=updatedAt=now()`).
- 응답 봉투는 기존 컨벤션과 동일하게 `{ success: true, data: DailyFakePurchaseAmount }`.
- **기존 `ensureNoCrossStoreReference()` 패턴 준수**: 요청 `storeId`가 `stores` 컬렉션에 존재하는지 검증.
- 감사 로그: `audit-log.service.ts`를 통해 `FAKE_PURCHASE_UPSERT` / `FAKE_PURCHASE_DELETE` 이벤트 기록(금액, 이전 값, 메모 포함).

프론트 프록시 Route Handler(`app/api/daily-fake-purchase-amounts/route.ts`)도 기존 `route-proxy.ts` 패턴으로 추가한다.

### 2.3. 손익 분석 탭 UI — 신규 "가구매 값" 박스

`apps/frontend/components/profits/profits-view.tsx`의 기존 3개 StatCard 그리드(`추정 배송 원가 / 스토어 부담 배송비 / 고객 부담 배송비`)를 **4개 그리드**로 확장한다.

- 위치: "고객 부담 배송비" StatCard 오른쪽에 **네 번째 StatCard** 추가
- 라벨: **`{선택 날짜의 한글 포맷} 가구매 값`** (예: `2026년 4월 21일 가구매 값`)
  - `formatDate(data.dateTo)` 활용
  - 라벨이 다소 길어지므로 StatCard 라벨 폭을 확인하고 필요 시 `label` prop만 짧게, 상세 날짜는 `hint`에 노출하는 대안도 허용(구현 시 판단)
- 값: 저장된 `amount`를 `formatCurrency()`로 표시. 값이 없으면 `"미입력"` 텍스트를 옅은 색으로 표시
- hint 영역:
  - 저장된 값이 있을 때: `마지막 수정 ${formatDate(updatedAt)}` 또는 `저장됨` 정도의 간단한 메타
  - 없을 때: `날짜 선택 후 저장할 수 있습니다` 안내
- **수정 버튼**: 박스 우상단(혹은 값 아래)에 아이콘 버튼 또는 `button-ghost` 스타일의 작은 "수정" 버튼을 둔다. 누르면 아래 2.5의 확인 흐름으로 진입

그리드 반응형은 기존 클래스(`grid gap-4 md:grid-cols-3`)를 `md:grid-cols-4`로 바꾸고, `xl:grid-cols-4`로 승격(또는 기존 `xl:grid-cols-5`와의 충돌 방지 위해 새 row로 분리). 디자인 상 흐름이 어색해지면 **별도 row로 분리**해도 무방하되, "고객 부담 배송비 박스 오른쪽"이라는 원 요청의 의도는 유지.

### 2.4. 날짜 변경 시 자동 로드

- 기존 `applyDateFilter(nextDate)` 실행 후 페이지가 refresh되면, `getProfitsPageData`(또는 동급 SSR 함수)에서 해당 날짜의 `DailyFakePurchaseAmount`를 **동시에 fetch**해 `ProfitsPageData`에 `dailyFakePurchase: DailyFakePurchaseAmount | null` 필드로 내려준다.
- 클라이언트는 `data.dailyFakePurchase?.amount`를 읽어 새 박스에 표시하고, `manualFakePurchaseAmount`의 초기값으로도 주입(이미 저장된 값이 있으면 헤더 입력창에도 자동 채움).
- 날짜를 바꾸고 제출하면 → URL query 변경 → SSR 재호출 → 새 날짜의 가구매 값이 다시 내려오는 흐름(기존 페이지 아키텍처와 일관).

### 2.5. 저장/수정 흐름과 확인 모달

저장 트리거는 **두 경로**를 모두 지원한다.

**트리거 A — 손익 분석 탭 헤더의 "가구매 금액" 입력 + 새로고침 버튼**
- 현재 헤더의 `가구매 금액` 입력 폼 옆에 **"저장" 버튼을 신규 추가**하거나, 기존 "새로고침" 버튼의 동작을 확장하지 말고 별도로 둔다(UX 상 명확히 분리).
- 저장 버튼 클릭 시 아래 공통 저장 로직을 탄다.

**트리거 B — 신규 "가구매 값" 박스의 "수정" 버튼**
- 모달(또는 인라인 편집)로 금액 입력 UI를 띄우고 저장 → 공통 저장 로직.

**공통 저장 로직**:
1. 프론트에서 해당 날짜의 기존 값이 있는지 판단(`data.dailyFakePurchase !== null`).
2. 기존 값이 **있으면** → 컨펌 모달 표시:
   - 제목: `가구매 금액 수정`
   - 본문: `{날짜}에 이미 저장된 가구매 값이 있습니다. {기존 금액} → {새 금액}(으)로 수정하시겠습니까?`
   - 버튼: `수정 저장` / `취소`
   - `수정 저장` 선택 시 `POST /api/v1/daily-fake-purchase-amounts` 호출(upsert).
3. 기존 값이 **없으면** → 모달 없이 즉시 `POST`.
4. 응답 성공 → `router.refresh()`로 SSR 재실행, 박스·입력창·"예상 순이익" StatCard가 모두 새 값으로 갱신.
5. 응답 실패 → 기존 에러 패턴(`rounded-2xl border border-red-200 bg-red-50 ...`)으로 안내 문구 표시.

모달은 기존 shared 컴포넌트에 Dialog/Modal이 있으면 재사용. 없다면 `mappings-view.tsx`의 confirm 패턴(간단한 inline blocking UI)으로 일관되게 구현하되, 새로운 범용 Modal 유틸을 만들 필요는 없다(1차 범위에서는 `window.confirm`으로 대체 가능 — 디자인 정식화는 후속 과업).

### 2.6. "예상 순이익" StatCard와의 연동 유지

- 0422수정요구사항①에서 도입한 **"예상 순이익 = 기본 순이익 − 가구매 입력 금액"** 계산은 그대로 유지한다.
- 단, 입력 소스가 다음 우선순위로 바뀐다:
  1. 사용자가 현재 세션에서 직접 입력·편집한 값(`manualFakePurchaseAmount`) — 저장 전 편집 중인 상태
  2. 서버에서 내려받은 저장값(`data.dailyFakePurchase?.amount`)
  3. 없음 → `0`
- 편집 중 값이 저장값과 다르면, "예상 순이익" hint 또는 박스 근처에 `저장되지 않은 변경사항 있음` 같은 가벼운 표시를 주는 것을 권장(필수 아님).

### 2.7. 판매단위 손익 분석(우측 상세 패널)에서의 편집 경로

현재 코드 기준 `profits-view.tsx`의 우측 상세 패널은 행 클릭 시 표시되는 `selectedDetail` 영역을 가리킨다. 사용자는 해당 패널에서 가구매를 조정하려는 수요를 언급했다. 1차 범위에서는 다음 중 **한 가지**를 구현한다(개발자 판단):

- **옵션 1(권장)**: 상세 패널에 가구매 편집 입력을 추가하지 않고, 2.5의 박스 "수정" 버튼을 상세 패널에서도 시인성 있게 노출(바로가기 링크/버튼).
- **옵션 2**: 상세 패널 내부에 가구매 입력 필드를 별도로 두되, 저장 시 2.5의 공통 저장 로직(확인 모달 포함)을 그대로 호출.

어느 쪽을 택하든 **저장 동작과 확인 모달 흐름은 2.5와 완전히 동일**해야 한다. 상세 패널은 상위 페이지의 `data.dailyFakePurchase`를 동일하게 읽으므로, 기존 값 유무 판정도 같은 소스를 쓴다.

## 3. 수용 기준

1. 손익 분석 탭(`/profits`)에서 날짜를 변경하면 해당 일자의 가구매 저장값이 자동으로 로드되어 새 "가구매 값" 박스에 표시된다.
2. 새 박스는 기존 "고객 부담 배송비" StatCard의 오른쪽(또는 바로 아래 row)에 대칭적인 톤으로 배치된다.
3. 저장값이 없는 날짜를 선택하면 박스는 "미입력" 상태로 표시되고, 헤더 입력창은 빈 값이 된다.
4. 헤더 입력 저장 버튼 또는 박스의 "수정" 버튼을 누를 때:
   - 해당 날짜에 **기존 저장값이 있으면** 확인 모달이 뜨고, "수정 저장"을 눌러야만 실제 저장된다.
   - 기존 저장값이 **없으면** 모달 없이 즉시 저장된다.
5. 저장 성공 시 SSR refresh로 박스 값, "예상 순이익" StatCard가 모두 새 값으로 갱신된다.
6. DB에는 `(storeId, date)` 조합당 최대 1개의 `DailyFakePurchaseAmount` 레코드만 존재한다(upsert 보장).
7. 스토어가 다른 날짜 값은 서로 절대 섞이지 않는다(`ensureNoCrossStoreReference` 준수).
8. 감사 로그(`auditLogs`)에 upsert 이벤트가 이전 금액과 새 금액, 날짜, 사용자(있다면)와 함께 기록된다.
9. 0422수정요구사항① / ②에서 반영한 기존 동작(가구매 즉시 차감 표시, 손익 탭 UI, 매핑 관리 광고 필터 등)은 그대로 동작한다.
10. PostgreSQL / JSON 파일 두 저장소 모두에서 정상 작동한다(`database.service.ts` 로드 경로 양쪽 검증).

## 4. 범위 외 (Out of Scope)

- 가구매 금액의 **판매단위 단위 분배**(본 범위는 스토어·날짜 단위 일괄 차감만)
- 가구매 자동 탐지·외주 API 연동
- 가구매 이력 차트·엑셀 export
- 범용 Modal/Dialog shared 컴포넌트 도입(1차 범위에서는 `window.confirm` 또는 간단한 inline confirm UI로 대체 가능)
- 사용자 계정별 권한/책임자 기록(현재 시스템에 auth가 없음)
- 가구매로 인한 VAT·세금 환원 처리
- 대시보드(`/`) 페이지의 "순손익(예상)" 카드에 가구매 반영 (본 수정은 `/profits`에 한정, 후속 과업)

## 5. 복잡도·파급도 평가

- **난이도: 중(medium)**. 0422수정요구사항①·②와 달리 **백엔드 컬렉션·API 신규 추가 + 프론트 UI/흐름 변경**이 모두 포함된다.
- 수정 대상 파일:
  - `packages/shared/src/types.ts` — `DailyFakePurchaseAmount` 타입 + `DatabaseShape` 확장
  - `apps/backend/src/database.service.ts` — `createEmptyDatabase`, `normalizeSnapshot`, PostgreSQL 스키마
  - `apps/backend/src/fake-purchase.service.ts` — **신규 파일**
  - `apps/backend/src/app.controller.ts` — 엔드포인트 3~4개 추가
  - `apps/backend/src/audit-log.service.ts` — 이벤트 타입 추가(기존 패턴에 맞게)
  - `apps/backend/src/run-tests.ts` — 회귀 테스트(upsert/단일 조회/cross-store 거부) 추가
  - `apps/frontend/lib/api/services.ts` — `getProfitsPageData`에서 `dailyFakePurchase` 동반 fetch
  - `apps/frontend/lib/api/types.ts` — `ProfitsPageData.dailyFakePurchase` 필드 추가
  - `apps/frontend/app/api/daily-fake-purchase-amounts/route.ts` — **신규 프록시**
  - `apps/frontend/components/profits/profits-view.tsx` — 새 박스, 저장 버튼, 확인 모달, 공통 저장 로직
- 주의 포인트:
  - `DailyFakePurchaseAmount.id`를 `${storeId}-${date}` 결정적 문자열로 두면 upsert 충돌·중복이 구조적으로 예방된다.
  - PostgreSQL 재시작·JSON 폴백 모드 모두에서 `normalizeSnapshot()`이 빈 배열을 보장해야 기존 DB와 호환된다(마이그레이션 없이 동작).
  - SSR refresh 흐름에서 저장 → 새 값 반영까지 race condition이 없는지 검증(`router.refresh()` 이후 `data.dailyFakePurchase` 변화 확인).
  - 모달 UI가 기존 `components/shared/*`에 없다면 과도한 재사용 부담 없이 **페이지 로컬 confirm UI**로 구현(불필요한 범용 컴포넌트 신설 금지).
