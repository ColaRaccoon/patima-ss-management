# 2026-04-16 수정 계획 — 수동 매핑 시 오토매퍼 자동 트리거 제거

> 수동 매핑(mapping, batch-mapping)을 할 때마다 `recalculateOrderMappingsForStore`가 비동기로 자동 실행되는 비효율 구조를 개선한다.
> 오토매핑은 사용자가 필요하다 판단할 때 **수동 실행 버튼**으로 트리거하도록 변경.

---

## 0. 현재 문제

`order-mapping.service.ts`의 `saveMappingsInternal()` 끝에서:

```ts
// order-mapping.service.ts 라인 127-132
await this.operationService.enqueue(
  "RECALCULATE_ORDER_MAPPING",
  storeId,
  `Recalculate order mappings for store ${storeId}`,
  () => this.recalculate(storeId),
);
```

사용자가 매핑 페이지에서 시그니처 하나를 수동 매핑할 때마다 **전체 오토매퍼가 비동기로 재실행**된다. 수동 매핑은 사용자가 이미 판단을 내린 결과이므로 그 위에 오토매퍼를 다시 돌릴 이유가 없다.

### 현재 오토매퍼 트리거 전체 지도

| # | 위치 | 트리거 시점 | 유지/제거 |
|---|---|---|---|
| 1 | `order-mapping.service.ts:23-25` | `onModuleInit` — 재시도 실행기 등록 | **유지** |
| 2 | `order-mapping.service.ts:27-32` | `onModuleInit` — 모든 스토어 전체 재계산 | **유지** |
| 3 | `order-mapping.service.ts:127-132` | `saveMappingsInternal` — 수동 매핑 저장 후 | **제거** ← 핵심 |
| 4 | `sales-unit.service.ts:202` | 판매단위 수정(update) 후 | **유지** (매핑 기준 변경) |
| 5 | `sales-unit.service.ts:236` | 판매단위 비활성화 후 | **유지** |
| 6 | `sales-unit.service.ts:264` | 판매단위 활성화 후 | **유지** |
| 7 | `sales-unit.service.ts:149-150` | 판매단위 생성 후 (skipOrderRecalculation 옵션 있음) | **유지** |
| 8 | `order-sync.service.ts:244` | 주문 동기화(syncOrders) 후 | **유지** (새 데이터 유입) |
| 9 | `mapping-seed.service.ts:293` | 매핑 시드 생성 후 | **유지** (대량 판매단위 생성 직후) |

**제거 대상은 #3 하나뿐.**

---

## 1. 변경 내용

### 작업 1. `order-mapping.service.ts` — 수동 매핑 후 오토매퍼 트리거 제거

**파일**: `apps/backend/src/order-mapping.service.ts`
**위치**: `saveMappingsInternal()` 메서드, 라인 127-132

**변경**: `operationService.enqueue("RECALCULATE_ORDER_MAPPING", ...)` 호출을 **제거**한다.

**이전**:
```ts
private async saveMappingsInternal(storeId: string, mappings: ...[]): Promise<void> {
  this.databaseService.write((draft) => {
    // ... 매핑 데이터 저장 로직 ...
  });

  // ← 이 부분 제거
  await this.operationService.enqueue(
    "RECALCULATE_ORDER_MAPPING",
    storeId,
    `Recalculate order mappings for store ${storeId}`,
    () => this.recalculate(storeId),
  );
}
```

**이후**:
```ts
private async saveMappingsInternal(storeId: string, mappings: ...[]): Promise<void> {
  this.databaseService.write((draft) => {
    // ... 매핑 데이터 저장 로직 ...
  });
  // 오토매퍼 자동 트리거 제거됨.
  // 수동 매핑은 사용자가 이미 판단한 결과이므로 전체 재계산 불필요.
  // 필요 시 사용자가 매핑 페이지의 "오토매핑 실행" 버튼으로 수동 트리거.
}
```

**영향 범위**: `saveMapping()`, `saveMappings()`, `createAndMap()`, `createAndMapMany()` 모두 이 메서드를 경유하므로 한 곳만 수정하면 됨.

**주의**: `recalculate()` 메서드 자체와 `onModuleInit`의 재시도 실행기 등록은 그대로 유지. 수동 실행 API에서 재사용하기 때문.

---

### 작업 2. 오토매핑 수동 실행 API 엔드포인트 추가

**파일**: `apps/backend/src/order-mapping.service.ts`

기존 `recalculate(storeId)` 메서드를 public으로 노출하는 새 메서드 추가:

```ts
/**
 * 사용자 요청에 의한 수동 오토매핑 실행.
 * 비동기 operation 큐에 등록하여 순서 보장.
 */
async triggerAutoMapping(storeId: string): Promise<{ operationId: string }> {
  const snapshot = this.databaseService.getSnapshot();
  ensureStoreExists(snapshot, storeId);

  const operationId = await this.operationService.enqueue(
    "RECALCULATE_ORDER_MAPPING",
    storeId,
    `Manual auto-mapping for store ${storeId}`,
    () => this.recalculate(storeId),
  );
  return { operationId };
}
```

**파일**: `apps/backend/src/app.controller.ts`

매핑 도메인 라우트 영역에 새 엔드포인트 추가:

```ts
@Post("stores/:storeId/auto-mapping")
async triggerAutoMapping(@Param("storeId") storeId: string) {
  const result = await this.orderMappingService.triggerAutoMapping(storeId);
  return formatApiSuccess(result);
}
```

**API 스펙**:
- **Method**: `POST`
- **Path**: `/api/v1/stores/:storeId/auto-mapping`
- **Request body**: 없음
- **Response**: `{ success: true, data: { operationId: string }, message: null }`
- **동작**: operation 큐에 `RECALCULATE_ORDER_MAPPING` 등록 → 비동기 실행. operationId로 진행 상태 추적 가능 (`GET operations/:id`).

---

### 작업 3. 프런트엔드 — 매핑 페이지에 "오토매핑 실행" 버튼 추가

**파일**: `apps/frontend/lib/api/services.ts`

매핑 페이지 데이터 fetch 함수 영역에 API 호출 함수 추가:

```ts
export async function triggerAutoMapping(storeId: string) {
  return fetchApi({
    label: "Trigger auto-mapping",
    path: `stores/${storeId}/auto-mapping`,
    options: { method: "POST" },
  });
}
```

**파일**: `apps/frontend/lib/api/browser.ts`

브라우저용 fetch 래퍼에도 동일 패턴으로 추가:

```ts
export async function triggerAutoMappingBrowser(storeId: string) {
  return browserFetchApi({
    label: "Trigger auto-mapping",
    path: `stores/${storeId}/auto-mapping`,
    options: { method: "POST" },
  });
}
```

**파일**: `apps/frontend/components/mappings/mappings-view.tsx`

매핑 페이지 상단 영역(필터/액션 바)에 버튼 추가:

```tsx
<button
  onClick={async () => {
    setAutoMappingLoading(true);
    try {
      await triggerAutoMappingBrowser(storeId);
      // 성공 토스트 + 페이지 새로고침 (router.refresh)
    } catch (e) {
      // 에러 토스트
    } finally {
      setAutoMappingLoading(false);
    }
  }}
  disabled={autoMappingLoading}
  className="..."  // 기존 버튼 스타일과 일관되게
>
  {autoMappingLoading ? "오토매핑 실행 중..." : "오토매핑 실행"}
</button>
```

**UI 동작**:
1. 버튼 클릭 → `POST /api/v1/stores/:storeId/auto-mapping` 호출
2. 로딩 상태 표시
3. 완료 시 토스트 알림 + `router.refresh()`로 매핑 목록 갱신
4. operation 큐 시스템 덕분에 동시 실행 방지됨 (스토어별 순서 보장)

---

## 2. 변경 파일 목록

| # | 파일 | 변경 요지 |
|---|---|---|
| 1 | `apps/backend/src/order-mapping.service.ts` | `saveMappingsInternal`에서 enqueue 제거 + `triggerAutoMapping` 메서드 추가 |
| 2 | `apps/backend/src/app.controller.ts` | `POST stores/:storeId/auto-mapping` 라우트 추가 |
| 3 | `apps/frontend/lib/api/services.ts` | `triggerAutoMapping` fetch 함수 추가 |
| 4 | `apps/frontend/lib/api/browser.ts` | `triggerAutoMappingBrowser` 함수 추가 |
| 5 | `apps/frontend/components/mappings/mappings-view.tsx` | "오토매핑 실행" 버튼 UI 추가 |

---

## 3. 작업 순서

1. **작업 1**: `order-mapping.service.ts`에서 `saveMappingsInternal`의 enqueue 호출 제거.
2. **작업 2**: 같은 파일에 `triggerAutoMapping` 메서드 추가 + `app.controller.ts`에 라우트 추가.
3. **작업 3**: 프런트엔드 API 함수 + 버튼 UI 추가.
4. **검증**: `npm run test` 전체 PASS 확인. 기존 매핑 관련 테스트가 enqueue 호출에 의존하는지 확인 후 필요 시 테스트 조정.
5. **수동 테스트**: dev 서버에서 매핑 페이지 열고 (1) 수동 매핑 시 operation 큐에 RECALCULATE가 안 쌓이는지, (2) "오토매핑 실행" 버튼 누르면 큐에 정상 등록되는지 확인.

---

## 4. 수용 조건

- 수동 매핑(단건/배치) 시 `RECALCULATE_ORDER_MAPPING` operation이 생성되지 않음.
- "오토매핑 실행" 버튼으로 수동 트리거 시 정상 실행 + operation 상태 추적 가능.
- 주문 동기화, 판매단위 CRUD, 매핑 시드, 모듈 초기화 시 기존 오토매퍼 자동 트리거는 그대로 동작.
- `npm run test` 전체 PASS.
- 기존 페이지 (orders, mappings, profits, dashboard 등) 회귀 없음.

---

## 5. 비파괴 원칙

- `recalculate()` 메서드, `onModuleInit`의 재시도 실행기, operation 큐 시스템은 건드리지 않음.
- 다른 서비스(sales-unit, order-sync, mapping-seed)의 오토매퍼 트리거는 그대로 유지.
- API 응답 shape 변경 없음 (기존 매핑 API는 그대로, 새 엔드포인트만 추가).
- 기존 코드 스타일(TypeScript strict, camelCase, NestJS 패턴, formatApiSuccess envelope) 준수.

---

## 6. 참고: 오토매퍼 3단계 동작 (`recalculateOrderMappingsForStore`)

이해를 위해 `sales-unit-auto-mapper.ts`의 핵심 로직 요약:

1. **Phase 1 — 시그니처 텍스트 매칭**: `orderSourceSignatures` 중 수동 확인(`confirmedAt`)되지 않은 것들에 대해 `matchAliases` 기반 텍스트 매칭 → `MAPPED`/`CONFLICT`/`UNMAPPED` 상태 업데이트.
2. **Phase 2 — 주문 아이템 ID 기반 매핑**: `optionManageCode` → `linkedManageCodes` / `optionCode` → `linkedOptionCodes` / `externalProductId` → `linkedProductIds` 우선순위로 개별 아이템의 `canonicalSalesUnitId` 설정.
3. **Phase 3 — 시그니처 역추론**: 같은 시그니처 내 모든 아이템이 동일 판매단위로 매핑되면 시그니처도 자동 매핑, 다르면 `CONFLICT`.

**중요**: 수동 확인(`confirmedAt != null`)된 시그니처는 Phase 1에서 건드리지 않음 → 수동 매핑 결과가 오토매퍼에 의해 덮어씌워지지는 않지만, Phase 2/3에서 아이템 단위 재연산이 불필요하게 수행됨.
