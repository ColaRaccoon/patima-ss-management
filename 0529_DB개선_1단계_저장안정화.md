# 0529 DB 개선 1단계 - 저장 안정화 작업 지침서

## 이 문서의 목적

이 문서는 다음 세션이 이 파일 하나만 보고도 DB 개선 1단계의 목표와 구현 범위를 이해하고 바로 작업을 시작할 수 있도록 만든 작업 지침서다.

이번 단계의 핵심은 보안/인증 작업이 아니라 **데이터 손실 방지**다. 현재 PostgreSQL을 사용하더라도 쓰기 성공 응답이 실제 DB commit 성공을 보장하지 않고, 중첩 `write()` 때문에 감사 로그가 유실될 수 있으며, 파일 저장 모드는 경로와 atomic write 문제가 있다. 이 단계는 전체 DB 정규화나 row-level persistence로 바로 뛰지 않고, 먼저 "쓰기 성공 = 저장 완료"라는 운영 기본선을 만든다.

## 선행 상태

작업 경로:

`C:\Users\seong\Desktop\workspace\patima-naver-ss`

이미 완료된 관련 작업:

- `ORDER_RAW_PAYLOAD_RETENTION_DAYS` 도입
- 오래된 `OrderRecord.rawPayload`, `OrderItem.rawPayload`를 `null` 처리
- rawPayload 없이 주문 목록과 마진 계산이 동작하는 테스트 추가

이번 단계는 위 작업을 되돌리지 않는다.

## 현재 문제

현재 `DatabaseService.write()`는 다음 구조다.

- 전체 snapshot을 deep clone한다.
- mutator를 실행한다.
- 메모리 snapshot을 먼저 교체한다.
- PostgreSQL 모드에서는 persistence queue에 저장 작업을 넣고 바로 반환한다.
- queue 실패는 `console.error`로만 남는다.

문제:

- API는 성공했지만 PostgreSQL 저장은 실패할 수 있다.
- 서버 재시작 시 메모리에만 있던 변경이 사라질 수 있다.
- `cost.service.ts` 등에서 `databaseService.write()` 내부에서 `auditLogService.record()`를 호출하는 중첩 write가 있다.
- 중첩 write는 내부 audit write가 외부 draft 덮어쓰기에 의해 사라질 수 있다.
- 파일 모드는 `writeFileSync` 직접 덮어쓰기라 프로세스 중단 시 JSON이 깨질 수 있다.
- 파일 경로가 `process.cwd()` 기준이라 실행 위치에 따라 다른 database.json을 사용할 수 있다.

## 이번 단계의 목표

1. PostgreSQL 모드에서 쓰기 API가 DB 저장 성공 이후에만 성공을 반환하도록 한다.
2. PostgreSQL 저장 실패를 조용히 묻지 않고 호출자에게 전달한다.
3. 중첩 `databaseService.write()`로 audit log가 유실되는 패턴을 제거한다.
4. 파일 저장 모드는 개발용 fallback으로 유지하되 경로와 atomic write를 정리한다.
5. 기존 API 응답 shape와 주요 서비스 동작은 유지한다.
6. row-level persistence, 정규 컬럼 테이블, 클라우드 DB 연결은 이번 단계에서 하지 않는다.

## 이번 단계에서 하지 말아야 할 것

- 관리자 인증, CORS, MASTER_KEY 필수화 같은 보안 작업은 하지 않는다.
- 모든 테이블을 정규화하지 않는다.
- `TRUNCATE + 전체 재삽입`을 완전히 제거하지 않는다. 그것은 2단계 범위다.
- 주문/광고/비용/매핑의 비즈니스 규칙을 바꾸지 않는다.
- rawPayload retention 정책을 되돌리지 않는다.
- 파일 저장 모드를 제거하지 않는다. 단, 개발 전용 fallback임을 명확히 한다.

## 집/회사 DB 동기화 보존 조건

이 단계 이후에도 `scripts/db-export.mjs`와 `scripts/db-import.mjs` 기반의 집/회사 전체 DB snapshot 이동은 계속 동작해야 한다.

공통 원칙은 `0529_DB개선_공통_집회사동기화_필수조건.md`를 따른다.

주의:

- `writeCommitted()`를 도입해도 명시적 import 도구는 유지한다.
- import 시 백엔드 중지 조건은 계속 필요하다.
- 이번 단계에서 파일 저장 경로를 정리하더라도 PostgreSQL snapshot export/import 경로를 깨면 안 된다.

## 관련 파일

주요 수정 파일:

- `apps/backend/src/database.service.ts`
- `apps/backend/src/audit-log.service.ts`
- `apps/backend/src/cost.service.ts`
- `apps/backend/src/operation.service.ts`
- `apps/backend/src/order-sync.service.ts`
- `apps/backend/src/ads.service.ts`
- `apps/backend/src/store.service.ts`
- `apps/backend/src/sales-unit.service.ts`
- `apps/backend/src/credential.service.ts`
- `apps/backend/src/fake-purchase.service.ts`
- `apps/backend/src/campaign-mapping.service.ts`
- `apps/backend/src/order-mapping.service.ts`
- `apps/backend/src/mapping-seed.service.ts`
- `apps/backend/src/run-tests.ts`
- `README.md`

참고 파일:

- `보완필요사항.html`
- `0529수정계획.md`
- `packages/shared/src/types.ts`

## 권장 설계

### 1. DatabaseService에 commit 보장 write API 추가

기존 `write<T>(mutator): T`는 동기 API라 PostgreSQL commit 완료를 표현할 수 없다. 바로 모든 호출부를 바꾸기 어렵다면, 먼저 새 API를 추가하고 고위험 쓰기부터 전환한다.

권장 API:

```ts
async writeCommitted<T>(mutator: (draft: DatabaseShape) => T): Promise<T>
```

동작:

1. 이전 write queue가 끝날 때까지 기다린다.
2. 최신 `this.database`를 clone한다.
3. mutator를 실행한다.
4. PostgreSQL 모드면 `persistSnapshotToPostgres(draft)`를 await한다.
5. file 모드면 atomic write를 완료한다.
6. 저장 성공 후에만 `this.database = draft`로 교체한다.
7. 실패하면 `this.database`를 바꾸지 않고 error를 throw한다.

주의:

- mutator 결과값은 저장 성공 후 반환한다.
- PostgreSQL 저장 실패 시 호출 API는 5xx 또는 기존 Nest error flow로 실패해야 한다.
- 기존 `write()`는 테스트와 아직 전환하지 않은 호출부를 위해 일시 유지한다.
- 장기적으로 `write()`는 deprecated 처리하고 전부 `writeCommitted()`로 바꾼다.

### 2. persistence queue 실패 상태를 추적한다

`DatabaseService`에 다음 상태를 둔다.

```ts
private lastPersistenceError: {
  message: string;
  occurredAt: string;
} | null = null;
```

필요 메서드:

```ts
getPersistenceStatus(): {
  storageMode: "postgres" | "file";
  hasPendingWrite: boolean;
  lastPersistenceError: { message: string; occurredAt: string } | null;
}
```

이번 단계에서는 health endpoint 확장이 가능하면 추가한다. 단, 보안 작업은 하지 않는다.

### 3. 파일 저장 모드를 atomic write로 바꾼다

현재는 `writeFileSync(filePath, json)`로 직접 덮는다. 다음 방식으로 바꾼다.

1. 같은 디렉터리에 `database.json.tmp-${process.pid}-${Date.now()}`로 쓴다.
2. write 완료 후 rename으로 `database.json`을 교체한다.
3. 실패 시 tmp 파일을 정리한다.

경로는 `process.cwd()`에 과하게 의존하지 않도록 정리한다.

권장:

- `DATA_DIR`이 있으면 그 경로를 우선한다.
- 없으면 repository root 기준 `apps/backend/data`를 사용한다.
- `process.cwd()`가 `apps/backend`인지 repository root인지에 따라 중복 경로가 생기지 않게 helper를 둔다.

예시 helper:

```ts
function resolveDataDir(): string {
  if (process.env.DATA_DIR) return resolve(process.cwd(), process.env.DATA_DIR);
  const cwd = process.cwd();
  if (basename(cwd) === "backend") return resolve(cwd, "data");
  return resolve(cwd, "apps", "backend", "data");
}
```

### 4. AuditLogService를 중첩 write 없이 사용할 수 있게 바꾼다

현재 `auditLogService.record()`는 내부에서 `databaseService.write()`를 호출한다. 이것 때문에 다른 `write()` 내부에서 호출하면 audit log가 유실될 수 있다.

권장 API:

```ts
createAuditLog(params): AuditLog
appendToDraft(draft: DatabaseShape, params): AuditLog
async recordCommitted(params): Promise<AuditLog>
```

구현 방향:

- `createAuditLog()`는 순수하게 객체만 만든다.
- `appendToDraft()`는 이미 열린 draft에 push한다.
- `recordCommitted()`는 단독 audit 기록이 필요할 때 `databaseService.writeCommitted()`를 사용한다.
- 기존 `record()`는 임시 compatibility wrapper로 두되, 내부적으로 `recordCommitted()` 사용을 검토한다.

중요:

- 중첩 write가 있는 곳은 반드시 `appendToDraft()`로 바꾼다.
- write 밖에서 별도 audit를 남기는 곳은 `await recordCommitted()`로 바꾸거나, 도메인 write와 audit write를 하나의 `writeCommitted()` 안으로 묶는다.

### 5. 중첩 write 제거 대상

우선 확인할 대상:

- `apps/backend/src/cost.service.ts`
  - 비용 설정 create/update/delete 내부 audit
- `apps/backend/src/operation.service.ts`
  - operation 성공 후 audit
- `apps/backend/src/order-sync.service.ts`
  - sync 성공/실패 audit
- `apps/backend/src/ads.service.ts`
  - preview/confirm/delete audit
- `apps/backend/src/store.service.ts`
- `apps/backend/src/sales-unit.service.ts`
- `apps/backend/src/credential.service.ts`
- `apps/backend/src/fake-purchase.service.ts`
- `apps/backend/src/mapping-seed.service.ts`

변경 원칙:

- "도메인 변경 + audit log"는 가능하면 같은 draft 안에서 처리한다.
- operation처럼 executor 뒤에 상태 업데이트와 audit가 이어지는 경우, 성공 상태 업데이트와 audit push를 같은 `writeCommitted()` 안에서 처리한다.
- 실패 기록도 DB 저장 실패를 삼키지 않는다.

### 6. 서비스 메서드 async 전환

`writeCommitted()`를 쓰려면 많은 서비스 메서드가 async가 된다. Nest controller는 Promise를 반환할 수 있으므로 가능하다.

우선순위:

1. 실제 운영 데이터 쓰기 API
2. operation 상태 변경
3. audit log 기록
4. bootstrap/seed 류

전환할 때 주의:

- controller에서 반환값을 그대로 반환해도 Promise면 Nest가 처리한다.
- 기존 테스트가 동기 호출이면 `runAsync`로 전환한다.
- `OperationService.enqueue()`는 즉시 operation record를 반환해야 하므로 초기 enqueue write는 commit 완료 후 operation을 반환하거나, enqueue 자체를 async로 바꾼다.

권장:

```ts
async enqueue(...): Promise<OperationRecord>
```

이 변경이 너무 크면 1단계 안에서 `enqueueCommitted()`를 추가하고 호출부를 단계적으로 바꾼다.

## 구현 순서

1. `DatabaseService.writeCommitted()`와 atomic file write helper를 추가한다.
2. PostgreSQL persistence 실패가 throw되도록 테스트 가능한 구조로 정리한다.
3. `AuditLogService`에 `appendToDraft()`와 `recordCommitted()`를 추가한다.
4. `cost.service.ts`의 중첩 write를 먼저 제거한다.
5. `operation.service.ts`, `order-sync.service.ts`, `ads.service.ts`의 operation/audit 쓰기를 commit 보장 흐름으로 바꾼다.
6. 나머지 서비스의 단순 audit write를 정리한다.
7. `run-tests.ts`에 저장 실패, 중첩 write, 파일 경로/atomic write 테스트를 추가한다.
8. README에 "쓰기 성공은 DB commit 성공을 의미한다"는 운영 메모를 추가한다.

## 테스트 지침

필수 테스트:

1. `writeCommitted()`에서 PostgreSQL persistence가 실패하면 error가 호출자에게 전달된다.
2. persistence 실패 시 메모리 snapshot이 성공한 것처럼 바뀌지 않는다.
3. 같은 draft 안에서 도메인 변경과 audit log가 함께 저장된다.
4. `cost.service.ts`의 비용 생성/수정/삭제 audit log가 유실되지 않는다.
5. operation 성공 상태와 audit log가 함께 남는다.
6. 파일 모드 atomic write가 임시 파일을 만들고 최종 파일로 교체한다.
7. `DATA_DIR` 미설정 시 저장 경로가 실행 위치에 따라 중복되지 않는다.

검증 명령:

```powershell
npm.cmd run typecheck --workspace @patima/backend
npm.cmd run test --workspace @patima/backend
npm.cmd run lint
```

가능하면 PostgreSQL local 환경에서 다음도 확인한다.

```powershell
npm.cmd run build --workspace @patima/backend
npm.cmd run start --workspace @patima/backend
```

## 완료 기준

- PostgreSQL 모드에서 쓰기 성공 응답은 DB 저장 완료를 의미한다.
- PostgreSQL 저장 실패가 `console.error`에만 묻히지 않는다.
- 중첩 `write()`로 audit log가 유실될 수 있는 주요 패턴이 제거된다.
- 파일 저장 모드가 atomic write를 사용한다.
- 파일 저장 경로가 실행 위치에 따라 중복 생성되지 않는다.
- backend test, backend typecheck, 전체 lint가 통과한다.

## 다음 단계로 넘길 내용

1단계 완료 후 2단계에서 할 일:

- `TRUNCATE + 전체 재삽입` 제거
- PostgreSQL JSONB row-level upsert/delete 도입
- 주요 JSONB expression index 또는 파생 컬럼 index 추가
- 장기적으로 정규 컬럼 테이블 전환 검토

## 다음 세션 실행 프롬프트

```text
너는 Codex다. 작업 경로는 C:\Users\seong\Desktop\workspace\patima-naver-ss 이다.

반드시 먼저 0529_DB개선_1단계_저장안정화.md 파일을 끝까지 읽고, 보안/인증 작업은 제외한 채 DB 저장 안정화만 구현해라.

이번 목표:
- PostgreSQL 쓰기 성공 응답이 실제 DB commit 성공을 의미하게 만든다.
- 저장 실패를 console error로만 숨기지 않는다.
- 중첩 databaseService.write() 때문에 audit log가 유실될 수 있는 패턴을 제거한다.
- 파일 저장 모드는 atomic write와 안정적인 경로를 사용하게 만든다.
- row-level persistence와 DB 정규화는 이번 단계에서 하지 않는다.

작업 후 다음을 실행해라:
- npm.cmd run typecheck --workspace @patima/backend
- npm.cmd run test --workspace @patima/backend
- npm.cmd run lint

최종 응답에는 변경 파일, 저장 실패 처리 방식, 중첩 write 제거 방식, 테스트 결과를 포함해라.
```
