# 2026-04-16 수정 계획 2 — 매핑/주문 페이지 UX 개선

> 매핑 관리·주문 조회 페이지에서 반복적으로 불편한 UX 4가지를 개선한다.

---

## 작업 목록

| # | 요약 | 영향 파일 |
|---|---|---|
| 1 | 매핑 관리 — 주문 시그니처 박스에 "미매핑만 보기" 필터 버튼 | `mappings-view.tsx` |
| 2 | 매핑 관리 — 주문 시그니처 박스에 "주문 0건 숨기기" 토글 | `mappings-view.tsx` |
| 3 | 매핑 관리 — 매핑 저장 후 스크롤 위치 보존 (모든 스크롤 컨테이너) | `mappings-view.tsx` |
| 4 | 주문 데이터 조회 — 날짜 필터를 단일 날짜 선택으로 변경 | `orders-view.tsx` |

---

## 작업 1. 주문 시그니처 박스 — "미매핑만 보기" 필터 버튼

### 현재 상태

`mappings-view.tsx`의 주문 시그니처 박스는 모든 상태(MAPPED / UNMAPPED / CONFLICT)의 시그니처를 한꺼번에 보여준다. 매핑 작업 시 UNMAPPED 항목을 찾으려면 긴 목록을 스크롤해야 한다.

### 변경 내용

광고 비용 박스에 이미 구현된 `hideZeroCostAdRows` 체크박스 패턴을 그대로 따른다.

**상태 변수 추가**:
```tsx
const [showOnlyUnmapped, setShowOnlyUnmapped] = useState(false);
```

**필터링 로직**:
```tsx
const visibleSignatures = showOnlyUnmapped
  ? data.signatures.filter((sig) => sig.mappingStatus === "UNMAPPED" || sig.mappingStatus === "CONFLICT")
  : data.signatures;

const hiddenMappedCount = data.signatures.length - visibleSignatures.length;
```

**UI** — 주문 시그니처 박스 헤더 영역(기존 필터/검색 옆)에 체크박스 추가:
```tsx
<label className="flex items-center gap-1.5 text-sm">
  <input
    type="checkbox"
    checked={showOnlyUnmapped}
    onChange={(e) => setShowOnlyUnmapped(e.target.checked)}
  />
  미매핑/충돌만 보기
  {showOnlyUnmapped && hiddenMappedCount > 0 && (
    <span className="text-ink/40">({hiddenMappedCount}개 숨김)</span>
  )}
</label>
```

**참고**: 광고 비용 박스의 `hideZeroCostAdRows` 구현 위치는 `mappings-view.tsx` 라인 228 (state), 라인 964-967 (UI). 동일한 패턴을 주문 시그니처 섹션에 복제.

---

## 작업 2. 주문 시그니처 박스 — "주문 0건 숨기기" 토글

### 현재 상태

주문이 0건인 시그니처도 목록에 표시된다. 광고 비용 박스에는 이미 `hideZeroCostAdRows`로 비용 0원 항목을 숨기는 기능이 있다.

### 변경 내용

**상태 변수 추가**:
```tsx
const [hideZeroOrderSignatures, setHideZeroOrderSignatures] = useState(true);  // 기본 true
```

**필터링 로직** — 작업 1의 `visibleSignatures`에 추가 필터 체이닝:
```tsx
const filteredSignatures = (() => {
  let result = data.signatures;
  if (showOnlyUnmapped) {
    result = result.filter((sig) => sig.mappingStatus === "UNMAPPED" || sig.mappingStatus === "CONFLICT");
  }
  if (hideZeroOrderSignatures) {
    result = result.filter((sig) => sig.orderCount > 0);  // 실제 필드명은 코드에서 확인 필요
  }
  return result;
})();

const hiddenZeroOrderCount = data.signatures.filter((sig) => sig.orderCount === 0).length;
```

**UI** — 작업 1의 체크박스 옆에 나란히 배치:
```tsx
<label className="flex items-center gap-1.5 text-sm">
  <input
    type="checkbox"
    checked={hideZeroOrderSignatures}
    onChange={(e) => setHideZeroOrderSignatures(e.target.checked)}
  />
  주문 0건 숨기기
  {hideZeroOrderSignatures && hiddenZeroOrderCount > 0 && (
    <span className="text-ink/40">({hiddenZeroOrderCount}개 숨김)</span>
  )}
</label>
```

**참고**: `sig.orderCount` 필드명은 실제 API 응답 shape에 맞춰야 함. `mappings-view.tsx`에서 시그니처 데이터 구조를 확인하고 적절한 필드 사용. 광고 비용 박스의 `hideZeroCostAdRows` 패턴(라인 228, 964-967)과 완전히 동일한 구조.

---

## 작업 3. 매핑 저장 후 스크롤 위치 보존

### 현재 상태

매핑을 저장하면 `startRefresh(() => router.refresh())` (`mappings-view.tsx` 라인 355-360)가 호출되면서 컴포넌트가 리렌더되고 스크롤이 맨 위로 초기화된다.

스크롤 컨테이너는 최소 2개:
- 주문 시그니처 박스: `max-h-[70vh] overflow-y-auto` (라인 616 부근)
- 광고 비용 박스: `max-h-[70vh] overflow-y-auto` (라인 926 부근)

### 변경 내용

`router.refresh()` 호출 전에 스크롤 위치를 저장하고, 리렌더 완료 후 복원하는 패턴.

**1단계: 스크롤 컨테이너에 ref 부착**:
```tsx
const orderScrollRef = useRef<HTMLDivElement>(null);
const adScrollRef = useRef<HTMLDivElement>(null);
// 추가 스크롤 컨테이너가 있으면 동일하게 ref 추가
```

각 스크롤 컨테이너 div에 ref 연결:
```tsx
<div ref={orderScrollRef} className="max-h-[70vh] overflow-y-auto pr-1">
  {/* 주문 시그니처 테이블 */}
</div>

<div ref={adScrollRef} className="max-h-[70vh] overflow-y-auto pr-1">
  {/* 광고 비용 테이블 */}
</div>
```

**2단계: 스크롤 저장/복원 유틸**:
```tsx
const scrollPositions = useRef<Map<string, number>>(new Map());

const saveScrollPositions = () => {
  if (orderScrollRef.current) {
    scrollPositions.current.set("order", orderScrollRef.current.scrollTop);
  }
  if (adScrollRef.current) {
    scrollPositions.current.set("ad", adScrollRef.current.scrollTop);
  }
};

const restoreScrollPositions = () => {
  requestAnimationFrame(() => {
    if (orderScrollRef.current) {
      orderScrollRef.current.scrollTop = scrollPositions.current.get("order") ?? 0;
    }
    if (adScrollRef.current) {
      adScrollRef.current.scrollTop = scrollPositions.current.get("ad") ?? 0;
    }
  });
};
```

**3단계: `refreshWithMessage` 수정** (라인 355-360):
```tsx
const refreshWithMessage = (target: "order" | "campaign" | "ad", message: string) => {
  if (target === "order") setOrderSuccess(message);
  else if (target === "campaign") setCampaignSuccess(message);
  else setAdSuccess(message);

  saveScrollPositions();
  startRefresh(() => {
    router.refresh();
  });
};
```

**4단계: transition 완료 후 복원**:
```tsx
useEffect(() => {
  if (!isRefreshing) {
    restoreScrollPositions();
  }
}, [isRefreshing]);
```

`isRefreshing`은 이미 `useTransition`에서 나오는 값 (라인 355). transition이 끝나면(`isRefreshing` → false) 스크롤 복원.

**주의**: `requestAnimationFrame`은 DOM이 실제로 업데이트된 후 실행되도록 보장. 만약 복원이 안 되면 `setTimeout(restore, 0)`으로 대체.

---

## 작업 4. 주문 데이터 조회 — 날짜 필터 단일 선택으로 변경

### 현재 상태

`orders-view.tsx` 라인 199-218에 `<input type="date">` 2개(dateFrom, dateTo)로 기간을 선택하는 구조. 네이버 커머스 API가 하루 초과 기간 요청을 거부하므로 실질적으로 의미 없는 UI.

### 변경 내용

dateFrom/dateTo 2개 필드를 **단일 date 필드**로 교체. 날짜 선택 즉시 필터 적용 (별도 "적용" 버튼 불필요).

**상태 변수 변경**:
```tsx
// 이전
const [filters, setFilters] = useState({ dateFrom: "", dateTo: "", ... });

// 이후 — dateFrom만 사용, dateTo 제거
// 또는 selectedDate라는 새 변수로 교체
const [filters, setFilters] = useState({ date: "", ... });
```

**UI 변경** (라인 199-218 영역):
```tsx
{/* 이전: dateFrom + dateTo 두 개 */}
{/* 이후: 단일 date input */}
<div className="flex items-center gap-2">
  <label className="text-sm font-medium">조회 날짜</label>
  <input
    type="date"
    value={filters.date}
    onChange={(e) => {
      const newDate = e.target.value;
      setFilters((prev) => ({ ...prev, date: newDate }));
      // 날짜 선택 즉시 applyFilters 호출 — 별도 버튼 불필요
      applyFiltersWithDate(newDate);
    }}
    className="..."  // 기존 스타일 유지
  />
</div>
```

**`applyFilters` 수정** (라인 60-72):

기존에 `dateFrom`/`dateTo`를 쿼리 파라미터로 보내는 로직을 `date` 하나로 변경. 백엔드에 보낼 때는 `dateFrom = date`, `dateTo = date` (같은 값)로 보내면 백엔드 수정 없이 동작.

```tsx
const applyFiltersWithDate = (date: string) => {
  const params = new URLSearchParams();
  if (date) {
    params.set("dateFrom", date);
    params.set("dateTo", date);  // 동일 날짜 — 백엔드 변경 불필요
  }
  // ... 나머지 필터 파라미터 ...
  router.push(`?${params.toString()}`);
};
```

**참고**: 광고 업로드(`ads-view.tsx`)에 있는 드롭다운 방식을 참고. 다만 주문은 날짜 범위가 넓을 수 있으니 `<input type="date">`가 더 적합. 핵심은 **2개 → 1개**, **선택 즉시 적용**.

---

## 변경 파일 목록

| # | 파일 | 변경 요지 |
|---|---|---|
| 1 | `apps/frontend/components/mappings/mappings-view.tsx` | 작업 1~3 (미매핑 필터, 주문0건 숨기기, 스크롤 보존) |
| 2 | `apps/frontend/components/orders/orders-view.tsx` | 작업 4 (날짜 필터 단일화) |

---

## 작업 순서

1. **작업 3 먼저** (스크롤 보존) — 다른 작업 테스트할 때도 스크롤이 유지되어야 편하므로.
2. **작업 1** (미매핑 필터) + **작업 2** (주문 0건 숨기기) — 같은 파일, 같은 영역이므로 동시 작업.
3. **작업 4** (날짜 필터 단일화) — 별도 파일이므로 독립 작업.
4. **검증**: dev 서버에서 매핑 페이지, 주문 페이지 각각 수동 테스트.

---

## 수용 조건

- 매핑 관리 페이지에서 "미매핑/충돌만 보기" 체크 시 MAPPED 시그니처가 숨겨짐.
- "주문 0건 숨기기" 체크 시 주문이 없는 시그니처가 숨겨짐 (숨긴 개수 표시).
- 매핑 저장 후 주문 시그니처 박스·광고 비용 박스 모두 스크롤 위치가 유지됨.
- 주문 데이터 조회에서 날짜를 하나 클릭하면 즉시 해당 날짜 데이터가 표시됨.
- 기존 기능(매핑 저장, 배치 매핑, 광고 매핑, 페이지네이션 등) 회귀 없음.

---

## 비파괴 원칙

- 백엔드 API 변경 없음 (dateFrom/dateTo 동일 값 전달로 호환).
- 기존 컴포넌트 구조 유지 — 필터/상태 변수만 추가.
- 광고 비용 박스의 기존 `hideZeroCostAdRows` 로직 건드리지 않음.
- 기존 코드 스타일(Tailwind, lucide-react, 인라인 스타일 패턴) 준수.
