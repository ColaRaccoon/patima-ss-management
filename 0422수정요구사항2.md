# 2026-04-22 수정 요구사항 ②: 매핑 관리 탭 광고 섹션 "미매핑/충돌만 보기" 필터 추가

## 1. 배경

매핑 관리 탭(`/mappings`)의 **주문 시그니처 섹션**에는 이미 "미매핑/충돌만 보기" 체크박스가 있어서, 매핑이 필요한 항목만 빠르게 좁혀 볼 수 있다. 반면 동일 화면 우측의 **광고 캠페인 섹션**에는 동일한 필터가 없어서, 매핑되지 않았거나 충돌 상태인 광고 항목만 골라내려면 검색어로 우회해야 한다.

주문 쪽과 광고 쪽은 동일한 매핑 워크플로우(MAPPED / UNMAPPED / CONFLICT)를 공유하므로, 광고 섹션에도 동일한 필터를 대칭으로 제공해 사용자 경험을 맞춘다.

## 2. 요구사항

### 2.1. 광고 섹션에 "미매핑/충돌만 보기" 체크박스 추가

- 위치: 매핑 관리 탭의 **광고 캠페인 섹션 툴바** 내부. 기존 "0원 캠페인 숨기기" 체크박스와 나란히 배치(주문 섹션과 대칭 구조).
- 라벨: **"미매핑/충돌만 보기"** (주문 섹션과 동일한 문구 사용)
- 기본값: **꺼짐(off)** — 주문 섹션과 동일
- 동작: 체크 시 `mappingStatus === "UNMAPPED"` 또는 `mappingStatus === "CONFLICT"` 인 행만 표시. 체크 해제 시 전체(필터 기본 상태)로 복귀.

### 2.2. 주문 섹션 필터와 독립적으로 동작

- 주문 섹션의 "미매핑/충돌만 보기" 상태와 **완전히 독립**. 한쪽을 토글해도 다른 쪽에는 영향을 주지 않아야 한다.
- 내부 상태도 별도의 state로 분리한다(예: 주문 측 `showOnlyUnmappedOrders`, 광고 측 `showOnlyUnmappedAds`). 기존 state명 `showOnlyUnmapped`가 있다면 주문 전용임을 드러내도록 리네이밍하는 것을 권장.

### 2.3. 광고 섹션 필터 체인 내 배치 순서

광고 섹션은 이미 다음 필터 체인을 가지고 있다.

1. `hideZeroCostAdRows` (0원 캠페인 숨기기) — 전처리 단계
2. 검색어(`adSearch`)로 문자열 필터
3. (신규) `showOnlyUnmappedAds` — `mappingStatus` 기준 필터

신규 필터는 기존 `hideZeroCostAdRows` / 검색 필터 **다음에 AND 조건**으로 붙여, 세 조건이 모두 만족되는 행만 남긴다. 순서 자체는 결과에 영향 없으나 구현 일관성을 위해 주문 섹션의 필터 체인과 같은 패턴으로 둔다.

### 2.4. "필터로 숨겨진 N건" 카운트 대칭 추가

주문 섹션 하단에는 `필터로 숨겨진 N건` 카운트가 표시된다(예: `hiddenSignaturesCount`). 광고 섹션에도 동일한 카운트를 추가해 필터 적용 후 몇 건이 숨겨졌는지 보여준다(예: `hiddenAdCostsCount`).

- 표시 조건: 숨겨진 건수가 1 이상일 때만
- 표시 위치: 광고 섹션 기존 `조회 N건 / 선택 N건` 라인 옆, 주문 섹션과 동일한 레이아웃

### 2.5. 선택 상태 처리

필터로 인해 화면에서 사라진 행이 이미 선택되어 있을 경우, 선택 상태는 유지하되 "검색으로 숨겨진 선택 N건" 안내가 기존에 주문 섹션에서 표시되는 방식과 동일하게 광고 섹션에도 반영한다. 이미 `selectedAdCostRows`, `visibleSelectedAdCostCount` 값이 계산되고 있으므로 주문 섹션의 `<span>검색으로 숨겨진 선택 ...건</span>` 블록을 광고 섹션에 대칭 추가하면 된다.

## 3. 수용 기준

1. 매핑 관리 탭 광고 섹션 툴바에 "미매핑/충돌만 보기" 체크박스가 보인다.
2. 광고 섹션 체크박스 토글이 주문 섹션 체크박스 상태에 **영향을 주지 않는다**(양방향 독립).
3. 체크 시 광고 테이블에 `UNMAPPED` 또는 `CONFLICT` 상태 행만 남는다. 해제 시 전체로 복귀한다.
4. 기존 `hideZeroCostAdRows`, 검색어 필터와 함께 AND로 조합되어 올바르게 동작한다.
5. 필터로 숨겨진 건수가 "필터로 숨겨진 N건" 형태로 카운트 라인에 반영된다.
6. 필터 때문에 화면에서 사라진 선택 행이 있을 때 "검색으로 숨겨진 선택 N건"이 정상 표시된다.
7. 주문 섹션의 기존 동작, 검색어 필터, 배치 처리 로직은 변경되지 않는다.

## 4. 범위 외 (Out of Scope)

- 백엔드 API 변경 (이미 `CampaignCostListItem.mappingStatus`가 응답에 포함되어 있어 불필요)
- 필터 상태의 URL query 또는 서버사이드 영속화
- 주문 섹션 필터 옵션 확장(본 수정 스코프는 광고 섹션에 한정)
- 새로운 매핑 상태 도입이나 `MAPPED/UNMAPPED/CONFLICT` 외 필터 조건 추가

## 5. 복잡도·파급도 평가

- 수정 대상 파일: **`apps/frontend/components/mappings/mappings-view.tsx` 1개**
- 타입/백엔드/테스트 수정 **불필요**
- 주문 섹션의 기존 패턴(`showOnlyUnmapped`, `matchesUnmapped`, 체크박스 JSX, `hiddenSignaturesCount`)을 광고 섹션에 그대로 미러링하면 되는 단순 작업
- 난이도: 하(low). 주의할 유일한 포인트는 주문/광고 state를 **별도 변수**로 분리하는 것
