# 0529 수정계획 - 저장 구조 개선 1차 작업 지침서

## 이 문서의 목적

이 문서는 다음 세션이 이 파일 하나만 보고도 작업 목표와 범위를 정확히 이해하고 바로 구현을 시작할 수 있도록 만든 작업 지침서다.

이번 작업의 핵심은 `보완필요사항.html` 전체 중에서도 가장 시급한 저장 구조 개선을 1차로 떼어내는 것이다. 전체 DB 정규화, 클라우드 DB 전환, 인증/인가 개선까지 한 번에 하지 않는다. 이번 1차 작업은 장기 운영에서 DB와 백업을 급격히 키우는 주문 원본 `rawPayload` 보관 방식을 바꾸는 데 집중한다.

## 현재 상황 요약

프로젝트 경로:

`C:\Users\seong\Desktop\workspace\patima-naver-ss`

관련 파일:

- `보완필요사항.html`
- `apps/backend/src/database.service.ts`
- `apps/backend/src/order-sync.service.ts`
- `apps/backend/src/naver-commerce.service.ts`
- `apps/backend/src/profit.service.ts`
- `apps/backend/src/helpers.ts`
- `packages/shared/src/types.ts`
- `apps/backend/src/run-tests.ts`
- `.env.example`
- `README.md`

현재 문제:

- PostgreSQL을 사용해도 실제 저장 방식은 `id + payload JSONB` 기반의 전체 스냅샷에 가깝다.
- `DatabaseService.write()`는 전체 DB 스냅샷을 `JSON.parse(JSON.stringify(...))`로 복제한다.
- PostgreSQL 저장 시 모든 JSONB 테이블을 `TRUNCATE` 후 전체 재삽입한다.
- 주문 동기화 시 네이버 API 원본 응답 전체가 `OrderRecord.rawPayload`, `OrderItem.rawPayload`에 저장된다.
- 최근 백업 기준으로 `order_items` 26,132건, `orders` 18,768건 수준인데 백업 파일이 약 349MB까지 커졌다.
- 백업 크기와 메모리 사용량 증가는 주문 수보다 `rawPayload` 크기에 더 크게 영향을 받는다.

중요한 판단:

- 초기 MVP 단계에서는 네이버 API 응답 구조를 완전히 알기 어려워 `rawPayload` 전체 저장이 합리적이었다.
- 실제로 `migrations/phase1-backfill-optioncode.sql`, `migrations/phase2-backfill-option-manage-code.sql`처럼 나중에 `rawPayload`에서 필드를 다시 꺼낸 흔적이 있다.
- 하지만 지금은 필요한 핵심 필드가 어느 정도 식별되었으므로 원본 전체를 영구 보관하는 방식은 운영 단계에 맞지 않는다.

## 이번 1차 작업의 목표

이번 작업의 목표는 다음과 같다.

1. 주문/주문상품 핵심 필드는 장기 보관한다.
2. 네이버 원본 `rawPayload`는 최근 기간만 운영 DB에 보관한다.
3. 보관 기간이 지난 주문/주문상품의 `rawPayload`만 `null`로 정리한다.
4. 주문 row, 주문상품 row, 매출/수수료/배송비/매핑/옵션 관련 핵심 필드는 절대 삭제하지 않는다.
5. 마진 계산, 주문 목록, 매핑, 비용 계산이 `rawPayload` 없이도 유지되도록 테스트한다.
6. 추후 클라우드 DB 또는 row-level persistence 전환을 쉽게 하기 위한 기반을 만든다.

## 이번 작업에서 하지 말아야 할 것

이번 1차 작업에서 하지 않을 것:

- 전체 `DatabaseService`를 완전한 repository/ORM 구조로 갈아엎지 않는다.
- 모든 JSONB 테이블을 정규 컬럼 테이블로 한 번에 바꾸지 않는다.
- 집/회사 DB 동기화 또는 클라우드 DB 연결을 이번 작업에서 하지 않는다.
- 주문 또는 주문상품 row 자체를 30일 뒤 삭제하지 않는다.
- 마감된 과거 마진 결과 테이블을 완전히 새로 구현하는 것을 필수 목표로 삼지 않는다.
- 인증/인가, CORS, mock fallback 제거는 이번 작업의 직접 범위가 아니다.

이유:

저장 구조 전체를 한 번에 바꾸면 주문 동기화, 광고 업로드, 매핑, 원가 스냅샷, 마진 계산, 백업 스크립트가 동시에 흔들린다. 이번 작업은 가장 큰 데이터 증가 원인인 `rawPayload`를 먼저 통제하는 안전한 1차 패치다.

## 목표 보관 정책

권장 보관 정책:

| 데이터 | 운영 DB 보관 | 장기 보관 | 이번 작업 |
|---|---:|---:|---|
| `OrderRecord.rawPayload` | 최근 30~90일 | archive 또는 삭제 | 보관 기간 초과 시 `null` |
| `OrderItem.rawPayload` | 최근 30~90일 | archive 또는 삭제 | 보관 기간 초과 시 `null` |
| 주문 핵심 필드 | 장기 | 장기 | 유지 |
| 주문상품 핵심 필드 | 장기 | 장기 | 유지 |
| 원가 스냅샷 | 장기 | 장기 | 변경 없음 |
| 매핑 이력 | 장기 | 장기 | 변경 없음 |
| 일별 마진 결과 | 장기 | 장기 | 이번 작업에서는 설계 메모만 가능 |

기본값 제안:

- 환경변수: `ORDER_RAW_PAYLOAD_RETENTION_DAYS`
- 기본값: `90`
- 허용값: `0` 이상 정수
- `0`의 의미: 새 동기화 주문도 원본 payload를 장기 저장하지 않고 가능한 즉시 `null` 처리
- 운영 권장값: 처음에는 `90`, 데이터 증가를 확인한 뒤 `30`까지 축소 가능

## 반드시 장기 보관해야 하는 주문 핵심 필드

`OrderRecord`에서 장기 보관해야 할 필드:

- `id`
- `storeId`
- `externalOrderId`
- `orderDatetime`
- `paymentDatetime`
- `orderStatus`
- `syncedAt`
- `createdAt`
- `updatedAt`

`OrderItem`에서 장기 보관해야 할 필드:

- `id`
- `orderId`
- `storeId`
- `productId`
- `orderSourceSignatureId`
- `canonicalSalesUnitId`
- `externalProductOrderId`
- `externalProductId`
- `optionCode`
- `optionManageCode`
- `packageNumber`
- `rawProductName`
- `rawOptionInfo`
- `normalizedProductName`
- `normalizedOptionInfo`
- `sourceSignature`
- `quantity`
- `productPaymentAmount`
- `totalProductAmount`
- `deliveryFeeAmount`
- `paymentCommission`
- `knowledgeShoppingSellingInterlockCommission`
- `saleCommission`
- `channelCommission`
- `orderDate`
- `paymentDate`
- `saleStatus`
- `orderStatus`
- `isCanceled`
- `isReturned`
- `createdAt`
- `updatedAt`

절대 하면 안 되는 일:

- `rawPayload`를 지우면서 `optionCode`, `optionManageCode`, `paymentCommission`, `deliveryFeeAmount` 같은 계산/매핑 필드를 같이 지우면 안 된다.
- `paymentDate`가 없는 주문을 무조건 오래된 주문으로 판단해 지우면 안 된다. 기준 날짜 fallback을 명확히 둔다.

## 구현 지침

### 1. 설정값 추가

환경변수:

`ORDER_RAW_PAYLOAD_RETENTION_DAYS`

추가 위치:

- `.env.example`
- `README.md`의 Environment 또는 운영 메모

권장 helper:

- 새 파일을 만들거나 기존 helper에 추가한다.
- 예: `apps/backend/src/raw-payload-retention.ts`

필요 함수 예시:

```ts
export const DEFAULT_ORDER_RAW_PAYLOAD_RETENTION_DAYS = 90;

export function getOrderRawPayloadRetentionDays(): number {
  const value = Number(process.env.ORDER_RAW_PAYLOAD_RETENTION_DAYS ?? DEFAULT_ORDER_RAW_PAYLOAD_RETENTION_DAYS);
  if (!Number.isFinite(value) || value < 0) {
    return DEFAULT_ORDER_RAW_PAYLOAD_RETENTION_DAYS;
  }
  return Math.floor(value);
}

export function getKstRetentionCutoffDate(days: number, now = new Date()): string {
  // YYYY-MM-DD, Asia/Seoul 기준
}
```

주의:

- 날짜 비교는 기존 코드처럼 `YYYY-MM-DD` 문자열 비교를 사용해도 된다.
- KST 기준이어야 한다.

### 2. rawPayload 보관 여부 결정

새 주문/주문상품을 저장할 때 `rawPayload`를 무조건 저장하지 말고, retention cutoff 기준으로 저장 여부를 결정한다.

판단 기준:

1. `paymentDate`가 있으면 `paymentDate`
2. 없으면 `orderDate`
3. 없으면 `orderDateTime`에서 날짜 추출
4. 없으면 현재 동기화 날짜 또는 `syncedAt` 기준

보관 여부:

- 기준 날짜가 cutoff 이상이면 `rawPayload` 유지
- cutoff보다 이전이면 `rawPayload: null`

적용 위치:

- `apps/backend/src/order-sync.service.ts`
- `performSync()` 내부에서 `OrderRecord`와 `OrderItem` payload를 만들 때

현재 코드 위치:

- 기존 주문 update: `existingOrder.rawPayload = entry.rawPayload`
- 신규 주문 create: `rawPayload: entry.rawPayload`
- 주문상품 payload: `rawPayload: entry.rawPayload`

변경 방향:

```ts
const retainedRawPayload = shouldRetainOrderRawPayload(entry, cutoffDate)
  ? entry.rawPayload
  : null;
```

그리고 `OrderRecord`, `OrderItem`에 동일하게 사용한다.

### 3. 기존 데이터 정리 함수 추가

이미 DB에 쌓여 있는 오래된 `rawPayload`도 정리해야 한다.

권장 함수:

```ts
function pruneExpiredOrderRawPayloads(
  draft: DatabaseShape,
  storeId: string,
  cutoffDate: string,
): {
  prunedOrderCount: number;
  prunedOrderItemCount: number;
}
```

정리 기준:

- `orders`: `paymentDatetime`, `orderDatetime`, `syncedAt`에서 날짜를 추출해 cutoff 이전이면 `rawPayload = null`
- `orderItems`: `paymentDate`, `orderDate`, `createdAt`에서 날짜를 추출해 cutoff 이전이면 `rawPayload = null`
- 이미 `rawPayload === null`이면 count하지 않는다.
- storeId가 다른 데이터는 건드리지 않는다.

호출 위치:

- `OrderSyncService.performSync()`의 성공 write 블록 끝부분
- 즉, 동기화 후 해당 store의 오래된 payload를 함께 정리한다.

작업 결과에 포함할 값:

```ts
rawPayloadRetentionDays
rawPayloadRetentionCutoffDate
rawPayloadPrunedOrderCount
rawPayloadPrunedOrderItemCount
```

### 4. 수동 정리 스크립트 추가 검토

가능하면 운영자가 한 번 실행할 수 있는 스크립트를 추가한다.

후보:

- `scripts/prune-order-raw-payloads.mjs`

단, 현재 저장 구조가 백엔드 서비스 로직에 많이 묶여 있으므로, 이번 작업에서 스크립트까지 구현이 과하면 생략해도 된다.

스크립트를 추가한다면:

- `DATABASE_URL` 기반 PostgreSQL JSONB payload를 읽는다.
- cutoff 이전 `orders`, `order_items` payload의 `rawPayload`만 `null`로 update한다.
- 실행 전 summary를 보여주고 `--yes` 없이는 실제 변경하지 않는다.
- 백엔드가 켜진 상태에서 실행하면 snapshot 덮어쓰기와 충돌할 수 있으므로 실행 전 백엔드 중지를 요구한다.

### 5. 타입 변경 여부

현재 `packages/shared/src/types.ts` 기준:

- `OrderRecord.rawPayload: Record<string, unknown> | null`
- `OrderItem.rawPayload: Record<string, unknown> | null`

이미 null 허용이므로 타입 변경이 필요 없을 가능성이 높다.

단, 새 메타 필드를 추가하고 싶다면 신중해야 한다.

가능한 추가 필드:

- `rawPayloadPrunedAt?: string | null`
- `rawPayloadRetentionCutoffDate?: string | null`

하지만 이번 1차 작업에서는 필드 추가 없이 `rawPayload: null`만으로 충분하다. 불필요한 타입 확장은 피한다.

### 6. 마진 계산 영향 확인

마진 계산은 `rawPayload`를 쓰면 안 된다.

확인할 파일:

- `apps/backend/src/helpers.ts`
- `apps/backend/src/profit.service.ts`
- `apps/backend/src/cost.service.ts`

확인해야 할 계산 필드:

- `productPaymentAmount`
- `totalProductAmount`
- `deliveryFeeAmount`
- `paymentCommission`
- `knowledgeShoppingSellingInterlockCommission`
- `saleCommission`
- `channelCommission`
- `quantity`
- `paymentDate`
- `saleStatus`
- `canonicalSalesUnitId`

테스트에서 `rawPayload: null`인 주문/주문상품으로도 마진 계산이 동일하게 동작해야 한다.

### 7. 테스트 추가

반드시 `apps/backend/src/run-tests.ts`에 테스트를 추가한다.

필수 테스트:

1. 오래된 주문/주문상품의 `rawPayload`만 `null` 처리된다.
2. 최근 주문/주문상품의 `rawPayload`는 유지된다.
3. `rawPayload`가 사라져도 `optionCode`, `optionManageCode`, 수수료, 배송비, 매출 필드는 유지된다.
4. `rawPayload: null`인 주문도 주문 목록 조회와 마진 계산이 깨지지 않는다.
5. prune은 storeId 범위를 지킨다.
6. 이미 `rawPayload: null`인 row를 다시 정리해도 count가 부풀지 않는다.

권장 테스트:

7. `ORDER_RAW_PAYLOAD_RETENTION_DAYS=0`일 때 새 동기화 row의 `rawPayload`가 저장되지 않는다.
8. 잘못된 환경변수 값은 기본값 90일로 fallback된다.
9. operation result에 prune count와 cutoff가 포함된다.

검증 명령:

```powershell
npm run test --workspace @patima/backend
npm run lint
```

환경에 따라 전체 `npm run lint`가 오래 걸리면 최소한 다음을 실행한다.

```powershell
npm run typecheck --workspace @patima/backend
npm run test --workspace @patima/backend
```

## 완료 기준

작업 완료 기준:

- 신규 주문 동기화 시 cutoff 이전 주문은 `rawPayload`를 저장하지 않는다.
- 기존 오래된 주문/주문상품의 `rawPayload`가 동기화 후 정리된다.
- 핵심 주문 필드는 삭제되지 않는다.
- 마진 계산과 주문/매핑 목록이 `rawPayload` 없이 동작한다.
- backend test가 통과한다.
- typecheck가 통과한다.
- `.env.example` 또는 README에 보관 기간 설정이 문서화된다.
- 작업 결과 요약에 실제 변경 파일과 테스트 결과가 포함된다.

## 다음 단계로 남길 사항

이번 1차 작업 이후 남길 후속 과제:

1. 일별 결과 테이블 도입
   - `daily_sales_unit_profit`
   - `daily_store_summary`
   - 계산식 버전, 원가 스냅샷 ID, 매핑 기준 시각 저장

2. PostgreSQL row-level persistence 전환
   - `TRUNCATE + 전체 재삽입` 제거
   - 변경 row만 `INSERT ... ON CONFLICT DO UPDATE`
   - 삭제 row만 `DELETE`
   - 필요한 인덱스 추가

3. 클라우드 DB 중앙화
   - 집/회사 각각 DB 동기화가 아니라 하나의 중앙 DB를 원본으로 사용
   - 단, 현재 snapshot 덮어쓰기 구조를 유지한 채 여러 백엔드가 같은 클라우드 DB를 바라보면 위험하다.
   - row-level persistence 전환 이후 클라우드 DB를 검토해야 한다.

4. 백업 구조 개선
   - rawPayload 없는 운영 DB 백업
   - 필요 시 rawPayload archive 별도 압축/암호화
   - restore 리허설

## 다음 세션에게 줄 실행 프롬프트

아래 프롬프트를 다음 세션 시작 시 그대로 전달한다.

```text
너는 Codex다. 작업 경로는 C:\Users\seong\Desktop\workspace\patima-naver-ss 이다.

반드시 먼저 0529수정계획.md 파일을 끝까지 읽고, 이 문서만을 기준으로 작업 범위를 잡아라. 보완필요사항.html은 참고 자료일 뿐이고, 이번 세션의 실제 구현 범위는 0529수정계획.md의 "이번 1차 작업의 목표"와 "구현 지침"이다.

이번 작업 목표:
- 주문/주문상품 핵심 필드는 장기 보관한다.
- OrderRecord.rawPayload와 OrderItem.rawPayload는 최근 기간만 운영 DB에 보관한다.
- ORDER_RAW_PAYLOAD_RETENTION_DAYS 환경변수를 도입한다. 기본값은 90일이다.
- cutoff보다 오래된 주문/주문상품의 rawPayload만 null로 정리한다.
- 주문 row나 주문상품 row 자체는 삭제하지 않는다.
- optionCode, optionManageCode, 매출, 수수료, 배송비, 매핑 필드는 절대 삭제하지 않는다.
- 마진 계산과 주문/매핑 목록이 rawPayload 없이도 동작해야 한다.
- 테스트와 타입체크를 통과시킨다.

작업 방식:
반드시 서브에이전트 2개를 사용한다.

서브에이전트 A: 개발자
- 역할: 구현 담당
- 0529수정계획.md를 기준으로 필요한 코드를 수정한다.
- 가능한 한 작은 변경으로 1차 목표를 달성한다.
- 전체 DB 정규화, 클라우드 DB 연결, 인증/인가 작업은 하지 않는다.
- 작업 후 backend test와 typecheck를 실행한다.
- 변경 파일과 테스트 결과를 요약한다.

서브에이전트 B: 평가자
- 역할: 코드 리뷰와 결함 평가 담당
- 개발자가 만든 diff와 테스트 결과를 검토한다.
- 특히 다음 결함을 찾아라:
  1. 주문/주문상품 row 자체를 삭제했는가?
  2. 핵심 필드를 실수로 지웠는가?
  3. rawPayload 보관 기간 계산이 KST 기준과 어긋나는가?
  4. paymentDate가 없는 주문 처리 기준이 불명확한가?
  5. storeId 범위를 넘어서 prune하는가?
  6. rawPayload null 상태에서 마진 계산/주문 목록/매핑이 깨지는가?
  7. 테스트가 부족한가?
  8. 환경변수 문서화가 빠졌는가?
  9. 이번 범위를 넘어서 불필요하게 큰 구조 변경을 했는가?
- 결함이 있으면 severity와 함께 개발자에게 재작업 지시를 작성한다.
- 결함이 없으면 "평가 통과"라고 명시한다.

루프 규칙:
1. 메인 에이전트는 먼저 현재 코드와 0529수정계획.md를 읽는다.
2. 개발자 서브에이전트에게 구현을 맡긴다.
3. 개발자가 작업을 끝내면 메인 에이전트는 테스트를 실행하고 diff를 정리한다.
4. 평가자 서브에이전트에게 diff와 테스트 결과를 전달해 평가시킨다.
5. 평가자가 결함을 지적하면 개발자 서브에이전트에게 재작업을 지시한다.
6. 재작업 후 다시 테스트하고 다시 평가한다.
7. 평가자가 결함 없음이라고 할 때까지 3~6을 반복한다.
8. 정말로 막히는 경우에만 사용자에게 질문한다. 막히지 않았으면 구현, 검증, 평가 루프를 끝까지 수행한다.

최종 응답에는 다음을 포함한다:
- 변경 요약
- 변경 파일 목록
- rawPayload retention 동작 방식
- 실행한 테스트 명령과 결과
- 평가자 통과 여부
- 남은 후속 과제
```

