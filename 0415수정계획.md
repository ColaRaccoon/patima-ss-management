# 2026-04-15 수정 계획

patima-naver-ss 프로젝트 수정 작업 상세 계획. 관리코드 수신 검증이 완료된 시점 기준으로 작성.

## 검증 완료 사항

`2026-04-12` 주문 데이터에서 `optionManageCode = BDL_RUN_VEST` 수신 확인. 관리코드는 `productOrder.optionManageCode` 필드로 정상 내려옴. 따라서 관리코드 기반 매핑 구현 가능.

---

## 작업 1. 관리코드(optionManageCode) 기반 함께배송 자동매핑

### 목적

함께배송(추가상품) 아이템은 메인 상품과 동일한 `externalProductId`를 공유하여 ID 매핑 불가. 현재 `optionCode` 기반 매핑을 사용하지만 `optionCode`는 주문마다 다른 값이 부여되는 불안정한 값. 판매자가 직접 입력한 `optionManageCode`(예: `BDL_RUN_VEST`)는 안정적이므로 이를 새로운 매핑 키로 사용.

### 수정 파일 및 내용

#### 1-1. `packages/shared/src/types.ts`

**SyncedOrderItemInput 타입 수정** (위치: `apps/backend/src/naver-commerce.service.ts` 내부에 있지만 shared 타입과 일치시켜야 함)

- `optionManageCode: string | null` 필드 추가

**CanonicalSalesUnit 타입 수정**

- `linkedManageCodes: string[]` 필드 추가 (기존 `linkedOptionCodes`, `linkedProductIds`와 동일한 구조)

**OrderItem 타입 수정**

- `optionManageCode: string | null` 필드 추가

#### 1-2. `apps/backend/src/naver-commerce.service.ts`

**normalizeOrderDetail() 메서드 수정** (lines 430~552)

- 결과 객체에 `optionManageCode: pickString(productOrder.optionManageCode)` 추가
- `optionCode` 추출 직후 위치에 삽입

#### 1-3. `apps/backend/src/mapping-seed.service.ts`

**판매단위 시드 생성 로직 수정** (lines 232~270)

- 함께배송 그룹 판매단위 생성 시:
  - 그룹 내 아이템들의 `optionManageCode`를 수집
  - 중복 제거 후 `linkedManageCodes` 배열로 설정
  - `linkedOptionCodes`는 백업용으로 유지 (관리코드 미설정 상품 대응)
- 일반 상품 판매단위는 `linkedManageCodes: []`로 빈 배열 유지

**findExistingSalesUnit() 매칭 로직 수정**

- 기존 판매단위 조회 시 `linkedManageCodes`도 비교 대상에 포함

#### 1-4. `apps/backend/src/sales-unit-auto-mapper.ts`

**recalculateOrderMappingsForStore() 우선순위 재정의** (lines 198~243)

매핑 우선순위를 다음과 같이 변경:

1. `confirmedAt` 있는 시그니처 매핑 (수동 확인, 기존)
2. **`optionManageCode` → `linkedManageCodes` (신규 추가, 최우선 ID 매핑)**
3. `optionCode` → `linkedOptionCodes` (기존)
4. `externalProductId` → `linkedProductIds` (함께배송 제외, 기존)
5. 시그니처 텍스트 매칭 fallback (기존)

**Map 캐시 추가** (lines 184~189)

- `manageCodeMap = new Map<string, string>()` 추가
- `idMappingSalesUnits.forEach`에서 `linkedManageCodes` 순회하여 맵에 추가

**resolveOrderItemByIds() 함수 수정** (lines 116~143)

- Phase 1에 `optionManageCode` 매핑 추가
- `resolvedBy: "optionManageCode"` 케이스 추가

#### 1-5. 마이그레이션 스크립트 작성

**신규 파일: `migrations/phase2-backfill-option-manage-code.sql`**

기존 주문 데이터의 `rawPayload`에서 `optionManageCode`를 상위 필드로 백필:

```sql
UPDATE order_items
SET payload = jsonb_set(
  payload, '{optionManageCode}',
  COALESCE(
    payload->'rawPayload'->'detail'->'productOrder'->'optionManageCode',
    'null'::jsonb
  )
)
WHERE payload->>'optionManageCode' IS NULL;
```

### 구현 순서

1. `packages/shared/src/types.ts` 타입 정의 추가
2. `naver-commerce.service.ts`의 `normalizeOrderDetail()` 수정
3. 마이그레이션 스크립트 작성 및 실행 (기존 데이터 백필)
4. `mapping-seed.service.ts` 판매단위 생성 로직 수정
5. `sales-unit-auto-mapper.ts` 매핑 우선순위 수정
6. 기존 판매단위 재생성 또는 수동으로 `linkedManageCodes` 보충
7. 전체 주문 재매핑 실행하여 검증

### 검증 방법

```sql
-- 관리코드로 매핑된 함께배송 아이템 비율 확인
SELECT 
  COUNT(*) FILTER (WHERE payload->>'optionManageCode' IS NOT NULL) as has_manage_code,
  COUNT(*) FILTER (WHERE payload->>'canonicalSalesUnitId' IS NOT NULL) as mapped,
  COUNT(*) as total
FROM order_items
WHERE payload->>'rawOptionInfo' LIKE '%함께배송%';
```

---

## 작업 2. 디버그 코드 정리

### 목적

관리코드 수신 검증 및 매핑 구현 완료 후 임시 디버그 코드 제거.

### 수정 파일 및 내용

#### 2-1. `apps/backend/src/naver-commerce.service.ts`

**디버그 로그 블록 제거** (lines 539~549)

```typescript
// 제거 대상
if (rawOptionInfo?.includes("[함께배송") || ...) {
  console.log("[DEBUG optionManageCode]", { ... });
}
```

#### 2-2. `apps/backend/src/mapping-seed.service.ts`

**debugGetOptionManageCodes() 메서드 전체 제거** (lines 375~420)

#### 2-3. `apps/backend/src/app.controller.ts`

**디버그 엔드포인트 제거**

- `GET /api/v1/debug/option-manage-codes/:storeId` 라우트 제거

### 선결 조건

- 작업 1이 완료되고 실제 주문 데이터로 매핑 검증이 완료되어야 진행 (최소 1주일 이상 관찰 권장)

---

## 작업 3. 광고 매핑 개선 — 스토어 전체 광고비 버킷 + 자동매핑 룰

### 목적

기존 광고 매핑은 캠페인명 텍스트 매칭만 지원. 실제 데이터 분석 결과, 다음 두 가지 개선이 필요:

- 특정 제품이 아닌 스토어 전체를 대상으로 하는 광고(카탈로그, 키워드타겟 등)를 별도 집계할 필요
- 실제 캠페인명에서 도출된 명확한 키워드 매핑 룰 적용

### 매핑 룰 설계

| 캠페인명에 포함된 키워드 | 매핑 대상 판매단위 |
|---|---|
| `한줄무릎보호대` | 한줄 무릎보호대 |
| `두줄무릎보호대`, `인피니티가드_두줄` | 두줄 무릎보호대 |
| `런닝양말` | 러닝양말 |
| `다이어트양말`, `인피니티가드_다이어트양말` | 다이어트양말 |
| `유쉴드`, `유쉴드마스크` | 자외선차단 마스크 |
| `카프슬리브` | 쿨토시 |
| `카탈로그`, `키워드타겟`, `무릎보호대 이외`, `인피니티가드`(한줄/두줄 미포함 단독) | **스토어 전체 광고비** |

### 수정 파일 및 내용

#### 3-1. `packages/shared/src/types.ts`

**CanonicalSalesUnit 타입 수정**

- `isStoreLevel: boolean` 플래그 추가 (기본값 false)
- 스토어당 `isStoreLevel: true` 인 판매단위는 1개만 허용

#### 3-2. `apps/backend/src/sales-unit.service.ts`

**스토어 전체 판매단위 생성 로직**

- 스토어 등록 시 또는 광고 매핑 최초 수행 시 자동으로 `isStoreLevel: true` 판매단위 생성
- `displayName`: "스토어 전체 광고비" 고정
- `linkedProductIds`, `linkedOptionCodes`, `linkedManageCodes`: 모두 빈 배열 (주문 매핑 불가)
- 중복 생성 방지 로직 필요

#### 3-3. `apps/backend/src/ad-mapping-engine.ts`

**evaluateAdMapping() 함수 수정** (lines 89~)

매핑 로직 순서:

1. `MANUAL_MAPPED` override 우선
2. `INTENTIONALLY_UNMAPPED` override 처리
3. **신규: 스토어 전체 패턴 우선 검사**
   - `카탈로그`, `키워드타겟`, `무릎보호대 이외` 포함 시 → 스토어 전체 판매단위로 매핑
   - `인피니티가드` 단독 (한줄/두줄 미포함) → 스토어 전체 판매단위로 매핑
4. 기존 텍스트 기반 자동매핑 (normalizedMatchAliases) 수행
5. 매칭 실패 시 UNMAPPED

**신규 함수: `resolveStoreLevelPattern(campaignName: string): boolean`**

- 스토어 전체 키워드 포함 여부 판정
- `인피니티가드` 단독 케이스 처리 (한줄/두줄/다이어트양말 등 특정 키워드가 함께 있으면 false 반환)

#### 3-4. `apps/backend/src/campaign-mapping.service.ts`

**기본 매핑 패턴 시드 작업**

- 스토어 첫 설정 시 위 매핑 룰의 키워드들을 `campaignMappings` 테이블에 시드 데이터로 자동 등록
- 사용자가 해당 룰을 수정/삭제할 수 있도록 일반 패턴으로 저장 (하드코딩 금지)

#### 3-5. `apps/frontend/components/sales-units/sales-units-view.tsx`

**판매단위 목록 UI**

- `isStoreLevel: true` 판매단위는 목록 상단에 고정 배치
- 구분 배지 표시 (예: "스토어 전체" 태그)
- 편집 시 `linkedProductIds`, `linkedManageCodes` 등 매핑 필드 비활성화
- 원가 설정(`unitCost`, `otherCost`) 입력 필드 숨김 처리

#### 3-6. `apps/frontend/components/mappings/mappings-view.tsx`

**매핑 UI**

- 캠페인 매핑 드롭다운에 "스토어 전체 광고비" 옵션을 최상단 고정
- 자동 매핑된 스토어 전체 항목 시각적 구분

#### 3-7. `apps/frontend/components/profits/profits-view.tsx`

**수익 대시보드**

- `isStoreLevel: true` 판매단위 행을 별도 섹션으로 분리 표시
- 합계 계산은 전체에 포함 (광고비는 실제 비용이므로)
- 원가/수량 컬럼은 `-` 처리 (해당 없음 표시)

### 수정 불필요 항목

- `apps/backend/src/helpers.ts`의 `calculateDailyProfitRows()` 함수
  - 광고비 루프와 주문 루프가 분리되어 있어 `revenue=0` 인 판매단위도 자연스럽게 처리됨
  - 광고비 있는 날만 row가 생성되므로 phantom row 문제 없음
  - `unitCost`, `otherCost`는 주문 수량에 곱해지는 구조라 스토어 전체 판매단위에는 적용되지 않음

### 구현 순서

1. `CanonicalSalesUnit.isStoreLevel` 타입 정의
2. 스토어 전체 판매단위 자동 생성 로직 구현
3. `ad-mapping-engine.ts`의 `resolveStoreLevelPattern` 및 매핑 우선순위 로직 추가
4. 기본 매핑 패턴 시드 등록
5. 프론트엔드 UI 3곳 수정 (판매단위 목록, 매핑 화면, 수익 대시보드)
6. 기존 광고 데이터 재매핑 실행하여 검증

### 검증 방법

- 업로드된 실제 캠페인 엑셀(`전체캠페인_20260414_*.xlsx`)로 매핑 결과 확인
- 스토어 전체 판매단위에 `카탈로그`, `키워드타겟`, `인피니티가드`(단독) 캠페인이 정확히 매핑되는지 확인
- 제품별 판매단위에 상품명 키워드 포함 캠페인이 매핑되는지 확인

---

## 전체 작업 순서 권장안

1. **작업 1** (관리코드 매핑) — 최우선. 검증 완료된 상태라 즉시 착수 가능
2. **작업 3** (광고 매핑) — 작업 1과 독립적으로 병행 가능
3. **작업 2** (디버그 코드 정리) — 작업 1 완료 후 1주일 이상 운영 데이터 관찰 후 진행

## 테스트 항목

- 작업 1 완료 후: 함께배송 아이템 자동매핑률 측정 (목표: 관리코드 설정된 항목 100%)
- 작업 3 완료 후: 실제 광고 엑셀 업로드 시 자동매핑률 측정 (목표: 85% 이상)
- 기존 매핑 로직 회귀 테스트: 일반 상품 매핑, 시그니처 기반 매핑이 기존과 동일하게 동작하는지 확인
